#!/usr/bin/env node
// TODO(theme port / ADR-005): stub — the real hex-lint guard arrives with the demo-console
// theme port. It will fail the build on raw hex colors anywhere outside tokens.css files,
// enforcing CLAUDE.md hard rule 2 (all UI color via --color-* custom properties).
process.exit(0);
