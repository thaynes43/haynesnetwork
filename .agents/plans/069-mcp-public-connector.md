# PLAN-069: Public MCP connectors (ChatGPT, Claude Code, Codex): build, deploy, live-verify

- **Status:** In progress — S1–S7 done 2026-09-24 (v0.98.0 live, haynes-ops #3160, probes green); S8 partially done: the S8 pre-checks passed from dev-env 2026-09-24, the owner connected Codex and ChatGPT 2026-09-25 and both answered watch questions (read tools only; audited 2026-09-26, see _Evidence (S8 live audit)_). Still open in S8: a token refresh from each, ChatGPT's zero-flip mark and undo, a Claude Code connect, a non-owner consent (AC-27) and a Disconnect (AC-28); then S9
- **ADRs:** ADR-091 (Proposed; Accepted at S9) · **Design:** DESIGN-050 · **Flow:** FLOW-001 ·
  **PRD:** R-247..R-251, US-14, AC-25..AC-28 · **Glossary:** T-254..T-259 · **Ops:** OPS-016
- **Owner:** whoever holds the session; this plan is the tracked owner.
- **Owner rulings:** 2026-09-23 directive, _"We should do what we did for cigar-journal so I can hook
  haynesnetwork-mcp up to ChatGPT"_; 2026-09-23 ruling, _"The auth path needs to be user aware too"_
  (no owner gate: any signed-in user may connect, the tools answer for that user's tracked account,
  Plex write-back stays owner-only; ADR-091 C-04, DESIGN-050 D-07).
- **Depends on:** PLAN-068 (completed; the seven tools and `@hnet/mcp` exist). **Followed by:**
  PLAN-070 (tracking household accounts, so a non-owner's connector has history to answer from).
- **Cross-repo:** haynes-ops (Gatus probes, Loki alert, the dev-env egress entry, the image tag).
  No IngressRoute change.

## Evidence (surveys 2026-09-23)

| Fact                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Source                                                                                                                                                                                               |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| cigar-journal already runs this authorization server for ChatGPT, Claude Code and Codex: DCR, PKCE S256, resource binding, rotating refresh families, consent through its own session; about 80 refresh rotations and 20 re-authorizations from ChatGPT in 30 days, no reuse detections                                                                                                                                                                                                                                                                                                         | ADR-091 context; cigar-journal ADR-005, FLOW-003, `packages/oauth/src/{config,crypto,errors,metadata,provider,validate,logger}.ts` (the port source; `service-tokens.ts` and the CLI are not ported) |
| Claude Code's OAuth is headless-drivable by pasting the redirect URL back; Codex uses `codex mcp login` with a `127.0.0.1` callback; ChatGPT caches authorization-server metadata and refreshes tool schemas only after a connector refresh plus a new chat                                                                                                                                                                                                                                                                                                                                     | cigar-journal `docs/mcp/client-compatibility.md`                                                                                                                                                     |
| All three IngressRoutes (`haynesnetwork.com`, `www.`, `haynesnetwork.haynesops.com`) match `PathPrefix(/) && !PathPrefix(/api/mcp)`, so `/mcp`, `/oauth/*` and `/.well-known/*` are already routed and `/api/mcp` stays excluded                                                                                                                                                                                                                                                                                                                                                                | haynes-ops origin/main `kubernetes/main/apps/frontend/haynesnetwork/app/ingressroute.yaml`                                                                                                           |
| **dev-env egress to `haynesnetwork.com`:** the dev-env CiliumNetworkPolicy allows DNS for `haynesnetwork.com` and `*.haynesnetwork.com`, but its HTTPS (443) `toFQDNs` rule lists only `cigars.haynesnetwork.com`. `https://haynesnetwork.com/api/health` still answered 200 from dev-env, because the apex and `cigars.` resolve to the same Cloudflare addresses (`172.67.143.112`, `104.21.27.181`) and Cilium admits those IPs through the `cigars.` entry. That is a coincidence of shared anycast IPs, not a rule; S7 adds an explicit `matchName: "haynesnetwork.com"` to the HTTPS rule | haynes-ops origin/main `kubernetes/main/apps/dev/dev-env/app/networkpolicy.yaml`; probes from the dev-env pod                                                                                        |
| `haynesnetwork.com` also publishes AAAA records (`2606:4700:3035::6815:1bb5`, `2606:4700:3037::ac43:8f70`) and the pod has no IPv6 egress (`curl -6` fails), so Node CLIs in dev-env need `NODE_OPTIONS=--dns-result-order=ipv4first` if a connect stalls. **DESIGN-050 D-12 says this "does not apply" to dev-env because dev-env uses the hop; it does apply to S8, which drives the public name from dev-env**                                                                                                                                                                               | probes from the dev-env pod; cigar-journal client-compatibility environment note                                                                                                                     |
| Live principal data: the owner's `watch_accounts` row has **`app_user_id` NULL**, while `user_account_map` has 12 rows, all with `plex_user_id`, and the owner's Plex id maps to exactly one app user, the Admin. So the D-07 path (`users.id` → Plex Account Map → `watch_accounts`) resolves the owner today, and `app_user_id` must not be used. `user_account_map.plex_user_id` is text and `watch_accounts.plex_account_id` is bigint: the join needs a cast                                                                                                                               | read-only queries on a CNPG replica (`postgres16-7`), 2026-09-23                                                                                                                                     |
| No test account exists: 0 of 14 app users has "test" in the email or display name, and sign-in is Authentik with Plex only (no local accounts), so the driver cannot create a second user                                                                                                                                                                                                                                                                                                                                                                                                       | the same read-only query; hard rule 5                                                                                                                                                                |
| `watch_marks.consumer` is free text (no CHECK), so `oauth:<client_id>` needs no migration; `plex_result` already allows `none`                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `packages/db/src/schema/watch-marks.ts`, `enums.ts`                                                                                                                                                  |
| The `rate_limit` table is Better Auth's shared bucket store (PLAN-063), keyed by `key`, pruned by `last_request`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | `packages/db/src/schema/rate-limit.ts`                                                                                                                                                               |
| The login button hard-codes `callbackURL: '/'`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `apps/web/app/login/login-button.tsx`                                                                                                                                                                |
| The settings navigation is the user menu in `apps/web/components/top-bar.tsx` (Collection settings, Trash settings, Admin settings); `(app)/settings/` holds only `trash/` today                                                                                                                                                                                                                                                                                                                                                                                                                | code                                                                                                                                                                                                 |
| Migration numbering: the last migration is `0077_watch_companion.sql`, so this plan takes `0078`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | `packages/db/migrations/`                                                                                                                                                                            |
| haynesnetwork's Loki rules live in `lokirule.yaml` (only `severity: critical` reaches a human; everything else routes to `null`); its Gatus endpoint file is `gatus.yaml` (public resolver `1.1.1.1`)                                                                                                                                                                                                                                                                                                                                                                                           | haynes-ops origin/main `kubernetes/main/apps/frontend/haynesnetwork/app/`                                                                                                                            |

## Evidence (S8 live audit, 2026-09-26)

A read-only sweep at 2026-09-26 ~08:10Z of everything since go-live: the web pods' log in Loki
(`{namespace="frontend", app="haynesnetwork", container="app"}`), the traefik-external access log (the
internal ingress's access log is not in Loki), and two read-only database reads:
`SET default_transaction_read_only` psql reads on a CNPG replica, and one script on the **primary**
(`pg_is_in_recovery()` false), run through the web container's `DATABASE_URL` inside
`BEGIN READ ONLY … ROLLBACK`. OPS-016 §3 asks for a replica, so the facts that came only from the
primary read (the 1.2 s code exchange, the refresh-token expiry, the 7-day mark count) were read again
on the replica `postgres16-11` later on 2026-09-26 and matched. Client ids are shown as 8-character
prefixes; no IP, `state`, `code_challenge`, token or hash was copied out. Times are UTC.

| Fact                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Source                                                                                                                                             |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| Three clients have registered, all public (`token_endpoint_auth_method` `none`, no secret): `3a1a3d27` "dev-env precheck" (2026-09-24T03:58:00Z, redirect `127.0.0.1:8765`, the S8 pre-check from dev-env; never authorized, `last_used_at` NULL), `1558985c` "Codex" (2026-09-25T00:33:48Z, redirect `127.0.0.1:62061`) and `ec531e20` "ChatGPT" (2026-09-25T00:44:50Z, redirect host `chatgpt.com`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | `oauth_clients` on a replica; `[auth] client_registered`                                                                                           |
| **Codex** is the owner's own Codex CLI (`codex-mcp-client/0.155.0-alpha.9.2`), registered over residential IPv6 from his home network; the dev-env pod has no IPv6 egress, so it did not run from dev-env. A signed-out `/oauth/authorize` went to `/login?next=`, he signed in and came back to consent (D-09, live), approved at 00:34:01Z, and the CLI exchanged the code 0.12 s later. Six tool calls 00:34:38Z..00:35:35Z: `recent_history` ×2, `recommend` ×4                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | web log (`client_registered`, `authorize_started`, `consent_granted`, `token_issued`, `tool_called`); traefik-external                             |
| **ChatGPT** registered from its backend (`Python/3.14 aiohttp`, no `scope` in the registration; its authorization covered all three scopes). The owner was already signed in; the consent page was open 3 min 37 s (00:44:55Z..00:48:32Z) and the code was exchanged 1.2 s after Approve (`openai-connectors-oauth/1.0`). Fourteen tool calls 00:50:38Z..00:51:06Z, user agent `openai-mcp/1.0.0 (Codex)`: `unfinished` ×2, `recommend` ×4, `recent_history` ×2, `watch_status` ×6                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | web log; traefik-external; `oauth_authorization_codes` (`consumed_at`) on the primary, re-read on the replica                                      |
| Both `consent_granted` audit rows are the owner's user, scopes `watch:read`, `watch:write`, `offline_access`. There is no other audit row: no non-owner consent (AC-27 not exercised), no `consent_denied`, no `client_disconnected` (AC-28 not exercised)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | `oauth_audit` on a replica                                                                                                                         |
| **No refresh yet.** Each client has one refresh family (`6d3d456e` Codex, `d625eb8e` ChatGPT) holding one token, 0 rotations, live until 2026-11-24, and one access token, expired 2026-09-25 01:34Z and 01:48Z. The external ingress saw only two `POST /oauth/token` calls, both the code exchange; neither client has called since 2026-09-25 00:51Z                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | `oauth_refresh_tokens`, `oauth_access_tokens` on a replica, except the refresh expiry (the primary read, re-read on the replica); traefik-external |
| **No mark or undo from a connector:** 0 `watch_marks` rows with an `oauth:` consumer, and 0 marks from any consumer between go-live (v0.98.0, 2026-09-24T03:57Z) and the audit (the last before it were three hop marks at 2026-09-24T01:08Z; hop watchlist changes from 2026-09-26T08:36Z on are after the audit). The replica's `TimeZone` is `America/New_York`, so a bare date literal such as `'2026-09-24'` means 04:00Z: write cutoffs with a `Z`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `watch_marks` on the primary (7 days by consumer) and on a replica (re-read with a UTC cutoff)                                                     |
| `[auth]` events never logged since go-live: `consent_denied`, `token_refreshed`, `refresh_reuse_detected`, `refresh_rejected`, `token_revoked`, `code_replayed`, `audience_mismatch`, `authorize_rejected`, `rate_limited`. No `[oauth]` unexpected error and no `[mcp] last_used stamp failed` line                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | web log 2026-09-22..26                                                                                                                             |
| **AC-26's log clause passes:** a scan of every web line since 2026-09-24 for base64url runs of 40+ characters and for `access_token`, `refresh_token`, `code_verifier`, `client_secret`, `bearer`, `authorization:` and `verifier` found 4 lines, all false positives (3 × the `refresh_token` grant-type name in `client_registered`, 1 × a 42-character Next.js server-action id). Tokens are 43 characters and hashes 64 hex; neither appears. Fingerprints are 6 hex characters as designed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Loki                                                                                                                                               |
| **ADR-091 C-13 answered:** ChatGPT did not send `GET /mcp` in its first session (2026-09-25). Codex sends `GET /mcp` before each connect round, got 405 nine times and carried on over POST. Neither client stalled, so the empty-stream GET fallback is not needed. Three later `GET /mcp` 405s with the user agent `Mozilla/5.0 (compatible)` (2026-09-25 20:29Z and 23:54Z ×2, no POST, no token call) are unattributed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | traefik-external                                                                                                                                   |
| Refused calls: ChatGPT's first three tool calls (`recent_history`, `unfinished`, `recommend`, 00:50:38Z..40Z) and one of Codex's `recommend` calls (its third call, 00:35:27Z, after a successful `recent_history` and `recommend`) were refused `invalid_args`, and each was followed by a successful call of the same tool (ChatGPT's 12 to 13 s later, Codex's 8 s). Every refusal carries a 43-character issue after `Invalid arguments for <tool>: `. That is the length of zod 4's `limit: Too big: expected number to be <=10.` (`<=20` for `recent_history`), a limit over the schema's maximum, but other issues are 43 characters too: an 11-character unrecognized key (`arguments: Unrecognized key: "max_results".`) for any tool, and `days: Too big: expected number to be <=365.` for `recent_history`. Arguments are never logged, so the cause is not known                                                                                                                                                                                         | web log `chars=`; the messages measured with zod 4.4.3 and `packages/mcp/src/server.ts` `invalidArgs`                                              |
| Right after the code exchange (00:48:34Z), a `POST /mcp` from ChatGPT's backend (`aiohttp`) answered 401, apparently sent without the new bearer. Then its first `POST /mcp` from `openai-mcp/1.0.0` (00:48:35Z, 413 bytes) answered 400 and its retry 0.2 s later 200; the cause of the 400 is not logged (`[mcp]` logs tool calls only)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | traefik-external                                                                                                                                   |
| Every OAuth line came from the v0.98.0 pods (`78bf5ff8df-*`); the hop kept answering (tool calls 2026-09-25T20:31Z and 2026-09-26T04:02Z, all ok). No request reached `/settings/connections` or `/api/mcp` through the external ingress                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | web log; traefik-external                                                                                                                          |
| **Runbook defects found by the sweep:** (1) the Loki selector in OPS-016 §2 and S8 below said `container="main"`, which is the sync CronJobs' container; the web container is `app` (the haynes-ops alert rules already use `app`). Fixed in both. (2) OPS-016 §3's connected-clients query joined `r.client_id = c.id` and failed live with `operator does not exist: text = uuid`: the token, code and transaction tables reference `oauth_clients.client_id` (text), not the uuid `id`. Fixed to `r.client_id = c.client_id` and run on a replica 2026-09-26. (3) haynes-ops `kubernetes/main/apps/frontend/haynesnetwork/app/lokirule.yaml`'s header comment said "the MCP connector auth alerts are warnings", but `McpConnectorRefreshReuseDetected` is `severity: critical` (only `McpConnectorAuthRejectionBurst` is `warning`). The rules were right and the comment wrong: **fixed by haynes-ops #3206** (merged 2026-09-26T08:46Z; the comment now names `McpConnectorRefreshReuseDetected` as critical and `McpConnectorAuthRejectionBurst` as a warning) | the sweep; the replica; haynes-ops origin/main                                                                                                     |

## Build stages

| #   | Stage                                                | Deliverable                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Gate                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| --- | ---------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| S1  | Docs merged                                          | ADR-091 (Proposed), DESIGN-050, FLOW-001, PRD R-247..R-251 / US-14 / AC-25..AC-28 / Q-14 resolved / Q-12 answered for connectors, glossary T-254..T-259, DDD-002 BC-06, OPS-016 skeleton, this plan, the PLAN-070 stub, HANDOFF                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | one `docs:` PR, squash-merged; the ADR/design inconsistencies found while writing the paperwork (listed in the HANDOFF entry and the PR) ruled or fixed by the driver before S2 starts                                                                                                                                                                                                                                                                                                                                      |
| S2  | `@hnet/oauth` port + migration 0078 + domain writers | `packages/oauth` (`@hnet/oauth`, depends on `@hnet/db` and zod only) ported from cigar-journal per D-01; the Dockerfile deps-stage `COPY packages/oauth/package.json` line; `0078_oauth_connectors.sql` + `_journal.json` + schema files + CHECKs from `enums.ts` arrays for the five D-03 tables; `@hnet/domain` `oauth/*`: `grantConsent`, `denyConsent`, `disconnectClient`, `revokeFamilyOnReuse`, each with its audit row in the same transaction; the guard lists. **Precondition:** D-01 says the domain writers are "the only writers of the OAuth tables" and join `no-direct-state-writes.test.ts`, which allows writes only from `packages/domain`, but `@hnet/oauth` (which may not import `@hnet/domain`) owns registration, transactions, codes, issue, rotation and pruning. The driver rules where those writes live (a guard allowance for `packages/oauth`, or the writes move into the domain) and records it in DESIGN-050 before this stage starts | migrate clean on embedded PG16; a test asserts the journal lists 0078; the `@hnet/oauth` unit matrix in DESIGN-050's test strategy (metadata, DCR caps, loopback matching, S256 constant-time, code single use and replay, rotation, reuse revocation, scope narrowing, revoke semantics, pruner bounds, no token in any log line); the four writers on embedded PG16 with their audit rows; the guard test passes with the ruled lists                                                                                     |
| S3  | Web routes + `/login?next=`                          | `apps/web` routes outside the `(app)` gate: both `.well-known` documents (each with its `/mcp` variant), `/oauth/register`, `/oauth/authorize`, `/oauth/consent` (page + server action with the decision bound in), `/oauth/token`, `/oauth/revoke`; CORS only where D-02 says; every redirect built from `BETTER_AUTH_URL`, never from the request; the D-10 rate limits through `rate_limit` with namespaced keys; the D-14 consent, expired and bad-request copy; `/login?next=` (D-09) in the login button and the already-signed-in redirect. DESIGN-050 D-10 names the limits but not the response status: rule it (for example 429 with `Retry-After`) in the design before coding                                                                                                                                                                                                                                                                               | route tests: metadata bodies and headers; register validation and its rate limit; authorize (no session → `/login?next=`; a session → transaction + consent; every D-05 parameter error; unknown client or redirect → the bad-request page with no redirect); the consent action honours Approve and Deny separately; `/login?next=` refuses `//evil`, absolute URLs and over-long values; token and revoke handlers                                                                                                        |
| S4  | Public `/mcp` + the `@hnet/mcp` OAuth consumer       | `authenticateOAuth(req)` → `McpConsumer { name: 'oauth:<client_id>', scopes, userId }`; the user-aware principal of D-07: `users.id` → `user_account_map.plex_user_id` → `watch_accounts` with `tracked = true` (cast the text id; never `app_user_id`), "Watch history isn't set up for your account yet." when none; `AnswerContext.owner` becomes `account` and the domain flows assert a tracked account instead of the owner; a non-owner `mark_watched` records `plex_result = 'none'` with the D-07 answer and never calls Plex; `apps/web/app/mcp/route.ts` exporting only `POST`; 401 / 403 challenges; `last_used_at` stamping at most once a minute. The hop consumer and `/api/mcp` are untouched                                                                                                                                                                                                                                                           | `@hnet/mcp` tests on a seeded database: valid, expired, revoked and wrong-resource tokens; the hop token refused on `/mcp` and an OAuth token refused on `/api/mcp`; scope filtering of `tools/list`; mapped + tracked → that account's answers; unmapped and untracked → "isn't set up"; a non-owner mark writes no Plex call (recording fake Plex); the owner's answers byte-identical to the hop's for the same fixture; the Voice Budget tests still pass; the route test for 401/403 headers and 405 on GET and DELETE |
| S5  | Connected apps page                                  | `(app)/settings/connections` per D-08 and the D-14 copy: one row per app with a live family or unexpired access token for the signed-in user; **Disconnect** as a `ConfirmButton` (armed "Confirm disconnect", width reserved for the armed label, ADR-014 / ADR-015); the Disconnected state in place; a user-menu entry in the settings cluster of `top-bar.tsx`. UX and copy stay on the driving session (never delegated down). D-08 does not say who sees the menu entry, and its "an admin sees the same list" predates the user-aware ruling: the driver settles both in DESIGN-050 first                                                                                                                                                                                                                                                                                                                                                                        | component and route tests; the Playwright resize matrix (AC-10) on the page with 0, 1 and 5 rows; Disconnect revokes and writes `client_disconnected`; no neighbour moves when a row is armed or disconnected                                                                                                                                                                                                                                                                                                               |
| S6  | Tests green + release                                | the feature PR(s); an Opus review of each before merge, every finding fixed on the branch or answered on the PR with a reason; release-please PR merged; `ghcr.io/thaynes43/haynesnetwork:vX.Y.Z` signed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | `pnpm lint && pnpm lint:css && pnpm typecheck && pnpm test && pnpm build` green locally and the required checks green; `pnpm dev:local` completes DCR → consent (stub OIDC) → token → `unfinished` over curl (recorded in OPS-003); image and signature on GHCR                                                                                                                                                                                                                                                             |
| S7  | haynes-ops + deploy                                  | one haynes-ops PR: two Gatus endpoints in `frontend/haynesnetwork/app/gatus.yaml` (`POST https://haynesnetwork.com/mcp` with an empty body → `[STATUS] == 401`; `GET https://haynesnetwork.com/.well-known/oauth-protected-resource` → 200 and `[BODY].resource == https://haynesnetwork.com/mcp`); a new group in `lokirule.yaml` for `[auth] refresh_reuse_detected` and more than 20 `authorize_rejected` / `rate_limited` in 10 minutes (D-11; the design gives no severity and only `critical` pages, so the driver picks and records it); `matchName: "haynesnetwork.com"` in the dev-env policy's HTTPS rule (a normal merge: not under `resources/**`, so it does not bounce the pod); the image tag bump. **No IngressRoute change.** Gatus conditions have no response-header placeholder, so the probe asserts the 401 but not the `resource_metadata` header; the OPS-016 §2 curl checks the header                                                         | flux-local green; reconcile; rollout 3/3 on the new tag; `/api/health` 200; both Gatus endpoints green; the OPS-016 §2 curls answer as documented; `https://haynesnetwork.com/api/mcp` still 404; the Movie Room hop unchanged (`tools/list` 2,712 bytes from the HA pod, OPS-015 §2)                                                                                                                                                                                                                                       |
| S8  | Live gate                                            | Claude Code, then Codex, then ChatGPT, then a second user, then Disconnect, as set out below. **Partially done 2026-09-25..26:** Codex and ChatGPT connected by the owner; the rest is in _S8 status_ below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | AC-25..AC-28 pass live; DESIGN-050 Q-01 (claude.ai) recorded either way; the ChatGPT-with-405 question of ADR-091 C-13 answered (the empty-stream GET fallback shipped only if a client stalls)                                                                                                                                                                                                                                                                                                                             |
| S9  | Close                                                | ADR-091 → Accepted; DESIGN-050 → Accepted; FLOW-001 → Accepted; OPS-016 → Active, with the per-client notes from S8; HANDOFF; this plan → `completed/`; PLAN-070 picked up next                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | a `docs:` PR merged                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |

### S8 in detail

The consent step needs a real signed-in haynesnetwork user, and only the owner can sign in as the owner
(Authentik with Plex). So every client below has an **owner step** (open a URL, sign in, approve) and a
**driver step** (run the client, verify from logs and the database). Each owner step is pushed to his
phone as one question at the moment it arises. An authorization code lives **60 seconds**
(DESIGN-050 D-03): the redirect URL must reach the client within a minute of Approve, or the exchange
answers `invalid_grant` and the step starts again.

Verification sources for every client: Loki `{app="haynesnetwork", container="app"} |= "[auth]"` and
`|= "[mcp] tool_called"` (the `consumer` field; the web container is `app`, while `main` is the sync
CronJobs'), and read-only SQL on a CNPG replica (OPS-016 §3).

1. **Claude Code from dev-env.** Driver: in a scratch directory (never the dev-env `haynesnetwork` hop
   entry), add the server under a distinct name (`hnet-public`, `https://haynesnetwork.com/mcp`,
   Streamable HTTP) with `NODE_OPTIONS=--dns-result-order=ipv4first`, start the authentication, and
   hand the owner the authorization URL. Owner: open it on his device, sign in if asked, check that the
   consent page says "Sends you back to localhost" (or `127.0.0.1`), Approve, and paste the browser's
   final `localhost` URL (the page itself will not load) into the Claude Code session, which he can
   drive from his phone (`agent-run --interactive`). Driver verifies: `client_registered` with a
   `Claude Code (…)` name, `consent_granted`, `token_issued`, a `tool_called` line with
   `oauth:<client_id>` for `unfinished`, and, after the 1-hour access token expires, a second call that
   succeeds with `token_refreshed` logged.
2. **Codex from dev-env.** Driver: register the server with Codex under the same distinct name, run
   `codex mcp login hnet-public` (it listens on `127.0.0.1:<port>`), and hand the owner the URL. Owner:
   approve and send back the browser's final `http://127.0.0.1:<port>/callback/…` URL. Driver: deliver
   it inside the pod with `curl` within the minute, then verify as for Claude Code (a `Codex` client,
   a tool call, a refresh). Remove both test registrations from the CLIs afterwards.
3. **ChatGPT (owner-driven end to end).** Owner: in ChatGPT's connector settings (Developer mode),
   create a connector with the MCP server URL `https://haynesnetwork.com/mcp` and OAuth
   authentication; when the haynesnetwork consent page opens, check that it says "Sends you back to
   chatgpt.com" and Approve. In a **new chat** with the connector enabled, ask the three US-13
   questions, then "I already watched \<title\>" for the title the driver names, then "undo that".
   The next day (or after an hour), ask once more. Driver: before the chat, pick the mark title by the
   live-test rule below and record its Plex state on the preferred server; verify `client_registered`
   (`ChatGPT`, redirect host `chatgpt.com`), `consent_granted`, `token_issued`, one `tool_called` per
   question with `oauth:<client_id>`, the `watch_marks` rows with `consumer = 'oauth:<client_id>'`, the
   Plex state identical after the mark and the undo, and `token_refreshed` on the later question.
   If ChatGPT stalls on the 405 GET, ship the empty-stream GET fallback (ADR-091 C-13) and repeat.
4. **A second user (AC-27).** There is no test account and the driver cannot make one, so this step is
   owner-run: a household member (or another Plex account the owner uses) signs in and connects any
   client, most simply by opening a fresh Claude Code authorization URL from step 1 in a browser signed
   in as that user and approving. Driver verifies: `consent_granted` for a user id that is not the
   owner's, and `unfinished` answering "Watch history isn't set up for your account yet." Then that
   user disconnects it.
5. **Disconnect (AC-28).** Owner: on **Connected apps**, disconnect the Claude Code test client.
   Driver: the next call from that client answers 401, `client_disconnected` is in the audit, and
   the row reads Disconnected without moving. Then disconnect the Codex test client the same way, and
   leave ChatGPT connected.
6. **Optional (DESIGN-050 Q-01).** If the owner wants it, the same with a claude.ai custom connector;
   record the result in OPS-016 either way.

**Live-test rule for marks:** a live `mark_watched` is only made on a title with **no unwatched
regular episode on the preferred server** (zero flips, so Plex does not change; the PLAN-068 S12
lesson), and it is followed by `undo_last_change`. The driver checks the preferred server's leaves
before and after. A non-owner mark is never exercised live in this plan: no non-owner account is
tracked until PLAN-070, and its no-Plex rule is proven by S4's recording-fake test.

#### S8 status (2026-09-26)

What each step has shown live, and what is left (the evidence is in _Evidence (S8 live audit)_). The
owner steps go to his phone one question at a time, as above; the driver verifies each from the log and
the replica.

| Step                          | Done                                                                                                                                                                                                                                  | Still open, and who                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ----------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1. Claude Code                | nothing: no Claude Code client has registered                                                                                                                                                                                         | **Owner** approves a Claude Code connect that the driver starts from dev-env as written in step 1, and asks again after the hour so a refresh is logged                                                                                                                                                                                                                                                                                                                                                        |
| 2. Codex                      | 2026-09-25, by the owner from his own PC (not dev-env): registration, the `/login?next=` sign-in, consent, token, six tool calls (`recent_history`, `recommend`). No dev-env Codex registration is needed for AC-26                   | **Owner** asks Codex one more watch question (its access token expired 2026-09-25 01:34Z, so the next call has to refresh); the driver checks `token_refreshed` and a rotated second token in family `6d3d456e`                                                                                                                                                                                                                                                                                                |
| 3. ChatGPT                    | 2026-09-25: registration (redirect host `chatgpt.com`), consent, token, fourteen tool calls covering `unfinished`, `recommend`, `recent_history` and `watch_status`; C-13 answered (it did not send `GET /mcp` in this first session) | The driver names a title by the live-test rule above and records its Plex state; then the **owner**, in a new ChatGPT chat, says "I already watched \<title\>" and then "undo that". The driver checks `token_refreshed` (its access token expired 2026-09-25 01:48Z, so this chat refreshes), a `watch_marks` row with `consumer = 'oauth:ec531e20…'` and zero flips, its revert, and the Plex state unchanged. PLAN-071 S6 also needs a ChatGPT chat (after a connector refresh); one session can serve both |
| 4. A second user (AC-27)      | nothing: both consents are the owner's                                                                                                                                                                                                | **Owner** (with a household member or another Plex account he uses), as written in step 4                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 5. Disconnect (AC-28)         | nothing: no `client_disconnected` row                                                                                                                                                                                                 | **Owner** disconnects the Claude Code test client on **Connected apps** once step 1 is done; the driver checks the 401 and the audit row. Step 5 as written also disconnects the Codex client, which is now the owner's own install rather than a dev-env test registration: the driver asks him before that second disconnect                                                                                                                                                                                 |
| 6. claude.ai (optional, Q-01) | nothing                                                                                                                                                                                                                               | the owner's choice; OPS-016 records the result either way                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| AC-26 "no secret in any line" | passes (leak scan 2026-09-26)                                                                                                                                                                                                         | re-run the scan when the refreshes and the mark are in                                                                                                                                                                                                                                                                                                                                                                                                                                                         |

The `dev-env precheck` registration (`3a1a3d27`) is the pre-check's and was never used; it owns no
token, code or transaction, so the inline pruner (run after `/oauth/token` requests) removes it on the
first token request after 2026-10-24, when it is 30 days old.

## Invariants a reviewer must not let regress

1. The hop token is never accepted on `/mcp`, and a Delegated Token is never accepted on `/api/mcp`.
2. `/api/mcp` is never routed by an IngressRoute; the public surface needs no route change and gets none.
3. There is no owner gate, and the owner is never a default: an OAuth consumer answers only for its own
   token user's Tracked Account, an unmapped or untracked user gets "isn't set up", and no tool accepts
   an account or user id. Plex is written only when the Tracked Account is the Server Owner's.
4. A Delegated Token is never a login: it opens no session, and nothing but `/mcp` reads it (hard rule 5).
5. Tokens, codes, client secrets and PKCE verifiers are never logged or stored in the clear; log lines
   carry at most a 6-character hash fingerprint; tokens are stored only as SHA-256 hashes.
6. Stateless: no in-process token or session cache; every replica validates with a database lookup; no
   `Mcp-Session-Id`; a disconnect takes effect on the very next request.
7. Scopes are exactly `watch:read`, `watch:write` and `offline_access`, advertised and granted; nothing
   else ever appears in the metadata.
8. PKCE S256 and `state` are always required; `plain` is refused; consent is never remembered; the
   consent page always shows the redirect host.
9. The published paths (D-02) never change and gain no aliases.
10. The Voice Budget tests stay, and the hop's answers are unchanged.

## Out of scope (tracked elsewhere)

- Tracking household accounts, so a non-owner connector has history: PLAN-070.
- Plex write-back for anyone but the Server Owner: needs per-person Plex tokens, not planned.
- Operator-minted service tokens (cigar-journal ADR-011): not ported (ADR-091 option 5).
- Seerr requests from a connector: PRD Q-13.

## Rollback

- **Before S7:** revert the feature PR; migration 0078 only adds tables, so an older image runs against it.
- **After S7:** the surface can be taken off the internet without a release by excluding `/mcp` and
  `/oauth` in the three IngressRoutes (haynes-ops, git), the way `/api/mcp` is excluded; the metadata
  documents stay harmless. Disconnecting every app is each user's own action on Connected apps (OPS-016 §4).
