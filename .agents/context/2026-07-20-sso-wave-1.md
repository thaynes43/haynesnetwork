# 2026-07-20 (evening) — SSO wave 1: rulings closed, Immich + OWUI zero-click LIVE, LAN Tautulli doors staged

The owner picked TRUE SSO (PLAN-058) as the next big thing and closed every open DESIGN-041
question in-session. Full rulings + the new D-11 LAN-ingress rule: DESIGN-041 (merged #449);
the as-executed amendment (merged with this note). Memory `sso-authentik-only-posture` added:
NO local accounts / password break-glasses — Authentik-hard-dependent is the accepted posture.

## State after wave 1

| Leg | State |
|---|---|
| Docs / rulings | DESIGN-041 **Accepted**; Q-03..Q-08 closed (#449); as-executed amendment (this PR) |
| Kavita, ABS, Paperless | Already at target (pre-existing) |
| Immich | **ZERO-CLICK LIVE** — autoLaunch flipped via admin API (key in homepage-secret), OAuth + mobile URI verified first; SSO-only per posture ruling |
| Open WebUI | **ZERO-CLICK LIVE** — catalog deep-link via audited updateApp writer (revert value recorded in the design amendment) |
| Tautulli pilot (family) | Done previously (admins+Family) |
| Tautulli ×2 LAN | **Infra live (haynes-ops #2176)**, verified: outposts 1/1 + 302→public Authentik, twin middlewares, ESO synced off the 1P Login items (no quirks). **ARM = owner: mark haynes-ops #2177 ready + merge, then LAN click-test both hosts** (flips them open→admins-only; unverifiable from the pod) |
| Seerr | Waiting upstream stable OIDC (Q-01) |
| Kavita role sync | Deferred until post-soak (Q-06; owner tier table) |

## Key discovery
Both LAN Tautulli instances serve their UI OPEN today → the forward-auth gate alone delivers
admins-only zero-click; the HTTP-Basic injection wiring is staged but DORMANT (optional
hardening only). The 1P Login-item shape gave ESO zero trouble (owner asked — answer: none).

## Remaining (the SSO thread)
1. **Owner arm + click-test** (#2177 + the two hosts, fresh browser, zero-click as admin /
   denied as non-admin). Then wave 1 is COMPLETE.
2. Soak → then the Q-06 Kavita role-sync leg (owner tier table) and the role-governed
   login-policy sync design (the 2026-07-17 amendment's follow-up ADR).
3. Phase-2 expansion candidates (anything-Authentik under D-11): app-by-app, later.
4. Tautulli agent left two pushed worktrees (`~/work/tautulli-lan-sso`, `-attach`) — branches
   pushed, nothing untracked; prune at leisure.
