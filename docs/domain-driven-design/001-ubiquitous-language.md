# DDD-001: Ubiquitous Language

- **Status:** Accepted
- **Last updated:** 2026-07-15 (T-173 Email Channel + T-174 Notification Preference — ticket email notifications; ADR-060 / DESIGN-031, PLAN-035. 2026-07-14 (DESIGN-004 D-22 nav restructure — four-tab bar + Metrics/Integrations to the user menu + `HELPDESK_NAME` ratified "Tickets"; amends T-81/T-106/T-145. Prior: T-170..T-172 Activity / In-Flight / Import Failure — the pipeline made visible; ADR-059 / DESIGN-030, PLAN-048. Prior: T-167..T-169 Integrations Hub / Shelf Chips / Library-Wanted (books) — ADR-057 / DESIGN-029, PLAN-045; T-166 Comic Acquisition (Kapowarr) — ADR-056, PLAN-046)
- **Related:** PRD-001

<!-- The normative project glossary. Code and docs use these terms exactly (see
     CLAUDE.md / docs/PROCESS.md). Append-only in spirit: if a term changes
     meaning, add a new T-NN and mark the old row Superseded — never rewrite or
     renumber. Cite terms across docs as `DDD-001 T-NN`. -->

## How to use this glossary

- New domain terms must be added here **in the same change** that introduces them
  (CLAUDE.md). PRD-001 wins on any conflict — fix this doc, not the PRD.
- Code identifiers follow the todos-for-dues naming style: snake_case plural tables
  (`users`, `user_role_transitions`), snake_case columns, PascalCase TypeScript types,
  camelCase helpers, SCREAMING_SNAKE env vars. Identifiers for not-yet-built tables are
  prescriptive: designs use them or amend this row in the same change.
- `R-NN` / `AC-NN` / `Q-NN` references are PRD-001 IDs; `BC-NN` references are DDD-002.

## Identity & access

| ID   | Term                           | Definition                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Code identifier                                                                       | Notes                                                                                                                                          |
| ---- | ------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| T-01 | User                           | A person known to the app. A row is auto-created in the Default Role (T-48) on first successful Authentik login — no approval gate (R-03).                                                                                                                                                                                                                                                                                                                                            | `users`                                                                               | Better Auth schema + `role_id` FK (was a `role` enum column, superseded by ADR-012); displayName/email from OIDC claims (AC-02).               |
| T-02 | Role                           | **Amended by ADR-012:** an admin-managed, DB-backed named group with an editable app set (T-46). A User has **exactly one** (`users.role_id`). No longer a `Member`/`Admin` text enum — that is gone. Two seeded System Roles (T-47): **Admin** (T-04, superuser) and **Default** (T-48, new-user role); admins create more. Role assignment writes Audit Rows (`user_role_transitions`) in the same transaction (R-04).                                                              | `roles`, `users.role_id`                                                              | The old `enums.ts` `ROLES` array and `users.role` CHECK are removed (migration 0007). Session carries `role = { id, name, isAdmin }`.          |
| T-03 | ~~Member~~                     | **Superseded by ADR-012:** there is no `Member` role. The role every new login lands in is now the **Default** system role (T-48); "basic user" = a user in the Default role.                                                                                                                                                                                                                                                                                                         | ~~`'Member'`~~                                                                        | Retained for history; use Default (T-48) / Role (T-02).                                                                                        |
| T-04 | Admin                          | **Amended by ADR-012:** the **superuser Role** (`roles.is_admin`) — implicitly sees every App (no `role_app_grants` rows; new Apps included automatically), grants admin-console access, and is immutable (no rename/edit/delete). First claimed via Bootstrap Admin (T-06); the last Admin-role member can't be moved off it (LastAdminError).                                                                                                                                       | `roles.is_admin` (session `role.isAdmin`)                                             | PRD-001 Actors & roles; R-02. `adminProcedure` gates on `role.isAdmin`.                                                                        |
| T-05 | Family                         | **Amended by ADR-012:** no longer a flag/designation — it is an example Role (T-02), now **seeded by migration 0007** as a normal (editable/deletable) role granting every App except Tautulli. Phase-3 Family-Only-Library gating (R-26/R-27) will attach to a **role attribute**, not the removed `users.is_family`.                                                                                                                                                                | seeded row in `roles` (name `Family`; `is_admin`/`is_default`/`grants_all` all false) | `users.is_family` / `tags.is_family` are dropped (migration 0007); see ADR-012 C-09/C-12 and T-21.                                             |
| T-06 | Bootstrap Admin                | Idempotent promotion to Admin of a first login whose email (case-insensitive) is on the allowlist; repeat logins are no-ops (R-02, AC-03).                                                                                                                                                                                                                                                                                                                                            | `BOOTSTRAP_ADMIN_EMAILS` (env)                                                        | Allowlist seeded with both owner emails (Q-01).                                                                                                |
| T-07 | Session                        | The authenticated browser session established after the Authentik OIDC round-trip; 7-day cookie (AC-01).                                                                                                                                                                                                                                                                                                                                                                              | `sessions` (Better Auth)                                                              | No password form exists anywhere (R-01).                                                                                                       |
| T-08 | Authentik                      | The OIDC identity provider at `authentik.haynesnetwork.com` — the only sign-in path. Plex is offered as the credential inside Authentik (R-01).                                                                                                                                                                                                                                                                                                                                       | `AUTHENTIK_*` (env)                                                                   | Syncing permissions into Authentik is a follow-on (R-30).                                                                                      |
| T-98 | Sign Out (RP-initiated logout) | Ending BOTH the app Session (T-07) AND the upstream Authentik (T-08) SSO session, so the next Log In shows the Authentik login page instead of silently re-authenticating. The app clears its own session, then redirects the browser to the issuer's OIDC `end_session_endpoint` with `post_logout_redirect_uri={BETTER_AUTH_URL}/login` (+ `id_token_hint` when stored). Degrades to a plain local logout when the issuer advertises no `end_session_endpoint` (the e2e stub OIDC). | `GET /api/auth/logout`; `resolveSignOutRedirect`/`buildEndSessionUrl` (`@hnet/auth`)  | DESIGN-002 D-15; Authentik provider needs `logout`-type redirect URIs (OPS-001).                                                               |
| T-46 | Role App Grant                 | (ADR-012) A `(role, app)` pair — the editable app set a Role grants (T-13). The Admin superuser role holds **no** rows here (implicit all-apps). Composite PK dedupes; both FKs cascade.                                                                                                                                                                                                                                                                                              | `role_app_grants`                                                                     | Replaces `user_app_grants` + `tag_app_grants` + `app_catalog.default_visible`.                                                                 |
| T-47 | System Role                    | (ADR-012) One of the two seeded, fixed-id, non-deletable Roles: **Admin** (T-04, `is_admin`) and **Default** (T-48, `is_default`). Partial unique indexes enforce at most one of each; a CHECK forbids a role being both. Migration 0007 seeds a **third** role, `Family` (T-05), but it is a _normal_ role, not a System Role — editable/deletable.                                                                                                                                  | `roles.is_admin` / `roles.is_default` (`SEEDED_ROLE_IDS`)                             | Fixed ids so `users.role_id` can DB-default to Default. Domain writers reject renaming/deleting them (SystemRoleImmutableError).               |
| T-48 | Default (Role)                 | (ADR-012) The System Role assigned to every new User (`users.role_id` DB default). Its seeded app set is **Seerr, Plex, K8Plex, and PlexOps** (T-10, migration 0007); the app set and description are editable but the role can't be renamed or deleted.                                                                                                                                                                                                                              | `roles.is_default`                                                                    | The successor to the old "Member" default (T-03).                                                                                              |
| T-49 | All-Apps Role                  | (ADR-012, shipped after) A non-Admin Role with `roles.grants_all` = true: grants **every** catalog App — including ones added later — and holds **no** Role App Grants (T-46), i.e. Admin's (T-04) all-apps reach **without** admin-console access. Effective Permissions (T-16) = all apps when `is_admin OR grants_all`. Surfaced as the **"All apps"** checkbox on `/admin/roles` (greys out + disables the per-app list).                                                         | `roles.grants_all`                                                                    | `createRole`/`updateRole` take `grantsAll` (true clears the explicit app set); `RoleInput`/`RolePatchInput` carry it; `roles.list` returns it. |

## Catalog & entitlements

| ID   | Term                  | Definition                                                                                                                                                                                                                                                                                                        | Code identifier                                                               | Notes                                                                                                                                                                                                          |
| ---- | --------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-09 | App (Catalog Entry)   | A DB-backed catalog row for a self-hosted app: name, description, icon, URL, default-visible flag, display order (R-11). **Amended by ADR-013:** the URL is any well-formed, normalized `http(s)` URL — the host allow-list is gone.                                                                              | `app_catalog`                                                                 | Admin-editable without redeploy; seeds per R-12/R-13. The domain's `normalizeCatalogUrl` canonicalizes on write; DB backstop is the scheme-only CHECK `app_catalog_url_scheme` (`^https?://`, migration 0008). |
| T-10 | Default-Visible       | **Amended by ADR-012:** no longer a per-App flag — "default visible" is now the **Default Role's app set** (T-46/T-48), seeded to Seerr, Plex, K8Plex, and PlexOps (R-12, migration 0007). Admins change what basic users see by editing the Default role's apps.                                                 | Default role's `role_app_grants` (was `app_catalog.default_visible`, dropped) | A fresh user (Default role) Dashboard shows exactly these (AC-04).                                                                                                                                             |
| T-11 | Tile                  | The Dashboard rendering of one App the viewer is permitted to open; links out to its configured URL (R-10).                                                                                                                                                                                                       | `Tile` (UI component)                                                         |                                                                                                                                                                                                                |
| T-12 | Dashboard             | The permissioned home page: the viewer's Tiles, derived from Effective Permissions.                                                                                                                                                                                                                               | `Dashboard` (route `/`)                                                       | Enforces (renders/hides); never decides — see DDD-002 §4.                                                                                                                                                      |
| T-13 | App Grant             | **Amended by ADR-012:** permission to see/open one App is granted to a **Role**, not a User — a `(role, app)` pair (T-46). A User gets an App by being assigned a role that grants it. No per-user or tag-derived grants remain.                                                                                  | `role_app_grants` (was `user_app_grants`/`tag_app_grants`, dropped)           | Role edits are audit-logged (`update_role`); role assignment via `user_role_transitions` (AC-05).                                                                                                              |
| T-14 | ~~Tag~~               | **Superseded by ADR-012:** tags are removed. Their permission-bundle role is now filled by the **Role** itself (T-02) — a Role _is_ the bundle.                                                                                                                                                                   | ~~`tags`, `user_tags`~~ (dropped, migration 0007)                             | Read "tag" as "role"; bespoke access = a dedicated role.                                                                                                                                                       |
| T-15 | Permission Bundle     | **Amended by ADR-012:** the bundle is now the **Role** (T-02): its App set (T-46) plus, since ADR-017, its allowed Plex libraries / family gating as role grants (R-20, R-25..R-27).                                                                                                                              | `role_app_grants` + `role_library_grants`                                     | Assigning the role applies the bundle; one role per user.                                                                                                                                                      |
| T-16 | Effective Permissions | **Amended by ADR-012:** the Apps a User can open = **their Role's app set** (`role_app_grants`), or **ALL** catalog Apps if the Role is the Admin superuser **or an All-Apps Role** (T-49, `grants_all`). Single provenance (the role) — no union, no per-source attribution. Never stored; recomputed per query. | `EffectiveApp` (interface) + `effectiveAppsForUser()`                         | The `effective_app_grants` VIEW is dropped (migration 0007); `effectiveAppsForUser` returns all apps when `is_admin OR grants_all`. Changes take effect on the next dashboard query — no re-login (AC-05).     |

## Plex servers & libraries

| ID   | Term                     | Definition                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Code identifier                                                                                                              | Notes                                                                                                                                                                                                                                                                                          |
| ---- | ------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-17 | Plex Server              | One of the three Plex instances under management, keyed by owner slug: `haynestower` (legacy Unraid), `haynesops` and `hayneskube` (k8s) (R-25; OPS-002). Note the subdomain↔slug mismatch (plexops↔haynesops, k8plex↔hayneskube).                                                                                                                                                                                                                                                                                                                                                                      | `plex_servers` (built, ADR-017; seeded by migration 0010)                                                                    | Owner tokens from 1Password via `token_ref` (R-28); `machine_identifier` (T-54) keys the sharing API.                                                                                                                                                                                          |
| T-18 | Plex Library             | A library on one specific Plex Server; **identity is `(server_id, section_key)`, never name** (Q-03 resolved: servers mirror Movies/TV under different names).                                                                                                                                                                                                                                                                                                                                                                                                                                          | `plex_libraries` (built; UNIQUE(`server_id`,`section_key`))                                                                  | Soft-state `available` flag — a vanished library is withheld, not deleted (DESIGN-007 D-04).                                                                                                                                                                                                   |
| T-19 | Library Grant            | **Redefined by ADR-017:** a **Role → Library** grant — permission for members of a Role to self-add a given Plex Library (R-25..R-27). No longer per-user/tag (`library_grants`/`tag_libraries` retired before build). Mirrors T-46 Role App Grant.                                                                                                                                                                                                                                                                                                                                                     | `role_library_grants` (built)                                                                                                | Share application happens in Plex Sharing (BC-04); grant edits audited (`update_role_libraries`).                                                                                                                                                                                              |
| T-20 | Allowed Library Set      | A User's effective set of self-addable libraries. **Amended by ADR-024:** for a non-admin user it is the UNION of their Role's explicit `role_library_grants` and every available library on any server their Role holds a Server All-Grant (T-67) on; Admin ⇒ all available libraries. `grants_all` does NOT imply all libraries (ADR-017 C-03).                                                                                                                                                                                                                                                       | `EffectiveLibrary` + `effectiveAllowedLibrariesForUser()`; `allGrantedServerIdsForUser()` (the all-toggle gate)              | Re-derived inside each share mutation (TOCTOU gate).                                                                                                                                                                                                                                           |
| T-21 | Family-Only Library      | **Redefined by ADR-017:** an ordinary Plex Library granted only to the `Family` Role — there is **no** `plex_libraries.is_family_only` column. Exactly `HNet Home Videos` and `HNet Photos` (HAYNESTOWER) today (R-26; C-09/C-12).                                                                                                                                                                                                                                                                                                                                                                      | rows in `plex_libraries` referenced only by `role_library_grants` for the Family role                                        | Family self-service is just its role's grant set.                                                                                                                                                                                                                                              |
| T-52 | Plex Share               | An applied library share on a specific Plex Server — the user's `SharedServer` on the server owner's plex.tv account. Usually a **per-section** grant (a set of shared sections) mutated via read-merge-write (never blind overwrite). **Amended by ADR-024:** a `SharedServer` can also be **all-libraries** (`allLibraries="1"` — share-everything, incl. future libraries); a user whose Role holds a Server All-Grant (T-67) self-toggles between that state and an explicit list. A per-section add/remove while all-libraries is refused (`PLEX_ALL_STATE`) — no silent demotion.                 | `plex_share_audit` (events `share_added`/`share_removed`/`share_all_enabled`/`share_all_disabled`); plex.tv `shared_servers` | Precondition: the user is already a Plex friend of the owner (email match; ADR-017 C-06) — **except the owner themselves**, who owns every library and is never a friend (Server Owner, T-94 / ADR-029). Not every share is per-section — see T-67.                                            |
| T-53 | Library Registry Refresh | The admin-triggered sync that reads each server's `GET /library/sections` (+ `/identity`) and upserts `plex_servers`/`plex_libraries` keyed on `(server_id, section_key)`, marking unseen libraries `available=false` (never deleting). **Degrades per server** (DESIGN-007 D-12): each server refreshes in its own transaction and a Plex failure on one is folded into a per-server summary (`{ ok, servers: [{ slug, name, ok, libraryCount?, error? }] }`) — it never aborts the others.                                                                                                            | `refreshPlexRegistry` / `plex.refreshRegistry`                                                                               | Admin-button only for now — a CronJob is future work (Q-06). NOT all-or-nothing: one unreachable server reports `error: 'unreachable'` while the rest still commit.                                                                                                                            |
| T-54 | Machine Identifier       | A Plex Server's stable GUID; the key the plex.tv sharing API addresses a server by (`/api/servers/{machineIdentifier}/…`). Distinct from the owner slug and the ingress subdomain.                                                                                                                                                                                                                                                                                                                                                                                                                      | `plex_servers.machine_identifier`                                                                                            | Verified live 2026-07-06; pinned in `PLEX_MACHINE_IDENTIFIERS`, re-read by the registry refresh.                                                                                                                                                                                               |
| T-67 | Server All-Grant         | (ADR-024) A `(role, server)` grant meaning the Role grants ALL libraries — including future ones — on that Plex Server. Sits alongside per-library Library Grants (T-19); the Allowed Library Set (T-20) unions both. A member of such a role self-toggles their own account between the plex.tv **all-libraries** state and an **explicit list** (leaving All seeds the current full set — no access loss; `setServerAllShare`). An `is_admin` role holds no rows and implies all-on-every-server.                                                                                                     | `role_plex_server_all_grants` (migration 0015); `allGrantedServerIdsForUser`; `setServerAllShare`                            | Written by `setRoleLibraries` (replace-whole-set, audited `update_role_libraries`). Server-scoped share events `share_all_enabled`/`share_all_disabled` (T-52).                                                                                                                                |
| T-94 | Server Owner (Plex)      | (ADR-029) The plex.tv account a server's owner token authenticates as — the account that OWNS the Plex Server (all three share one owner, `manofoz@gmail.com`). The owner is **never in their own friend list** (`/api/users` lists friends, not the account itself), so friend-matching (T-52 precondition) can't match them. `plex.myLibraries` recognizes the owner by comparing the caller's email to the owner email (from plex.tv `GET /api/v2/user`) and returns an **owner state** — every library implicitly theirs, no add/remove/friend/all-toggle controls, never the "not a friend" error. | `getOwnerAccount`/`getOwnerEmail` (`@hnet/plex/read`); `MyServer.owner` (`plex.myLibraries`)                                 | Resolved from the token account, not a hard-coded email. Owner lookup failure degrades to the friend flow (server stays available). The owner signs in **via Plex** (bootstraps Admin); the local `admin@haynesnetwork.com` account has no Plex identity and matches neither owner nor friend. |

## Media ledger & fix

| ID   | Term            | Definition                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Code identifier                                                           | Notes                                                                                                                                                                                                                                                                                                                                                                            |
| ---- | --------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-22 | *arr            | Collective for Sonarr (TV), Radarr (movies), Lidarr (music) — the Source of Truth for media lists (R-40).                                                                                                                                                                                                                                                                                                                                                                    | `arr_kind` (`'sonarr' \| 'radarr' \| 'lidarr'`)                           |                                                                                                                                                                                                                                                                                                                                                                                  |
| T-23 | Monitored       | *arr state meaning the *arr actively manages/searches for an item. The Ledger mirrors all monitored media (R-40).                                                                                                                                                                                                                                                                                                                                                            | `ledger_items.monitored`                                                  | *arr concept we reference.                                                                                                                                                                                                                                                                                                                                                       |
| T-24 | Media Item      | One Ledger row mirroring a monitored *arr item: on-disk state, quality profile, root folder, *arr tags (R-40). One row per Sonarr series / Radarr movie / Lidarr artist (DESIGN-005 D-04).                                                                                                                                                                                                                                                                                   | `media_items`                                                             | Written only by Sync — never hand-edited. Identifier amended from `ledger_items` to the DESIGN-001 D-15 reserved name (DESIGN-005).                                                                                                                                                                                                                                              |
| T-25 | Ledger          | The app's durable, queryable mirror of the media estate: Media Items + Ledger Events + Wanted Items. Doubles as the disaster-recovery source for Restore (R-40..R-42, R-50, R-51).                                                                                                                                                                                                                                                                                           | `media_items`, `ledger_events`, `wanted_items` (view)                     | A synced copy plus attribution/audit — never the Source of Truth for media.                                                                                                                                                                                                                                                                                                      |
| T-26 | Ledger Event    | A history record on the Ledger: who requested what (attributed via Seerr), what was deleted when, grabs/imports from *arr history (R-41).                                                                                                                                                                                                                                                                                                                                    | `ledger_events`, `ledger_events.event_type`                               | Maintainerr enrichment is a follow-on (Q-04).                                                                                                                                                                                                                                                                                                                                    |
| T-27 | Wanted Item     | A Monitored Media Item with nothing on disk; captured and browsable (R-42).                                                                                                                                                                                                                                                                                                                                                                                                  | `wanted_items` (view over `media_items`, DESIGN-005 D-08)                 | Derived, never stored.                                                                                                                                                                                                                                                                                                                                                           |
| T-28 | Seerr           | The request app users request content through. We link to it and read request attribution from its API; we do not replace it (Non-goals, R-41).                                                                                                                                                                                                                                                                                                                              | `seerr` API client                                                        | Tile URL flips at the owner's cutover (Q-02).                                                                                                                                                                                                                                                                                                                                    |
| T-29 | Fix Request     | A user's tracked repair action on a Media Item they believe is broken: requester, item, Fix Reason, *arr actions taken, outcome (R-43, R-46).                                                                                                                                                                                                                                                                                                                                | `fix_requests`                                                            | Users see their own history/status; admins see all. Rate-guarded (R-47, Q-05).                                                                                                                                                                                                                                                                                                   |
| T-30 | Fix Reason      | Required taxonomy value on every Fix Request: `wont_play_corrupt`, `wrong_language`, `wrong_version_quality`, `missing_subtitles`, `wrong_content`, or `other` + free text (R-45).                                                                                                                                                                                                                                                                                           | `fix_requests.reason`, `fix_requests.reason_text`                         | Stored for analysis. Identifiers aligned to DESIGN-001 snake_case enum convention (DESIGN-005 D-09); free text rides only on `other`. **Per-kind offer rule (ADR-016):** `missing_subtitles` is offered for Sonarr/Radarr only (Music/Lidarr excluded — Bazarr covers movies/TV, not music) via `fixReasonsForKind`; the enum itself is unchanged (all six values remain valid). |
| T-31 | Release         | *arr concept: a specific downloadable version/file of a media item offered by an indexer. Referenced, not owned.                                                                                                                                                                                                                                                                                                                                                             | —                                                                         |                                                                                                                                                                                                                                                                                                                                                                                  |
| T-32 | Grab            | *arr concept: the *arr taking a Release for download; appears in *arr history. Fix chooses its path by grab history (R-44).                                                                                                                                                                                                                                                                                                                                                  | —                                                                         |                                                                                                                                                                                                                                                                                                                                                                                  |
| T-33 | Blocklist       | *arr concept: marking a Release failed so the *arr won't re-grab it. Fix = mark-failed (blocklist) + trigger a new search (R-44, AC-07).                                                                                                                                                                                                                                                                                                                                     | —                                                                         | Via the *arr's history-failed endpoint.                                                                                                                                                                                                                                                                                                                                          |
| T-34 | Fix Fallback    | The Fix path when an item has no Grab history: delete the file(s) + trigger search (R-44, AC-08).                                                                                                                                                                                                                                                                                                                                                                            | `fix_requests.path_taken = 'delete_search'`, `fix_requests.actions_taken` | Outcome recorded either way.                                                                                                                                                                                                                                                                                                                                                     |
| T-35 | Restore         | The admin-only failsafe: diff the Ledger against a live *arr, preview, then re-add missing items Monitored with recorded quality profile, root folder, and tags (R-50..R-52). **Amended (ADR-022):** Restore is now the **diff-driven, searches-off, skip-if-present special case** of the generalized `executeArrAdd` (`reason:'restore'`); the Ledger's bulk Add-&-search (T-64) is the same engine with `reason:'ledger_add'`, searches on, and the monitor-flip outcome. | `restore_runs` (shared with Ledger-add via the `reason` column)           | The failsafe bulk write-back; explicitly admin-initiated and audit-logged.                                                                                                                                                                                                                                                                                                       |
| T-36 | Restore Preview | The pre-execution diff listing exactly the Ledger items absent from the target *arr (R-52, AC-09).                                                                                                                                                                                                                                                                                                                                                                           | `RestorePreview`                                                          | Execution reports successes/failures per item.                                                                                                                                                                                                                                                                                                                                   |
| T-37 | Sync            | The one-way import *arr → app that keeps the Ledger current (R-40). Sync direction is strictly *arr → app; the only write-backs are Fix and Restore (R-52).                                                                                                                                                                                                                                                                                                                  | `sync_runs`                                                               |                                                                                                                                                                                                                                                                                                                                                                                  |

## Cross-cutting

| ID    | Term                         | Definition                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Code identifier                                                                                                                                                                                                        | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ----- | ---------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-38  | Source of Truth              | The ownership split: the *arrs own media lists (the Ledger mirrors them); this app owns permissions and the App catalog.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | —                                                                                                                                                                                                                      | CLAUDE.md hard rule 4.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| T-39  | Audit Row                    | A record (who, what, when, Initiator Kind) written in the same transaction as any role/permission mutation (R-04), library change (R-28), or Restore (R-52).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | `permission_audit` (generic permission mutations, fixed `action` enum); `user_role_transitions` (role changes only); analogs per mutation type                                                                         | `permission_audit` (schema/permission-audit.ts) is append-only, written only by the packages/domain single-writer helpers; role assignments live in `user_role_transitions`, not here. _(Amended by ADR-012: `permission_audit.action` is now `create_role`/`update_role`/`delete_role` + `create_app`/`update_app`/`delete_app` (tag/grant/family actions dropped), and its `tag_id` FK is replaced by `role_id`; `user_role_transitions` keys `from_role_id`/`to_role_id`.)_ Pattern borrowed from todos-for-dues. |
| T-40  | Initiator Kind               | Audit field distinguishing what caused a change: system (e.g. bootstrap promotion) vs admin vs user.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | `initiator_kind`                                                                                                                                                                                                       | AC-03: bootstrap writes a system-initiator row.                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| T-41  | Tombstone                    | A Media Item marked as no longer present in its *arr (`deleted_from_arr_at` set) but retained in the Ledger — deletion history (R-41) and the Restore source (R-50) both require keeping the row. Cleared if the item reappears.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | `media_items.deleted_from_arr_at`                                                                                                                                                                                      | Sync tombstones, never deletes; mass-tombstone guard in DESIGN-005 D-14.                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| T-42  | Sync Cursor                  | The per-source high-water mark (history timestamp) that incremental Sync resumes from; advanced in the same transaction as each ingested batch.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | `sync_state.history_cursor`                                                                                                                                                                                            | One row per source (`sonarr`, `radarr`, `lidarr`, `seerr`).                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| T-43  | Fix Lifecycle                | The tracked status progression of a Fix Request: `pending → actioned → search_triggered → completed`, with `failed` reachable from any active state. Completion is asynchronous — observed by Sync (ADR-007 C-06). The stored `fix_requests.status` is UNCHANGED by ADR-028; the live **Action Progress Phase (T-90)** is a richer _derived_ projection on top of it (queued/downloading/importing/nothing_found/stalled).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `fix_requests.status`                                                                                                                                                                                                  | Transitions only via `packages/domain` writers (DESIGN-005 D-09/D-12); the derived phase never writes.                                                                                                                                                                                                                                                                                                                                                                                                               |
| T-44  | Force Search                 | The search-only action on content that is Monitored but has nothing on disk — missing, not broken. It triggers ONLY the owning *arr's search command (episode / movie / album / series), with NO Blocklist, NO file delete, and no Fix Reason. It records an audited `search_requested` Ledger Event and draws on the same per-user hourly budget as Fix (R-42/R-47). Its live progress (ADR-028) derives from the LATEST `search_requested` event + the *arr Queue (no `fix_requests` row); it stays lock-free (no open-search guard) — the button's in-flight state is driven by the derived phase.                                                                                                                                                                                                                                                                                                                                       | `ledger_events.event_type = 'search_requested'` (no `fix_requests` row)                                                                                                                                                | The UI shows Force Search wherever an item/episode is not on disk (Fix shows when it is). Search-only writer + orchestrator in `packages/domain` (DESIGN-005 D-17).                                                                                                                                                                                                                                                                                                                                                  |
| T-45  | Action Scope                 | The level a Fix or Force Search targets: a single **episode**/**album**, a whole sonarr **season**, the whole **show**/**artist**, or the radarr movie (**item**). Force Search supports every scope; Fix supports `item`/`episode`/`season`/`album` only — whole-show/artist repair is Force-Search-only (blocklisting an entire series/discography is too broad). Sonarr episodes group into collapsible season sections in the UI.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | `fix_requests.target_scope` (`item\|season\|episode\|album`) + `target_season`; `ledger_events.payload.scope` for Force Search                                                                                         | Validated by the shared `resolveFixTarget`/`resolveSearchTarget` (packages/domain `action-scope.ts`); commands: `SeasonSearch`/`ArtistSearch` added (DESIGN-005 D-15/D-17, migration 0006).                                                                                                                                                                                                                                                                                                                          |
| T-50  | Bazarr                       | The subtitle manager for the **Radarr/Sonarr estate** (`bazarr.media.svc.cluster.local:6767`); the Fix target for reason `missing_subtitles` (ADR-016). Covers movies + episodes, **not** music. Base path `/api`, auth header `X-API-KEY`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | `@hnet/arr` `BazarrClient`/`BazarrWriteClient`; `BAZARR_URL`/`BAZARR_API_KEY`                                                                                                                                          | Async `search-missing` action (HTTP 204, queued); not an *arr — `assertBazarrEnv`, not `ARR_SERVICES`.                                                                                                                                                                                                                                                                                                                                                                                                               |
| T-51  | Subtitle Fix                 | The Fix Path (`fix_requests.path_taken = 'bazarr_subtitle'`) taken when reason = `missing_subtitles`: no Blocklist (T-33), no Fix Fallback delete (T-34), no *arr re-grab — a Bazarr subtitle search only. Fire-and-forget: rests at `search_triggered` (never auto-completes; `completeFixRequests` excludes it).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `fix_requests.path_taken = 'bazarr_subtitle'` (migration 0009)                                                                                                                                                         | `runSubtitleFix` (packages/domain `fix-flow.ts`); movie → `PATCH /api/movies`, sonarr episode/season → series-level `PATCH /api/series` (ADR-016 / DESIGN-005 D-19).                                                                                                                                                                                                                                                                                                                                                 |
| T-55  | Media Metadata               | The harvested descriptive/quality data per Media Item: IMDb/TMDb rating + votes, RT tomatometer/audience, runtime, resolution, genres, *arr added date, watch-stats, parsed requester/collection tags, and a poster reference. Held 1:1 with `media_items` in a separate sibling table (ADR-018), multi-source, refreshed on a 6h cadence with per-source degradation, tombstone-survivable.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | `media_metadata` (migration 0012); single writer `upsertMediaMetadataBatch` (packages/domain)                                                                                                                          | Enums `METADATA_SOURCES`/`RESOLUTIONS`/`POSTER_SOURCES`; in the `no-direct-state-writes` guard. No `media_type` column — the noun is `media_items.arr_kind` (reuse `ARR_KINDS`).                                                                                                                                                                                                                                                                                                                                     |
| T-56  | Poster Proxy                 | The authed server-side route that STREAMS a Media Item's small poster (never stores it — ADR-019): from the owning *arr's pre-resized MediaCover variant (API key in a header) or the TMDB CDN. A miss → 404 → the UI KindIcon fallback.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | `apps/web/app/api/posters/[mediaItemId]/route.ts`; `resolvePosterUpstream` (`@hnet/api`); `media_metadata.poster_source`/`poster_ref`                                                                                  | Cache `private, max-age=86400, stale-while-revalidate=604800` + ETag from `poster_ref`. Session-gated; not a public endpoint.                                                                                                                                                                                                                                                                                                                                                                                        |
| T-57  | Watch Stats                  | Tautulli-sourced `play_count` (SUM) + `last_viewed_at` (MAX) on a Media Item, unified across the THREE estate Tautulli instances (HaynesOps/HaynesKube/HaynesTower), joined by TMDB/IMDb/TVDB GUID (rating keys differ per server). Per-instance breakdown kept in `extra.tautulli`. The substrate PLAN-006 uses to protect actively-watched media. **DESIGN-010 D-12** adds the display pair `last_watched_at`/`last_watched_server` (migration 0028) — the SAME cross-server MAX instant as `last_viewed_at` plus the estate slug that owns it — computed in the same `mergeWatchContributions` pass (no extra Tautulli reads). See T-104 Watch Visibility.                                                                                                                                                                                                                                                                                                                          | `media_metadata.play_count`/`last_viewed_at`/`last_watched_at`/`last_watched_server`/`extra.tautulli`; `TautulliClient` (`@hnet/arr`)                                                                                                                          | Env `TAUTULLI_API_KEY` / `TAUTULLI_K8PLEX_API_KEY` / `TAUTULLI_HAYNESTOWER_API_KEY` (+ `_URL`), all optional/skip-if-absent.                                                                                                                                                                                                                                                                                                                                                                                         |
| T-58  | Filter Engine                | The `@hnet/ui` shared filter/chip/sort primitives (ported from demo-console, mechanism-shared / look-per-app) reused by Library, Ledger, Trash — the editable chip model, the FilterChip + inline editor, the click-to-filter cells, the pure FilterMap helpers, and the generic tri-state sort. Its wire counterpart is the `ledger.search` sort/filter + generalized NULLS-LAST keyset cursor contract (DESIGN-008 D-09). **Amended (ADR-022):** the Ledger Section (T-64) reuses this same wire contract via `ledgerAdmin.browse` — the shared `buildLibraryWhere`/`SORT_SPECS`/keyset assembly (`packages/api/src/ledger-query.ts`) with tombstoned rows always in + the Ledger-only `monitored`/`hasFile` dims — so the DSL never forks.                                                                                                                                                                                               | `packages/ui/src/filters/*` (D-10, follow-up UX change); `ledger.search`/`ledger.filterFacets`/`ledgerAdmin.browse`, `packages/api/src/{keyset,ledger-query}.ts` (built)                                               | i18n-free (host injects labels), theme-free (`classPrefix`, default `hnet`); the cursor + facets + browse contract is shipped; the UI port lands in the follow-up.                                                                                                                                                                                                                                                                                                                                                   |
| T-59  | Keyless Signing              | Signing a container image with **no long-lived private key**: cosign exchanges the CI runner's short-lived OIDC token for an ephemeral Fulcio (T-60) certificate, signs the image digest, and records the signature + cert in Rekor (T-61). The chosen signing model for `haynesnetwork` images (ADR-020).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `cosign sign --yes …@<digest>` in `release-please.yml`; `permissions: id-token: write`                                                                                                                                 | No `COSIGN_*` secret at rest (CLAUDE.md rule 7). Same model the `haynes-ops` Kyverno policy already verifies for upgrade-agent/shepherd.                                                                                                                                                                                                                                                                                                                                                                             |
| T-60  | Fulcio                       | Sigstore's public certificate authority: it issues a **short-lived** X.509 signing certificate bound to an OIDC identity (here, the GitHub Actions workflow ref). The cert exists only long enough to sign — nothing to store or rotate.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | attestor `issuer: https://token.actions.githubusercontent.com`                                                                                                                                                         | Trust roots in the transparency log (T-61), not in a stored key.                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| T-61  | Rekor (transparency log)     | Sigstore's public, append-only transparency log where the cosign signature + Fulcio cert are recorded, so a signature can be verified after the ephemeral cert expires.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | `rekor.url: https://rekor.sigstore.dev` (Kyverno attestor); default in `cosign verify`                                                                                                                                 | Public log — no private infra to run; an availability dependency at admission (ADR-020 C-05).                                                                                                                                                                                                                                                                                                                                                                                                                        |
| T-62  | Attestor / Subject           | The Kyverno `verifyImages` config identifying **who** must have signed an image: the keyless `subject` (the signer's OIDC identity — the workflow ref, e.g. `https://github.com/thaynes43/haynesnetwork/.github/workflows/*`) + `issuer`. The signer identity is load-bearing config shared cross-repo with `release-please.yml` (ADR-020 C-04).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | `attestors[].entries[].keyless.{subject,issuer,rekor}` in `verify-thaynes43-images.yaml`                                                                                                                               | Per-source-repo: `haynesnetwork` needs its own attestor subject, distinct from `haynes-ops`.                                                                                                                                                                                                                                                                                                                                                                                                                         |
| T-63  | Admission Verification       | Kyverno verifying an image's signature **at pod admission** (`background: false`): the pod is admitted only if the image digest carries a signature matching the attestor (T-62). Runs in Audit (report only) or Enforce (deny). Fail-open (`failurePolicy: Ignore`) so a Sigstore outage never wedges deploys.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | `verifyImages` rule, `failureAction: Audit\|Enforce`, `verify-thaynes43-images.yaml`                                                                                                                                   | Audit→Enforce is a one-field, per-rule switch (OPS-006); the only failing-closed surface.                                                                                                                                                                                                                                                                                                                                                                                                                            |
| T-64  | Ledger Section               | The top-level Movie/TV/Music spreadsheet browse over the WHOLE ledger (live + tombstoned rows), the home of **bulk Add-&-search** (`executeArrAdd`, `reason:'ledger_add'`) + **export** (T-35 Restore is now the failsafe special case of the same engine — ADR-022). Access is gated by the Section Permission (T-65); reached from the **user menu** (ADR-032 — no top-row entry; default Disabled for members).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `ledgerAdmin.browse`/`bulkAddAndSearch`/`run`/`runs`; `/api/ledger/export`; `restore_runs.reason='ledger_add'`                                                                                                         | No posters here (TODO #5). The mutating *arr add/monitor/search stays confined to `packages/domain` (executeArrAdd).                                                                                                                                                                                                                                                                                                                                                                                                 |
| T-65  | Section Permission           | A role's access **level** for a top-level section — **Edit** (full), **Read-Only** (browse/export, no mutation), **Disabled** (section hidden). One row per (role, section); no row ⇒ the section default (**Ledger = Disabled — flipped from Read-Only by ADR-032**, Trash = Disabled, Bulletin = Read-Only); Admin implies Edit everywhere (ADR-021). Carried on the session; gated by `sectionProcedure`. Shared base for Ledger + Trash. **Amended (ADR-023):** for Trash it gates only VIEW; the individual write actions are the fine-grained Trash Action Grant (T-72) layered on top. **Amended (ADR-032):** the ledger default is `disabled` — the Ledger is user-menu tooling, admin-only out of the box; a role row opts members back in.                                                                                                                                                                                        | `role_section_permissions` (role_id, section_id, level); `setSectionPermission`; `SessionRole.sectionPermissions`; `sectionProcedure`                                                                                  | Server-authoritative (never client-hidden only); `update_section_permission` audit in-tx.                                                                                                                                                                                                                                                                                                                                                                                                                            |
| T-66  | Fileless Set                 | The _filterable state_ of ledger rows that are **on-disk none + unmonitored** — the backlog the household wants to (re)acquire. NOT an import: the rows already exist as live *arr rows (ADR-022 C-04 evidence), so the set is reached by the Ledger's `monitored:false` + `hasFile:'none'` filters (+ vote tiers) and acted on by T-64's bulk Add-&-search (monitor-flip + search). Reframes PLAN-005's draft "fileless backlog import" (a placeholder term that was never assigned a T-number) into a filter state.                                                                                                                                                                                                                                                                                                                                                                                                                       | `media_items.monitored=false AND on_disk_file_count=0`; `ledgerAdmin.browse` `monitored`/`hasFile` dims                                                                                                                | Batch by vote tier to stay under the 1000-item search cap.                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| T-68  | Trash Section                | The top-level Movie/TV section that wraps **Maintainerr** (the deletion system of record) behind a friendly UI: pending-deletion tables, Save/whitelist, Expedite, a Rules editor, Recently-Deleted → Restore, and an Activity feed. Read-through (no mirror tables) + a confined write surface — never a re-implementation (ADR-023 C-01/C-02). Music is never in scope (T-73 note / R-86).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | `trash.*` router; `packages/domain/src/trash-flow.ts`; `@hnet/arr` Maintainerr read + `@hnet/arr/write` MaintainerrWriteClient                                                                                         | Gated by Section Permission (T-65, VIEW) + Trash Action Grants (T-72, writes).                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| T-69  | Deletion Candidate           | An item sitting in a Maintainerr **collection** pending deletion — surfaced in the Trash Movies/TV tables merged with our ledger by tmdbId (movie→radarr)/tvdbId (tv→sonarr). Carries its per-item size + the **scheduled-delete date** (derived = `addDate + collection.deleteAfterDays`, since Maintainerr never returns it) + our facets (poster/requester/watch).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | `listTrashPending` → `TrashPendingItem`; `GET /api/collections/media/{id}/content/{page}` (`sizeBytes`)                                                                                                                | An item unknown to our ledger is still listed with Maintainerr's own fields.                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| T-70  | Exclusion / Whitelist / Save | Adding an item to Maintainerr's exclusion list so it is **never deleted**. Our "Save 🛡️" ⇄ a Maintainerr exclusion (bidirectional). Idempotent; the protective `dnd` tag is Maintainerr-managed (settings deploy step, "remove tag on un-exclude" ON) and read off `media_items.arrTags` as `protectedByTag`. The cross-server **watch guardian** auto-Saves any item watched on ANY of the three Plex servers within `RECENTLY_WATCHED_WINDOW_DAYS` (30) or carrying a Seerr requester tag.                                                                                                                                                                                                                                                                                                                                                                                                                                                | `saveExclusion`/`removeExclusion`; `guardRecentlyWatched`; `POST /api/rules/exclusion`; `trash_excluded` event (source `maintainerr`)                                                                                  | Protective ordering: external call first, then the audit event (never a phantom protection — ADR-023 C-05).                                                                                                                                                                                                                                                                                                                                                                                                          |
| T-71  | Recently Deleted             | The list of items Maintainerr deleted, sourced from **our** ledger's tombstoned rows (`deleted_from_arr_at`, T-41) — the durable, restore-able set (Maintainerr has no deletion-history API). Restore reuses the failsafe `executeRestore` (searches off, skip-if-present) + a `trash_restored` marker.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | `listRecentlyDeleted`; `trash.restoreDeleted` → `executeRestore`; `trash_restored` event                                                                                                                               | Restore is a Trash Action Grant (T-72 `restore_deleted`).                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| T-72  | Trash Action Grant           | A role's **fine-grained per-action** Trash permission — one of `save_exclude, remove_exclude, expedite_item, expedite_all, edit_rules, restore_deleted` (ADR-023) plus `save_leaving_soon, manage_batches` (ADR-025 — the curation-pipeline grants, migration 0017; neither seeded, Q-04). A ROW is the grant (presence = granted; no boolean); layered on top of the coarse Section Permission (T-65, which gates VIEW). Section edit-level implies nothing extra; Admin implies all. `edit_rules` additionally requires section Edit; `save_leaving_soon` gates the windowed user rescue, `manage_batches` the batch lifecycle.                                                                                                                                                                                                                                                                                                           | `role_trash_action_grants` (role_id, action, PK); `setRoleTrashActions`; `SessionRole.trashActions`; `trashActionProcedure`; migration 0016/0017; `update_trash_actions` audit                                         | Server-authoritative; replaces the plan's tentative "Action Permission".                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| T-73  | Expedite                     | **Hastening** a deletion. BOTH scopes — one item and the whole set — delete PER ITEM via `POST /api/collections/media/handle`, looping over each survivor; the estate-wide `POST /api/collections/handle` is **never** called (ADR-023 C-07a — it is unscopeable and would delete beyond the media kind/set the user saw). DESTRUCTIVE: it re-runs the Safety Audit (T-74) and refuses on an unsafe verdict; BOTH scopes first honor any LIVE Maintainerr exclusion (a just-saved item, even before its `dnd` tag syncs — F1) and the watch guardian auto-protects watched/requested items; the `trash_expedited` intent event is committed BEFORE the per-item Maintainerr call (Fix D-09 discipline). Whole-set is pinned to the snapshot the user saw (`maintainerrMediaIds`) so items that become pending after the modal opens are never deleted (F2). Music is never a target (R-86).                                                 | `expediteDeletion`; `trash.expediteItem`/`expediteAll`; `trash_expedited` event; `MaintainerrUnsafeError`→PRECONDITION_FAILED                                                                                          | The only destructive Trash path; gated behind `expedite_item`/`expedite_all` + confirm.                                                                                                                                                                                                                                                                                                                                                                                                                              |
| T-74  | Maintainerr Safety Audit     | The read-only preflight verdict `{ safe, reachable, version, integrations, armedRules, activeCollections }` — SAFE = reachable AND every required integration (Plex/Radarr/Sonarr/Tautulli/Seerr) connected (derived, keyless, from `app/status` + `settings/test/plex` + `rules/constants`). Writes no state; re-run on every destructive path (T-73).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | `auditMaintainerr`; `trash.status`; `GET /api/app/status`, `GET /api/settings/test/plex`, `GET /api/rules/constants`                                                                                                   | Maintainerr failures map like `ArrUpstreamError` → BAD_GATEWAY (fail closed).                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| T-75  | Curation Batch               | The **deletion unit** of the curation pipeline: a frozen snapshot of the current pending set for ONE media kind (never mixed; music never batchable) that an admin curates via a poster wall, green-lights, and a windowed sweep deletes. State machine `draft → admin_review → leaving_soon → deleted \| cancelled`; at most one OPEN batch per kind. Item snapshots (title/ids/size/poster) are frozen at creation; a deletion snapshot (T-79) is captured at sweep time.                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | `trash_batches` / `trash_batch_items` (migration 0017); `createBatchFromPending`, `greenlightBatch`, `cancelBatch`, `sweepExpiredBatches`; `trash.batches.*`; `trash_batch_transition` event                           | Every transition writes its event same-tx; only `leaving_soon` expires (ADR-025 C-01).                                                                                                                                                                                                                                                                                                                                                                                                                               |
| T-76  | Leaving Soon                 | The Plex-visible collection a green-lit batch drives so **users** can browse and rescue items during the window. A rolling per-kind manual Maintainerr collection (`visibleOnHome`/`visibleOnRecommended` pushed to Plex; `deleteAfterDays: null` — Maintainerr never ages it, our sweep deletes). A Save (T-77) pulls the item out of it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `driveLeavingSoonCollection`; `POST /api/collections` (+ `/add`, `/remove`, `/removeCollection`, v3.17.0); `trash_batches.maintainerr_collection_id`                                                                   | Created external-first at green-light (ADR-023 C-05); released on cancel (ADR-025 C-05, Q-05/Q-09).                                                                                                                                                                                                                                                                                                                                                                                                                  |
| T-77  | Save Window                  | The configurable duration (`window_days`, default 21 via `trash_default_window_days`, Q-10) a `leaving_soon` batch stays open for the user rescue exercise. A Save during the window is PERMANENT protection — a Maintainerr GLOBAL exclusion (T-70) + a `trash_batch_saves` tuning row; the item leaves the batch (`saved`) and the Leaving-Soon collection (Q-03). Role-gated by the `save_leaving_soon` action (T-72). Window expiry triggers the guarded sweep (T-73 discipline).                                                                                                                                                                                                                                                                                                                                                                                                                                                       | `trash_batches.window_days`/`expires_at`; `setBatchItemSaved`; `trash_batch_saves` (Q-07); `save_leaving_soon` grant                                                                                                   | The rescue dataset feeds rule tuning (PLAN-014).                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| T-78  | Gate Skip                    | The audited escape hatch that lets a trusted install delete WITHOUT the admin poster-review gate: when the `trash_skip_admin_gate` App Setting (T-80) is on, `createBatchFromPending` auto-green-lights straight `draft → leaving_soon` with `gate_skipped = true` and system attribution — the audit trail always distinguishes a skipped gate from a human green-light. The mechanism ships here; the DECISION to flip it (graduation criteria) is PLAN-014.                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `trash_batches.gate_skipped`; `trash_skip_admin_gate` setting; `createBatchFromPending` skip path                                                                                                                      | Only `greenlightBatch` OR this audited path reaches `leaving_soon` (ADR-025 C-01).                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| T-79  | Deletion Snapshot            | The per-item metrics frozen in the SAME transaction as an item's `deleted` state + `trash_expedited` event at sweep time: `deleted_size_bytes`, `deleted_resolution`, `deleted_imdb_rating`, `deleted_tmdb_rating` (title/ids ride the creation snapshot; media kind = the batch). The durable source PLAN-013's reclaim metrics read from real rows (Q-08).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | `trash_batch_items.deleted_*`; `expireOneBatch`                                                                                                                                                                        | Written intent-first, alongside the guarded per-item delete (never `/collections/handle`).                                                                                                                                                                                                                                                                                                                                                                                                                           |
| T-80  | App Setting                  | A generic, audited key→jsonb store for small operational knobs (`app_settings`, key CHECK-constrained to `APP_SETTING_KEYS`). Written ONLY by the `setAppSetting` single-writer, which co-writes an `update_app_setting` permission_audit row same-tx (Q-06). First keys: `trash_skip_admin_gate` (bool), `trash_default_window_days` (int). Reusable — PLAN-010 (MOTD) + PLAN-013/014 (space target, tuning) add keys.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | `app_settings` (migration 0017); `getAppSetting`/`setAppSetting`; `trash.settings.*`; `update_app_setting` audit                                                                                                       | Absent key ⇒ the documented default (fail-safe).                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| T-81  | Bulletin Section             | The top-level section (BC-05) wrapping two sub-tabs — the Feed (T-84) + the Messages board (T-85, since PLAN-034 the Ticket [T-145] Helpdesk). Gated by the Section Permission (T-65), section id `bulletin`, whose no-row default is **Read-Only** (the Feed is for everyone; Disabled hides the whole section). The backend vertical ships first; the Feed/Messages UI is a Fable UX follow-up (ADR-026 / DESIGN-012, PLAN-009). **Amended (DESIGN-004 D-22, 2026-07-14):** surfaced in the top-nav + section heading under the ratified name **Tickets** (`HELPDESK_NAME`) — a LABEL change; the section id / route / grants stay `bulletin` / `messages`. The `/admin/roles` section column keeps "Bulletin".                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | `SECTION_IDS += 'bulletin'` (migration 0018); `communication.*` router                                                                                                                                                 | Default `read_only` (vs Trash's `disabled`) — SECTION_DEFAULT_LEVELS.                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| T-82  | Notification                 | A normalized inbound third-party event persisted in the durable `notifications` store — a `source` + `type` (event type) + `title`/`body` + a sanitized `payload`, plus best-effort attribution (`actor_user_id` via email, `media_item_id` via tmdb/tvdb) + `occurred_at` + a `source_event_id` dedupe key. NOT a `ledger_events` row (ADR-026 B — a separate store, different cadence/sources).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | `notifications` (widened migration 0018); `recordNotification` single-writer                                                                                                                                           | Idempotent via the `(source, source_event_id)` partial-unique index.                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| T-83  | Notification Source          | One of the webhook sources the receiver accepts: `maintainerr` (PLAN-006 #1), `seerr` (the single canonical name for **both** Overseerr and Seerr), `tautulli`. The *arrs are a deferred addition (Q-04). Each has its own env shared secret + payload adapter.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | `NOTIFICATION_SOURCES` (text+CHECK); `WEBHOOK_SECRET_ENV`; `parserForSource`                                                                                                                                           | Overseerr folds into `seerr` — not its own value.                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| T-84  | Feed                         | The Bulletin sub-tab: a keyset-paginated (newest-first by `occurred_at`) browse over the whole `notifications` store, joined to the attributed user + linked Media Item, with simple params (source, event type, media-link presence). The Trash Activity tab is the same store filtered to `source='maintainerr'`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `communication.feed`; `notifications` + `keyset.ts`                                                                                                                                                                    | Read-gated (`sectionProcedure('bulletin','read_only')`); no full filter-engine port backend-side (Q-05).                                                                                                                                                                                                                                                                                                                                                                                                             |
| T-85  | Message                      | A user-posted durable board entry (`messages`): subject optional, body required, optionally linked to a Media Item. Free-form discussion/triage that **complements** — never replaces — the structured Fix flow, and never mutates media/*arr state. Flat v1 (no threads/reactions).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | `messages` (migration 0018); `postMessage`/`editMessage`/`moderateMessage`                                                                                                                                             | Authors edit their OWN; a linked Media Item is a reference only.                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| T-86  | Message Status               | A Message's soft moderation lifecycle — `visible` (shown), `hidden` (moderator soft-hide), `deleted` (moderator soft-delete). A status change **preserves the row + content** (the audit trail); content is never physically removed. Non-moderators only ever see `visible`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | `MESSAGE_STATUSES`; `messages.status` (CHECK)                                                                                                                                                                          | Moderation stamps `moderated_by/at/note`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| T-87  | Message Action Grant         | A role's fine-grained per-action Bulletin permission — `post` (create/edit own messages) or `moderate` (hide/delete/restore any). A ROW is the grant (presence = granted; no boolean); layered on the coarse `bulletin` Section Permission (which gates READ). Admin implies both with no rows. Mirrors the Trash Action Grant (T-72).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | `role_message_action_grants` (migration 0018); `setRoleMessageActions`; `SessionRole.messageActions`; `messageActionProcedure`; `update_message_actions` audit                                                         | Server-authoritative; session-carried.                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| T-88  | Webhook Receiver             | The generic, session-unauthenticated, per-source-shared-secret-gated in-cluster ingress `POST /api/webhooks/<source>` that persists a Notification (T-82) through `recordNotification`. Hardened: constant-time secret compare, 64KB body cap, known-shape validation, prototype-key stripping. Fail-closed (unset secret ⇒ 503).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | `apps/web/app/api/webhooks/[source]/route.ts`; `webhook-sources.ts` (`secretsMatch`, parsers)                                                                                                                          | In-cluster only (works before the R-64 public cutover); `/api/webhooks/maintainerr` unchanged.                                                                                                                                                                                                                                                                                                                                                                                                                       |
| T-89  | MOTD (Message of the Day)    | The optional single admin-set dashboard banner broadcast to every authed user, mounted above the greeting. **Active** when `enabled`, message non-blank, and within its optional `startsAt`/`endsAt` window (inclusive start, exclusive end). Severity `info`\|`warning` (ARIA `status`\|`alert`; `--color-info`/`--color-warning` tint). Dismissible per user (localStorage keyed by a content version — an edit re-shows it). Message is a **sanitized markdown subset** (DESIGN-004 D-17: links/bold/italic/code/breaks/lists + bare-URL autolink, ≤500 chars) rendered React-element-only — no HTML, http(s)-only hrefs; severity glyph is a themed inline SVG (never emoji). **Reuses the App Setting (T-80) store** under key `motd` (jsonb record) — no bespoke table; written only by the `setMotd`/`clearMotd` single-writers → `setAppSetting` (audited `update_app_setting` same-tx, T-39).                                                                                                                                                                                                                                                                                        | `app_settings['motd']` (CHECK relax, migration 0019); `getActiveMotd`/`setMotd`/`clearMotd`; `motd.*` router; `motd-banner.tsx`; `/admin/motd`                                                                         | Single global banner (no per-role/per-user targeting); dismissal is client-only in v1 (ADR-027 C-04).                                                                                                                                                                                                                                                                                                                                                                                                                |
| T-90  | Action Progress Phase        | The derived, user-facing status of a downstream *arr action (Fix / Force Search) between trigger and terminal: `queued \| searching \| grabbed \| downloading \| importing \| completed \| nothing_found \| stalled \| failed`. A **projection** over the Fix Lifecycle (T-43) / the `search_requested` event (T-44) + the live Download Queue (T-91) + recent ledger milestones — **never stored**, recomputed per poll (ADR-028). `downloading` carries `progressPct` (from size/sizeleft) + `etaSeconds`; a season/artist roll-up carries `perChild` phases with a least-advanced headline.                                                                                                                                                                                                                                                                                                                                              | `computeFixProgress`/`computeSearchProgress`, `derivePhaseForTarget` (`packages/domain/src/action-progress.ts`); `fix.progress`/`fix.searchProgress`                                                                   | Derived, read-only; the durable `fix_requests.status` can momentarily lag (the projection reads a matching import before the sync cron flips the row).                                                                                                                                                                                                                                                                                                                                                               |
| T-91  | Download Queue               | The owning *arr's live download queue (`GET /api/v3\|v1/queue`), read read-only for progress: `size`/`sizeleft` → percent, `status`/`trackedDownloadStatus`/`trackedDownloadState`, `estimatedCompletionTime`, `errorMessage`. Filtered server-side by the parent id (`seriesIds`/`movieIds`/`artistIds`, verified live 2026-07-07). Not synced, not stored; the source of the `downloading`/`importing` phases.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | `getQueue()` on the read clients + `queueRecordBaseSchema` (`packages/arr`)                                                                                                                                            | BC-03 ACL — only the consumed subset enters the app; the `@hnet/arr/write` guard is untouched (read side).                                                                                                                                                                                                                                                                                                                                                                                                           |
| T-92  | Found-Nothing / Stalled      | The two never-stuck terminals (ADR-028): **nothing_found** (no grab within the 15-min found-nothing window after a search) and **stalled** (a non-terminal action past the 45-min staleness threshold with no queue/history activity, or a live queue record with `trackedDownloadStatus:'error'` / `importBlocked` / `importFailed`), the latter carrying a retry affordance (re-issue the same action).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | `FOUND_NOTHING_WINDOW_MS` (15m) / `STALLED_THRESHOLD_MS` (45m) constants                                                                                                                                               | Documented constants, not settings v1; graduate to `app_settings` (T-80) if music indexers prove slower.                                                                                                                                                                                                                                                                                                                                                                                                             |
| T-93  | Action Feedback              | The contract (R-106/R-107) that every user-triggered downstream *arr action reports status back — wire-ack ("searching") → live phase → terminal — surfaced beside the button, in the shared item History, and in My Fixes, cascading to every child a roll-up touched. The household-visibility promise for the deliberately small *arr blast radius.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | ADR-028; DESIGN-005 D-20/D-21                                                                                                                                                                                          | Poll-on-demand, read-only v1 (no push, no background poller); the progress UI is a Fable follow-up.                                                                                                                                                                                                                                                                                                                                                                                                                  |
| T-95  | Space Target                 | (ADR-030 / R-111) A configurable **percent-used ceiling per Plex server** (the owner's mental model — "HaynesTower < 80%"), the number utilization is judged against. Stored in the App Setting store (T-80) under key `space_targets` (jsonb, keyed by `plex_servers.slug` → percent; sparse, default `{}` = no target). PLAN-013 **stores + displays** it (a reference line); PLAN-014 acts on it (Q-03 split). A documented slug→rootfolder-path map (`STORAGE_ARRAYS`, DESIGN-013 §4) ties a server slug to a physical array; only `haynestower` maps to a surfaced array today.                                                                                                                                                                                                                                                                                                                                                        | `AppSettingValueMap['space_targets']` / `SpaceTargets`; `storage.targets.get/set` (`setAppSetting`, audited)                                                                                                           | Set through the audited single-writer; CHECK-relax migration 0021. `haynesops`/`hayneskube` reserved (CephFS in-cluster Plex), not yet surfaced.                                                                                                                                                                                                                                                                                                                                                                     |
| T-96  | Fill/Drain Rate              | (ADR-030 C-02 / R-109; **amended 2026-07-09** — C-04 amendment) Utilization as a **time-series** — is the array draining toward the space-target floor or still filling? Sourced from the exportarr `{radarr,sonarr,lidarr}_rootfolder_freespace_bytes` series in Prometheus (the ONLY media-library disk series — node-exporter/kubelet don't see the libraries), surfaced **natively** since 2026-07-09: `storage.trend` queries the in-cluster Prometheus directly (`query_range`; `PROMETHEUS_URL`, in-cluster default in code) and the app draws the chart — per-array free-bytes lines grouped exactly like `getUtilization`, the Space Target (T-95) drawn as a dashed FREE-bytes floor. exportarr carries freeSpace only (no total), so the trend plots free-bytes with a target line, never a % (totals come live from `/diskspace`). The Grafana dashboard stays the LAN power tool behind a muted footnote (the old deep-link was LAN-only — dead off-LAN). | `getStorageTrend` / `storage.trend` (`packages/api/src/storage-trend.ts` + `prometheus.ts`); the Storage-tab chart (`freespace-trend.tsx`); Grafana uid `media-storage-utilization` (OPS-007, LAN footnote)            | Native chart (WAS deep-linked Grafana — retired as the user-facing surface). Prometheus down ⇒ `unavailable` degrade, never a crashed tab. No HaynesTower node/SMART fallback (C-06).                                                                                                                                                                                                                                                                                                                                |
| T-97  | Reclaim Attribution          | (ADR-030 C-01 / R-110) Who/what freed how much space — reclaimed **bytes by category** (Movies vs TV) AND **by resolution** (the bang-for-buck "3 × 4K = 90%" view), a cumulative-reclaim curve, and a **per-batch rollup attributed to the green-lighting admin** (`trash_batches.greenlit_by`), over a selectable window (30/90/365/all). Source of record = the batch-sweep **Deletion Snapshots** (T-79, `trash_batch_items.deleted_*`). Direct-expedites are a best-effort second-class series from `trash_expedited` payloads (forward-captured size/resolution; pre-capture ones excluded).                                                                                                                                                                                                                                                                                                                                          | `getReclaim` / `storage.reclaim`; native page (source of record)                                                                                                                                                       | Consumes, never backfills — starts empty, accrues from sweeps. Native (Postgres), not Grafana.                                                                                                                                                                                                                                                                                                                                                                                                                       |
| T-98  | Space Policy                 | (ADR-031 / R-112) The **propose-only** response to space pressure: a scheduled, **opt-in, DEFAULT-OFF** `space-policy` sync mode that reads utilization (`getUtilization`) against the Space Target (T-95) and, for each **over-target** (`usedPct > target`), opted-in physical array with no open batch + past cooldown + enough candidates, **PROPOSES** a draft Curation Batch (T-75) via `createBatchFromPending` — the ordinary `admin_review` path. It **never** greenlights, **never** sweeps; the admin gate stays the human check and the audited Gate Skip (T-78) is not special-cased. One-open-per-kind blocks duplicates (graceful skip); a per-kind **cooldown** (default 7d) + **minCandidates** (default 1) prevent spam. Config lives in the App Setting store (T-80) under key `space_policy`. Each proposal writes a `trash_space_policy` ledger event + a `space_policy` notification (source `trash`) explaining WHY. | `app_settings['space_policy']` / `SpacePolicy` (CHECK relax, migration 0022); `evaluateSpacePolicy` / `getSpacePolicyStatus`; `storage.policy.{get,status,set}`; `space-policy` sync mode; `/admin/storage` card       | Propose ≠ dispose (ADR-031 C-01/C-02). Writes no `sync_runs` row (like the sweep). CronJob ships **suspended** — owner arms it. array→kind: `haynestower`→movie+tv; music never batchable.                                                                                                                                                                                                                                                                                                                           |
| T-99  | Rules Tuning / Graduation    | (ADR-031 C-05/C-06 / R-113/R-114) The **save-data as tuning evidence** — a read-only report of what the family **rescued** vs. what was **deleted** (T-77 Save Window data + T-79 states + Media Metadata), bucketed by **resolution**, **rating band**, and **collection** (Q-02 collection-grain), with `save-rate = rescued/(rescued+deleted)`. It **never** edits a Maintainerr rule (Q-03 — human-driven). It also computes **skip-gate graduation readiness** (gives T-78 teeth, resolving ADR-025 C-07): the suggested bar is **≥3 completed policy-proposed batches, ≤10% aggregate save-rate, 0 restores of swept items**; the owner MAY then flip the audited skip-gate (the flip stays an owner action).                                                                                                                                                                                                                         | `getTuningReport` (`byResolution`/`byRatingBand`/`byCollection` + `graduation`); `trash.tuning`; `GRADUATION_THRESHOLDS`; `/admin/storage` tuning block                                                                | REPORT, not auto-tuner. Rescued = items that ended `saved`; guardian-`skipped` excluded from the ratio.                                                                                                                                                                                                                                                                                                                                                                                                              |
| T-100 | Notification Outbox          | (ADR-034 / DESIGN-015 / R-115) The **transactional outbox** decoupling a durable batch-state change from the flaky Pushover push: `notification_outbox` (migration 0024) holds `channel`/`event_type`/`payload`/`earliest_send_at`/`sent_at`/`attempts`/`last_error`. Batch writers **enqueue in the SAME tx** as the transition (`enqueueOutbox`) on `batch_created` (manual + policy-proposed), `batch_leaving_soon` (+ a `batch_leaving_soon_reminder` for the day before expiry), and `batch_swept`. A **`notify-outbox` sync mode** drains due rows (`sent_at IS NULL AND attempts < 5 AND earliest_send_at ≤ now`) to `api.pushover.net`, deep-linking `…/trash?tab=<movies\|tv>`; success sets `sent_at`, failure backs off then parks at 5. **No `PUSHOVER_*` env ⇒ a clean no-op that leaves rows queued** (disabled-safe). Guarded single-writer table.                                                                           | `notification_outbox` / `NOTIFY_OUTBOX_EVENT_TYPES`; `enqueueOutbox` / `deliverOutbox` / `pushoverSenderFromEnv` (`@hnet/domain`); `notify-outbox` sync mode                                                           | DISTINCT from the T-82 in-app Notification feed (that is inbound/in-app; this is the outbound push queue). At-least-once delivery. Writes no `sync_runs` row (like the sweep).                                                                                                                                                                                                                                                                                                                                       |
| T-101 | Delivery Window              | (ADR-034 / DESIGN-015 / R-116) The owner's **quiet-hours** control for T-100 pushes: App Setting (T-80) `notify_window` (jsonb `{ startHour, endHour, tz }`, default **18–22 `America/New_York`**), admin-editable + audited via `setAppSetting`. Enqueue computes each outbox row's `earliest_send_at` against it — **inside the window ⇒ ASAP; outside ⇒ the next window-open** in `tz`; the leaving-soon **reminder ⇒ window-open on `expiresAt − 1 day`** (the "day before it leaves", clamped forward if the window is < 1 day). Window math is timezone-correct via `Intl.DateTimeFormat` offset probing (no dependency).                                                                                                                                                                                                                                                                                                             | `app_settings['notify_window']` / `NotifyWindow` (CHECK relax, migration 0024); `getNotifyWindow`; `computeEarliestSend`/`computeReminderSend`; `storage.notify.window.{get,set}`; `/admin/storage` Notifications card | Quiet hours are DATA (`earliest_send_at`), not a resident timer. Overnight windows (`start ≥ end`) out of scope.                                                                                                                                                                                                                                                                                                                                                                                                     |
| T-102 | Requested Auto-Save          | (ADR-025 errata / DESIGN-011 D-11, build B) A **system** save of a requester-carrying Deletion Candidate (T-69) at batch snapshot: `createBatchFromPending` marks a non-tag-protected requester item `trash_batch_items.state='saved'` with **`saved_reason='requested'`**, `saved_by=NULL`, and **no Maintainerr exclusion** (migration 0026 adds `saved_reason` + `requested_override`). It is the **person-shield** glyph (distinct from the human filled shield) and is **un-savable by any holder of save rights** (the T-77 ownership gate governs human rescues only). Un-saving one is the human **override** — it sets the sticky `requested_override` flag (audited `overrodeRequested:true`), and the sweep guardian's requester keep no longer protects it (**requested + never-unsaved → kept; requested + explicitly-unsaved → deleted**). Excluded from the T-99 "who rescued" tuning stats (not a human rescue); counted in `counts.saved`. On the live pending wall a requester item is never inert — the person-shield is a normal save-toggle. | `trash_batch_items.saved_reason`/`requested_override` (migration 0026); `createBatchFromPending`/`setBatchItemSaved`/`sweepExpiredBatches`; `wallGlyph`/`pendingWallGlyph`; `RequesterShieldGlyph` | Distinct from T-77 Save Window (a HUMAN rescue). The guardian override defeats ONLY the requester keep — dnd/watched/unevaluable keeps are unchanged. |
| T-103 | Candidate Snapshot           | (ADR-035 / DESIGN-010 D-11) The Postgres **read-model** of Maintainerr's pending set: `trash_candidates` (per-kind flat rows — collection id/title/`deleteAfterDays`, maintainerr id, tmdb/tvdb ids, size, verbatim `addDate`, crawl `ord`) + `trash_candidates_state` (per-kind `refreshed_at`/count/bytes), migration 0027. Rebuilt by the full/incremental sync post-step (15 min), rule-edit triggers, and the walls' manage-gated **Refresh**; expedite/sweep drop their deleted ids immediately. Serves `trash.pending`/`trash.pendingCandidates`/Overview counts; the ledger join + the visible page's exclusion check stay LIVE at read. **Display-only** — every deletion decision still reads Maintainerr fresh (T-69 stays the live notion). Prod serves stale-instantly + background refresh past 20 min ("candidates as of N min ago"); non-prod refreshes inline per read (determinism). | `trash_candidates`/`trash_candidates_state` (migration 0027); `refreshTrashCandidates`/`removeTrashCandidateRows`/`readCandidateSnapshot` (`@hnet/domain trash-candidates.ts`); `trash.refreshCandidates` | Derived, rebuildable state — the single writer appends no ledger audit rows (ADR-035 C-05 exemption). Guard list covers both tables. |
| T-104 | Watch Visibility             | (DESIGN-010 D-12, owner-directed 2026-07-09 — option B, INFO not protection) The cross-server "when/where a slated title was last watched" surfaced on the trash walls + item detail card, WITHOUT changing any protection. Backed by the T-57 pair `last_watched_at`/`last_watched_server` (migration 0028): the MAX last-watch across all three Tautulli histories (full history; TV rolled up to the show) + its estate slug. A **recently** watched item (≤ `RECENTLY_WATCHED_WINDOW_DAYS`) keeps its inert `eye` corner glyph (guardian keeps it). A title watched **longer ago** shows a MUTED meta-line indicator (`WatchedAgoNote`, `wall-watched`) + tooltip `Last watched on <server> · <Mon YYYY>` and stays FULLY actionable (tap-save/slate/delete) — an ever-watched-but-not-recent item is still sweep-deletable. Requested / person-shield still WINS the corner glyph; watch info moves to the caption/tooltip. | `media_metadata.last_watched_at`/`last_watched_server` (migration 0028); `shapePendingItems`/`getBatchDetail` → `TrashPendingItem`/`BatchDetailItem`; `WatchedAgoNote`/`TrashPendingNotice`; `watchServerLabel`/`lastWatchedLabel`/`watchedLongAgo` (`apps/web/lib/trash.ts`) | Info only — NO guardian/keep/candidate-filter change (T-70/T-73 unchanged). Same source instant as T-57 `last_viewed_at`; stored as a server-attributed display pair. |
| T-105 | Aging Invariant (Delete-After Horizon) | (ADR-036 / DESIGN-010 errata 2026-07-09) The SAFE-gate assertion that Maintainerr's OWN aging worker can never mass-delete outside this app's pipeline. That worker deletes a collection member when `addDate <= now − deleteAfterDays·86_400_000`, skipping ONLY `arrAction === DO_NOTHING (4)` (no null/0 guard — a null/0 horizon deletes immediately). `auditMaintainerr` therefore requires, per ACTIVE collection: a **rule pool** (non-Leaving-Soon) has `deleteAfterDays ≥ AGING_HORIZON_MIN_DAYS (3650)` AND `arrAction === 0` (so the app keeps the delete via `/collections/media/handle`); an app-managed **Leaving-Soon** collection has `arrAction === 4`. A breach folds into `safe:false` with a specific banner reason and blocks Expedite + sweep; a collections read failure fails closed. | `evaluateAgingInvariants` / `AGING_HORIZON_MIN_DAYS` / `MaintainerrAudit.agingViolations` (`@hnet/domain trash-flow.ts`); `SafetyBanner` (`apps/web/components/trash-safety.tsx`); `maintainerrCollectionSchema.arrAction`/`manualCollection` | Incident 2026-07-09: both prod rule pools were `deleteAfterDays:60`/`arrAction:0` (~16 TB) — defused to 9999. Reinforces T-76: a Leaving-Soon collection's protection is its `arrAction:4`, not `deleteAfterDays:null`. |
| T-106 | Metrics Section | (ADR-037 / DESIGN-016 / R-117, PLAN-017; **placement amended by DESIGN-004 D-22, 2026-07-14 — now a role-gated USER-MENU entry, not a top-nav tab**) The **Metrics** section with a sub-tab **shell**: an **Overview** ships now (WAN upload/download usage-vs-capacity meters + a cluster load/memory tile + a storage-utilization snapshot reusing T-95 `getUtilization`); Apps/Hardware/Network sub-tabs are reserved placeholders for PLAN-018/019/020. Visibility reuses the **`metrics`** T-63 Section Permission with a **`disabled` no-row default** → ships **Admin-only**; a role row opts other roles in. Grafana stays deep-linked (never embedded — the ADR-030 C-04 posture). | `SECTION_IDS += 'metrics'` / `SECTION_DEFAULT_LEVELS.metrics='disabled'`; `apps/web/app/(app)/metrics/*`; `metricsRouter` (`metrics.overview`/`metrics.access`/`metrics.capacity.*`) | Member-facing (unlike the admin-only T-95 Storage surface). Overview degrades to a note when Prometheus is unreachable — never a broken page (ADR-015 in-place, bounded poll). |
| T-107 | Metrics Level (Full / Limited) | (ADR-037 / DESIGN-016 / R-118, PLAN-017) A per-role access level — **`full`** or **`limited`** — on **`roles.metrics_level`** (`text`, default `limited`; Admin implies `full` via the session short-circuit). `limited` **hides user-aware metrics** (who requested what, per-user space, any username-bearing label) and finer network grain; **hardware is ungated**, and **no device identities at any level**. Enforced **server-side in the payload shape** (the router omits full-only keys — e.g. `network.wanLinks` — for `limited`, never serialized). Flips are audited: the `setRoleMetricsLevel` single-writer co-writes a `permission_audit` **`update_role_metrics_level`** row in the SAME tx (Admin immutable). | `METRICS_LEVELS`/`METRICS_LEVEL_RANK` (`@hnet/db`); `roles.metrics_level` (migration 0031); `setRoleMetricsLevel` (`@hnet/domain`); `effectiveMetricsLevel`/`metricsProcedure` (`@hnet/api`); `roles.setMetricsLevel` | Orthogonal to T-63 section visibility: visibility gates *whether* you see Metrics; level shapes *how much*. Column mirrors `roles.grants_all` (single scalar per role). |
| T-108 | Prometheus Reader | (ADR-037 C-07 / DESIGN-016 D-02, PLAN-017) The read-only **`@hnet/metrics`** package: a Prometheus HTTP client — instant `query` + range `query_range`, zod-validated wire shapes — against the in-cluster `kube-prometheus-stack` (default `http://kube-prometheus-stack-prometheus.observability.svc.cluster.local:9090`, `PROMETHEUS_URL` override, **no secret**), plus the WAN/node/memory read models (`getNetworkOverview`/`getHardwareOverview`). **No write surface ⇒ no import-confinement.** Any query failure degrades that tile to `unavailable` — never a throw. | `@hnet/metrics` (`createPrometheusClient`/`prometheusClientFromEnv`/`getNetworkOverview`/`getHardwareOverview`); `resolveMetricsReader` (`@hnet/api`); e2e stub `stub-prometheus.ts` | Distinct from the ADR-030 D-07 inline `packages/api/src/prometheus.ts` (range-only, storage-trend) — left untouched; the small client overlap is a deliberate, documented duplication (a future re-home is deferred). |
| T-109 | WAN Usage-vs-Capacity | (ADR-037 C-04/C-06 / DESIGN-016 / R-120, PLAN-017) The Overview headline: current WAN **usage** (`unpoller_site_transmit_rate_bytes{subsystem="wan"}` up / `…receive…` down, ×8/1e6 → Mbps) drawn against an admin-editable **capacity** (the T-80 App Setting keys `upload_capacity_mbps` default **300** = the practical Plex-server cap, and `download_capacity_mbps` default **2256 provisional**, Q-02), with the T-95 `.storage-array` meter idiom (tone ok→warn→danger). At `full` a per-uplink `wanLinks` breakdown (primary/failover capacity + current) is added; at `limited` only the aggregate meters — performance/load only, no client devices, no ISP-identifying strings. | `app_settings['upload_capacity_mbps']`/`['download_capacity_mbps']` (migration 0031 CHECK relax); `metrics.capacity.{get,setUpload,setDownload}`; `MetricMeter` (`apps/web/app/(app)/metrics/`) | Capacity is the meter **denominator**, not the usage source. `unpoller_wan_provider_*_kbps` corroborates the seed but is not the source of record. |
| T-110 | ytdl-sub Library | (ADR-038 / DESIGN-017 / R-121, PLAN-022) A **non-\*arr** Plex library produced by **ytdl-sub** on the k8plex/HAYNESKUBE server (`HOps Peloton`, `HOps YT`), surfaced as a **Library** sub-tab and read **directly** from Plex (`/library/sections/{key}/all`) — **never synced** into `media_items`/ledger (hard-rule 4 is N/A: no \*arr, Plex is the source of record). Sections are resolved by **library title** (not a hardcoded id); visibility is gated by the new **`ytdlsub`** T-65 Section Permission (`disabled` no-row default ⇒ ships **Admin-only**, owner flips per role). | `PlexReadClient.listSectionContents`/`sectionContentsSchema` (`@hnet/plex`); `ytdlsubRouter` (`ytdlsub.list`/`.libraries`/`.access`); `ytdlsubProcedure` (`@hnet/api`); `apps/web/app/(app)/library/ytdlsub-browser.tsx` | Read-only; no write-back to Plex/ytdl-sub. Music (`HOps Music`) stays the T-17..T-21 registry **share** library, not a browse grid. |
| T-111 | Time-based Show | (ADR-038 / DESIGN-017, PLAN-022) A "TV Show by Date"-style Plex show in a ytdl-sub library where **seasons/episodes encode time or grouping**, not broadcast order — Peloton class types whose **seasons encode duration** (*Bike Bootcamp (30 min)* → season 30); YouTube channels grouped by genre. Rendered as a poster tile with a muted "N seasons · M episodes" caption (`childCount`/`leafCount`). | `PlexSectionItem.childCount`/`leafCount` (`@hnet/plex`); `YtdlsubShow` (`@hnet/api`); the `.poster-card__body` caption | ~~Display idiom only; no drill-in this round~~ Since DESIGN-017 D-09 / R-132 (2026-07-10) the tile click-throughs open a **read-only drill-in** (show → seasons → episodes, lazily per expanded season, section-confined). Still no ledger, no actions. |
| T-112 | Durable Poster | (ADR-038 C-09 / DESIGN-017 Q-01 / R-124, PLAN-022) The owner's **replacement poster** for a time-based show whose Plex art keeps getting **wiped by re-scans**. This round ships **resilient display only** — the authed Plex-thumb **proxy** + a graceful `MediaPoster` **fallback tile** when art is missing — **NOT** a durable store. The durable **sink** (repo / PVC / Kometa-style overlay; serve-vs-reapply) is **deferred to owner Q-06**. | `apps/web/app/api/ytdlsub/poster/route.ts`; `resolveYtdlsubThumbUpstream` (`@hnet/api`); `MediaPoster` fallback (`apps/web/components/media-poster.tsx`) | The fallback tile is a **known interim**, not the final answer to the lost-poster pain (PRD Q-06). |
| T-113 | Apps Metrics (\*arr + downloaders) | (DESIGN-018 / OPS-008 / R-125..R-126, PLAN-018) The **Metrics → Apps** sub-tab: a curated, phone-friendly read of the media-automation apps grouped as **Collection** (library totals · monitored · missing · upgrades-pending), **Acquisition pipeline** (queue · grabs/hr · health issues), **Download clients** (SABnzbd `sabnzbd`/`sabnzbd-fast` lanes + qbittorrent/slskd reachability), and **Indexers/Prowlarr** (fleet · avg response time · query rate) — read from the T-108 Prometheus Reader via the new `getAppsMetrics` model, one muted **Grafana deep-link** per group (`d/arr-library-overview`, `d/downloads-clients-indexers`). **Both-levels** (T-107): the exportarr/first-party series are labelled only by `job`/`indexer`/`path`/`download_state` — **none name a user** — so nothing here is full-only; the level seam is still plumbed (`getAppsMetrics({ includeUserAware })` → a **present-but-empty** full-only branch) so a future requester panel slots in without a refactor. | `getAppsMetrics`/`AppsMetrics` (`@hnet/metrics`); `metrics.apps` (`metricsRouter`); `apps/web/app/(app)/metrics/apps-tab.tsx`; e2e `stub-prometheus.ts` app series | No migration / no new column — rides the T-106 `metrics` section + T-107 level. Read-only (no write surface). Grafana stays the verbose LAN layer; the in-app panels are the off-LAN surface. |
| T-114 | Network Metrics | (ADR-039 / DESIGN-019 / R-127, PLAN-020) The **Metrics → Network** sub-tab: at **`limited`** the two T-109 WAN usage-vs-capacity meters **plus a 7-day WAN throughput history sparkline** (its only value-add over the Overview); at **`full`** it ADDS the T-115 Infra-device Performance groups + WAN health (gateway speedtest + internet-path latency) + per-uplink caps + site rollup COUNTS (APs/switches/gateways/connected-device count). Read from the T-108 Prometheus Reader via the new `getNetworkMetrics` model (which REUSES `getNetworkOverview` for the meters — one WAN-PromQL home + one capacity denominator). One muted **Grafana deep-link** per group to the UniFi-Poller Network Sites / USW / UAP boards; the **Client-Insights board is deliberately NOT linked**. Bounded 45s poll, ADR-015 in-place, per-group degrade. | `getNetworkMetrics`/`NetworkMetrics`/`NetworkInfra` (`@hnet/metrics network.ts`); `metrics.network` (`metricsRouter`); `apps/web/app/(app)/metrics/network-tab.tsx`; `sparklinePolyline` (`apps/web/lib/metrics.ts`); e2e `stub-prometheus.ts` network series | No migration / no new column — rides the T-106 `metrics` section + T-107 level + T-109 capacity `app_settings`. Read-only. The disjoint `limited`/`full` shape is server-authoritative (`includeInfra`). |
| T-115 | Infra-device Performance | (ADR-039 C-02 / DESIGN-019 D-02, PLAN-020) The **`full`-only** per-infrastructure-device grain: the UniFi gateway/switch/AP **CPU · memory · load-average** (from `unpoller_device_{cpu,memory}_utilization_ratio` / `_load_average_1`, keyed by the device `name` + `type` label — `udm`→gateway, `usw`→switch, `uap`→AP; `pdu`/`uci` dropped). A device **name** ("Garage U7 Outdoor", the gateway "Westford DMSE") is **INFRASTRUCTURE, not a client** — allowed at `full`. There are **no per-client / per-station rows** at any level; the only client-adjacent number is the aggregate `unpoller_site_stations` **COUNT**. | `DevicePerfRow`/`mergeDevicePerf`/`deviceCategory`/`ratioToPct` (`@hnet/metrics network.ts`); the `.metrics-apps-table` device tables (`network-tab.tsx`) | Distinct from T-109's WAN meters (both-levels) — infra performance is the `full` value-add. Never named: `unpoller_client_*`, `_remote_user_`, `*_info` (see T-116). |
| T-116 | Allow-listed PromQL Module | (ADR-039 C-01 / DESIGN-019 D-05, PLAN-020) The privacy-invariant enforcement for Network Metrics: `network.ts` is the **single place any `unpoller` series is named** for the tab, and every PromQL it can issue is enumerated in **`NETWORK_ALLOWLIST_QUERIES`**. A **unit test PROVES** the invariant — each query names only allow-listed `unpoller_(site\|device\|wan)_*` metrics and matches **none** of the deny substrings (`unpoller_client_`, `_remote_user_`, `_info`, `mac`, `hostname`, `rssi`, `signal`), and a full end-to-end payload dump contains no client-identity field. So **"no client identities at ANY level" is machine-checked**, not a review convention. | `NETWORK_ALLOWLIST_QUERIES` (`@hnet/metrics network.ts`); `packages/metrics/__tests__/network.test.ts` ("network privacy invariant — the allow-listed PromQL module") | Refines T-107's server-side payload-shape enforcement (never-fetch/never-serialize) with a **never-name** structural guard. The reviewer-facing proof PLAN-020 required. |
| T-117 | Hardware Metrics / NVMe Endurance | (ADR-040 / DESIGN-020 / R-129, PLAN-019) The **Metrics → Hardware** sub-tab — **ungated** (both `full` and `limited` see the same payload; hardware is not user-aware). Its headline is the **NVMe endurance** panel: per-pool framing of the NAS cache — **Cache-apps** (`nvme0`+`nvme3`, btrfs MIRROR, the **critical** appdata pool, 57–60 % worn) vs **Cache-staging** (`nvme1`+`nvme2`, btrfs striped, **expendable**, over rated endurance — `percentage_used` 100, `critical_warning` bit 2, `smart_status` FAILED, yet `available_spare` 100 / `media_errors` 0 = "holding") — with a wear odometer (`smartctl_device_percentage_used`), a wear-rate **projection to 90 %** from Prometheus history (graceful **"insufficient history"** until it accrues), and the real EOL signals (`available_spare` vs threshold, `media_errors`, `critical_warning` bits). Plus a **Drive health** table (every reporting SMART device; a sleeping array disk emits no series → shown **"asleep", never red**), **Node load** (load-per-core, memory, hottest temp), and the T-119 Proxmox showcase. | `getHardwareMetrics`/`HardwareMetrics`/`NvmePool`/`DriveHealth`/`projectWear`/`NVME_POOLS` (`@hnet/metrics hardware.ts`); `metrics.hardware` (`metricsRouter`); `apps/web/app/(app)/metrics/hardware-tab.tsx`; e2e `stub-prometheus.ts` smartctl/pve/node series | Sources are pve-exporter (host/VM) + node-exporter + smartctl — **all already scraped** (haynes-ops change = image bump only); glances deferred (ADR-040 Q-01). Read-only; rides the T-106 `metrics` section (no level shaping). |
| T-118 | SMART Alert Transition | (ADR-040 / DESIGN-020 / R-130, PLAN-019) The **critical-only, transitions-only** SMART page. A **`smart-alerts`** sync mode (the `notify-outbox`/`space-policy` early-return precedent — no `sync_runs` row) reads the smartctl series and `evaluateSmartAlerts` compares each drive to its persisted **`smart_drive_state`** row; on a critical transition — `smart_status` pass→FAIL, `media_errors` 0→n, `available_spare` crossing `threshold + 10`, a **NEW** `critical_warning` bit, or the **critical appdata pool** wear crossing 80/90 % — it enqueues **one** `smart_degraded` T-100 outbox row **and** updates the state row in the **same transaction** (the outbox row is the durable transition record). **First sight of a drive records a baseline and pages NOTHING** — so the known staging-pool bad state never pages; only NEW deterioration does. FAIL→pass ⇒ `smart_recovered`. Temperature + warn-tier are dashboard-only. Disabled-safe: no `PUSHOVER_*` ⇒ the enqueue still records the transition, the drainer no-ops. | `evaluateSmartAlerts`/`SmartDriveReading`/`SmartAlertsReport` (`@hnet/domain smart-alerts.ts`); `smart_drive_state` (migration 0033, guard-listed); `NOTIFY_OUTBOX_EVENT_TYPES += smart_degraded/smart_recovered`; `SYNC_RUN_KINDS += smart-alerts`; `runSync` `smart-alerts` block (`@hnet/sync`); `renderOutboxMessage` smart cases (deep-links `…/metrics?tab=hardware`) | The baseline-on-first-sight rule is the structural guarantee (unit-proven: baseline ⇒ 0 rows, forced transition ⇒ exactly 1, unchanged re-run ⇒ 0). Extends T-100/T-101 with two hardware event types. |
| T-119 | Proxmox Showcase | (ADR-040 / DESIGN-020 / R-129d, PLAN-019) The host→VM capability view in the Hardware tab: `pve_node_info` lists the **5 PVE nodes** (HaynesIntelligence, twin-top, twin-bottom, pve-filet02, pve04); each host tile shows CPU (`pve_cpu_usage_ratio`), memory (`pve_memory_usage_bytes`/`_size_bytes`), and uptime (`pve_uptime_seconds`), and **expands in place** (ADR-015 allowed exception — a deliberate in-place expansion, no neighbor reflow) to its VMs (nested by the guest's `node` label; per-VM up/CPU/mem). GPU rows are out of scope (PLAN-021). Source is the newly-live `prometheus-pve-exporter` (owner added the 1Password `proxmox` item overnight). | `PveHost`/`PveVm` (`@hnet/metrics hardware.ts`); `pve_*` series; `hardware-tab.tsx` host expander | pve-exporter is cluster-native + durable (survives glances restarts) — ADR-040 chose it over glances REST. No dedicated pve Grafana board yet ⇒ deep-links to Grafana Explore (DESIGN-020 Q-02). |
| T-120 | Thumb Variant | (ADR-041 / DESIGN-017 D-07 / R-131) A **fixed, allow-listed size** the ytdl-sub Plex-thumb proxy serves instead of the original full-size art: `grid` (300×450 WebP — the 2:3 poster tile, ≈2× the 132–160 px grid box) or `still` (320×180 WebP — the 16:9 episode row). Produced by k8plex's own **photo-transcode** endpoint (`/photo/:/transcode`, token in the header), memoized in an **in-process byte-capped LRU** (NOT a store — evaporates on restart), and revalidated by a **strong ETag** over `(size, thumb)` — self-versioning because Plex thumb paths embed `lastWrite`. A transcode miss falls back to the **original art**, then the T-112 fallback tile. | `YTDLSUB_THUMB_SIZES`/`resolveYtdlsubThumbUpstream`/`ThumbLruCache` (`@hnet/api` `ytdlsub-poster.ts`); `apps/web/app/api/ytdlsub/poster/route.ts` (`?size=`) | The variant set is CLOSED (never client-chosen dimensions — not an open resize proxy). Measured 2026-07-10: Peloton wall 29.3 MB → 46 KB, YouTube 6.9 MB → 856 KB. |
| T-121 | Blueprint Baseline / Config-as-Code | (ADR-042 / OPS-009 / R-136, PLAN-011) The Authentik login estate (brand, flows, sources, MFA) managed as declarative **Authentik blueprints** in `haynes-ops`, one file per concern (`10-hnet-brand`/`20-hnet-flows`/`30-hnet-sources`/`40-hnet-mfa`), delivered via a kustomize `configMapGenerator` → chart `values.blueprints.configMaps` → worker mount at `/blueprints/mounted/cm-authentik-hnet-blueprints` → engine discovery. The `10`/`20`/`30` **baseline** reproduces the live estate **drift-zero** (zero `model_updated` on apply) — a faithful, reviewable, revertible mirror. Authoring conventions: **`!Find` over `!KeyOf`** (robust to a partially-committed apply), **pk-pin pre-existing objects** (a `PolicyBinding.target` is the `pbm_uuid`, not the `FlowStageBinding` pk), and an in-process **`Importer.validate()` → `VALID=True` dry-run before every commit**. Ownership rule: a behavior blueprint that takes over a baseline object removes the baseline's copy so the two never flip-flop (`40` owns the order-30 stage+binding; absent from `20`). | `haynes-ops/kubernetes/main/apps/network/authentik/app/blueprints/{10,20,30,40}-hnet-*.yaml` + `kustomization.yaml` `configMapGenerator` + `helmrelease.yaml` `blueprints.configMaps` | Supersedes the API-applied precedent (OPS-001 / `authentik-apply-seed/`) for brand/flows/sources/MFA; the OIDC provider/app stay API-managed (ADR-042 Q-11). No app-repo code — config lives in the GitOps repo. |
| T-122 | MFA Exemption Group | (ADR-042 / OPS-009 / R-135, PLAN-011) The **`mfa-exempt`** Authentik group whose members (`hnet-e2e`, `hnet-e2e-member`) **skip** the native MFA challenge so Playwright automation + non-interactive accounts stay green. Enforced by the expression policy **`hnet-mfa-exempt-skip`** bound (at order 20) to the order-30 authenticator-validation binding of `default-authentication-flow`, with `policy_engine_mode: all`. **Fail-closed**: an unresolved pending user returns "enforce MFA", and the policy-binding's `failure_result: true` means a policy error enforces rather than exempts. The only sanctioned exception to T-123. ak-outpost service accounts run no interactive flow and need no membership. | `40-hnet-mfa.yaml` entries: `authentik_core.group` `mfa-exempt`, `authentik_policies_expression.expressionpolicy` `hnet-mfa-exempt-skip`, `authentik_policies.policybinding` (pk `5415acce-…`) | Membership is by `!Find [authentik_core.user, [username, …]]`. Distinct from the app's role/permission model — this is an Authentik-side login-flow exemption, not an `@hnet` grant. |
| T-123 | Native-Account MFA | (ADR-042 / OPS-009 / R-133..R-134, PLAN-011) WebAuthn/TOTP **validation + first-challenge enrollment** enforced on the **username+password** path (order-30 stage `default-authentication-mfa-validation`, pk `3cdaa8fa-…`, of `default-authentication-flow`) for **native (internal-type)** accounts — `thaynes`, `akadmin`, hand-created locals. Live config: `not_configured_action: configure`, `device_classes: [totp, webauthn]`, `webauthn_user_verification: preferred`; enrollment chooser shows friendly names "Passkey (recommended)" / "Authenticator app (6-digit codes)". **Plex-source logins are unaffected** — they traverse `default-source-authentication`, a login-only source flow with no password/MFA stage (owner ruling: `thaynes`' Plex path accepted, Plex 2FA covers it). Owner factor: 1Password passkey + TOTP backup. | `40-hnet-mfa.yaml` `authentik_stages_authenticator_validate.authenticatorvalidatestage` (pk `3cdaa8fa-…`) + its `authentik_flows.flowstagebinding` (pk `3640815a-…`); webauthn/totp setup stages' `friendly_name` | Does not add a password surface to this app (hard-rule 5 intact — Authentik OIDC is still the sole sign-in). Break-glass: `akadmin` recoverable via `ak` shell; fast rollback = stage `not_configured_action → skip`. Safari/WebKit TOTP-setup caveat: use Chrome (OPS-009). |
| T-124 | Poster Guard | (ADR-043 / DESIGN-021 / R-137..R-140, PLAN-024) The **`poster-guard`** sync mode that keeps the owner's durable Peloton override posters applied on k8plex. It resolves the **HOps Peloton** section by title, walks shows + seasons, maps each to its override by **live show title / season index** (never a pinned ratingKey), and **re-applies only DRIFTED targets** — a target drifts when the live Plex thumb ≠ the baseline recorded at last apply, OR the mapped asset's sha256 changed, OR it was never applied. `decidePosterAction` (pure) returns `initial`/`drift`/`asset-updated`/`null`; a drifted target is re-pushed via the confined `PlexWriteClient.uploadPoster` (`POST …/posters`, non-destructive — Plex keeps prior art in the gallery) and one **append-only `poster_guard_applications`** row is inserted in the same tx (the drift baseline + audit). Like `smart-alerts` it writes **no `sync_runs` row** (a per-run correlation `run_id` groups a run's rows); bounded (~14 reads + drift-gated writes); hourly haynes-ops CronJob mirroring `sync-smart-alerts`. Unmapped targets (unknown show; index 0/75) are reported, never guessed. | `runPelotonPosterGuard`/`decidePosterAction`/`PosterGuardReport` (`@hnet/domain poster-guard.ts`); `poster_guard_applications` (migration 0034, guard-listed); `PlexWriteClient.uploadPoster` (`@hnet/plex` write subpath); `SYNC_RUN_KINDS += poster-guard`; `runSync` `poster-guard` block (`@hnet/sync`) | The first post-deploy run has no baseline ⇒ re-applies all mapped targets once (idempotent). Restores art **on the server** so native Plex clients see it; the ADR-041 proxy self-invalidates (ETag keyed on the thumb path). |
| T-125 | Peloton Override Poster | (ADR-043 / DESIGN-021 / R-137, PLAN-024) A **durable, owner-authored** poster the T-124 guard applies to a HOps Peloton show or season: 13 **series** posters (class types — Bike Bootcamp, Cardio, …, Yoga) + duration posters (`5/10/15/20/30/45/60/90/120-minutes.png`, plus the unused `60+-minutes.png`). They live **versioned in the app repo/image** at `packages/sync/assets/peloton-posters/` (ADR-043 C-01 — **not** a ConfigMap/DB `bytea`/NFS mount; the CronJob reads them from its own image), with normalized filenames (`60-minuites-02.png`→`60-minutes.png`, `90-minuites.png`→`90-minutes.png`). **Season index 60 → the clean `60-minutes.png`** ("60 MINUTES"), NOT the "60+ MINUTES" bucket art — the library uses a distinct Season 60 alongside 75/90/120. Adding/changing art = drop a PNG + a mapping entry + a release. | `PELOTON_POSTER_MAPPING`/`createFilePosterAssetSource` (`@hnet/sync peloton-poster-map.ts`); `packages/sync/assets/peloton-posters/*.png` | The asset home is a one-file seam (`createFilePosterAssetSource`) — portable to a DB/NFS home later without touching the guard. Diverges from the owner's initial DB-`bytea` lean (ADR-043; flagged for review). |
| T-126 | AI Usage Sync | (ADR-044 / DESIGN-022 / R-141, PLAN-021) The **`ai-usage-sync`** sync mode that mirrors **Open WebUI** chat usage into the app. It polls OWUI's **admin API** (`OPENWEBUI_API_KEY`; `GET /api/v1/chats/all/db` + `GET /api/v1/users/`) — **read-only against OWUI, never a live cross-DB read** (the *arr-ledger pattern) — normalizes each chat, and **UPSERTS** one row per chat into the **`ai_usage_chats`** mirror via the `@hnet/domain syncAiUsage` single-writer. A rebuildable read-model (the `trash_candidates`/`smart_drive_state` class): OWUI is the system of record, so the writer appends **no audit row** and the mode writes **no `sync_runs` row** (`SYNC_RUN_KINDS += ai-usage-sync` for CLI `--mode` parity only). Runs as a bounded haynes-ops CronJob mirroring `sync-smart-alerts`. | `syncAiUsage`/`getAiUsage` (`@hnet/domain ai-usage.ts`); `OpenWebUiClient`/`fetchOwuiUsage`/`normalizeOwuiChat` (`@hnet/sync openwebui.ts`); `ai_usage_chats` (migration 0035, guard-listed); `runSync` `ai-usage-sync` block | Verified against OWUI 0.7.2. A users-endpoint failure degrades to unresolved attribution that cycle (chats still sync). |
| T-127 | Image-Generation Count | (ADR-044 C-02 / DESIGN-022 / R-142, PLAN-021) The metric counting how many images the self-hosted AI **generated**. Heuristic: an **assistant-role** message whose `files[]` carries an entry of `type === 'image'` (url `/api/v1/files/{id}/content`) is one generation. **Only assistant files count** — a **user image upload** attaches to a **user-role** message and is never counted. Verified live: 27/27 observed image files were on assistant messages. The count (not the bytes) is stored per chat as `ai_usage_chats.image_count`. | `normalizeOwuiChat` image loop (`@hnet/sync openwebui.ts`); `ai_usage_chats.image_count`; unit-pinned in `packages/sync/__tests__/openwebui.test.ts` | A future OWUI version that records generations differently would need this heuristic revisited (unit-pinned to catch it). |
| T-128 | AI Usage Attribution Level | (ADR-044 C-03 / DESIGN-022 / R-142..R-143, PLAN-021) The **level-gated** shaping of the `metrics.aiUsage` payload, mirroring the ADR-037 metrics level model. A **`limited`** caller gets **aggregate counts + the per-day trend ONLY** (# chats, # image generations, # messages) — **no user identity, no model breakdown, no active-user count** (the user-aware-metrics gating rule; the identity columns are never selected into a limited response). A **`full`/admin** caller additionally gets the **by-model** ("for what") and **by-user** ("who / how long") breakdown. Server-authoritative — `getAiUsage` returns early for `limited`, so a member response structurally cannot carry a user id. | `getAiUsage` level branch (`@hnet/domain ai-usage.ts`); `metrics.aiUsage` (`@hnet/api`); seam proven in `packages/domain/__tests__/ai-usage.test.ts` + `packages/api/__tests__/metrics.test.ts` | The Metrics section ships Admin-only by default (ADR-021); the owner opens the AI tab to a role via `/admin/roles`, and the level decides the grain. |
| T-129 | Synced Tier | (ADR-045 / DESIGN-023 / R-146, PLAN-026) A **Role** that opts into **projecting to an Authentik group** — the cross-app role primitive delivered over the OIDC `groups` claim. Flagged by `roles.synced_tier` (default false; the seeded **Family** role is backfilled true — it projects to the pre-existing `family` group; Admin/Default stay app-local). Turning it on **auto-creates** the Authentik group (name = role name lowercased, unless a T-131 map override) AND ensures the same-named Open WebUI group (T-134), and adds the group to the Owned-Groups Allowlist (T-130). The tier's **existence** propagates to every Authentik-backed app automatically; per-app **entitlements** (OWUI models, Kavita libraries) stay per-app config. Turning it off is **non-destructive** (T-131). | `roles.synced_tier` (migration 0036); `provisionSyncedTier`/`deactivateSyncedTier`/`groupNameForRole` (`@hnet/domain authentik-portal.ts`); `roles.setSyncedTier`/`roles.create` `syncedTier` (`@hnet/api`) | A non-synced role writes nothing to Authentik. Existence ≠ entitlements — the creation UX states this so nobody expects model access to follow the group. |
| T-130 | Owned-Groups Allowlist | (ADR-045 C-02 / DESIGN-023 D-05 / R-147, PLAN-026) THE GUARDRAIL: the positive allowlist of Authentik group names the app may write membership for (`app_settings` key `authentik_owned_groups`, ships `['family']` + every auto-created tier). `assertGroupOwned` throws `AuthentikGroupNotOwnedError` (→ FORBIDDEN) **before any external call**, so the app is structurally incapable of touching admin-managed groups (`authentik Admins`, `mfa-exempt`) or any Authentik flow/stage/policy/provider/brand (those stay owned by the ADR-042 blueprints). Deactivating a tier removes its group from this list (stops managing) — non-destructive, group **deletion is out of scope**. | `getAuthentikOwnedGroups`/`setAppSetting`/`assertGroupOwned` (`@hnet/domain`); `app_settings.key 'authentik_owned_groups'` (migration 0036) | Mutated only through `setAppSetting` (same-tx `permission_audit` `update_app_setting`). The role→group override map is the companion key `authentik_group_map`. |
| T-131 | Authentik Group Portal | (ADR-045 / DESIGN-023 / R-144..R-145, PLAN-026) The capability that makes haynesnetwork the **role portal**: an admin assigns a Role and the app writes the matching **Authentik group membership**, propagating to every Authentik-backed app. The **group-membership write surface** — `@hnet/authentik/write` (group create + add/remove membership) and `@hnet/openwebui/write` (tier-group pre-create) — is **import-confined to `packages/domain`** exactly like `@hnet/plex/write` (the arr-write-import-guard test, extended to `/@hnet\/(arr\|plex\|authentik\|openwebui)\/write/`). Assignment is **exclusive across owned tier groups** (join the role's group, leave every other owned group) and is **enforcement, never decision** — the Role (BC-02) decides, the client applies. | `assignRolePortal`/`AuthentikPortalBundle` (`@hnet/domain authentik-portal.ts`, `authentik-clients.ts`); `AuthentikWriteClient`/`OwuiWriteClient`; `authentikPortal.assignRole` (`@hnet/api`) | The BC-04 (Plex Sharing) pattern applied to identity — delivers the R-30 "push permissions into Authentik" intent. External writes are audited after the apply (T-133 sibling `authentik_group_audit`). |
| T-132 | Pending Role Assignment | (ADR-045 C-05 / DESIGN-023 D-09 / R-148, PLAN-026) A **parked role intent** for an Authentik identity that has no app `users` row yet (someone who only logged into OWUI/Kavita). Assigning writes the Authentik group membership **immediately** and parks a `pending_role_assignments` row (one live row per identity), **consumed lazily on that identity's first haynesnetwork login** — the Better Auth `session.create.after` hook applies the app role + stamps the row consumed in ONE tx. The join key is **email** (case-insensitive) because the OIDC `sub` is a `hashed_user_id` the app can't pre-compute (the same email-match precedent as Plex accounts, ADR-017 C-06). | `pending_role_assignments` (migration 0036); `consumePendingRoleForUser` (`@hnet/domain`); `consumePendingRoleOnSignin` (`@hnet/auth` hook, after `bootstrapAdminOnSignin`) | Create co-writes `permission_audit` `assign_pending_role`; consume co-writes `user_role_transitions` via `assignRole`. Never throws into the auth flow — next login retries. |
| T-133 | Authentik Directory Mirror | (ADR-045 C-09 / DESIGN-023 D-10 / R-149, PLAN-026) The synced **copy of the whole Authentik directory** the `/admin/users` portal reads — one `authentik_users` row per identity (incl. external Plex-source + never-logged-in + service accounts), keyed by the Authentik user `pk`. A rebuildable **read-model** (the `ai_usage_chats` / `trash_candidates` class): the sole writer is `upsertAuthentikUsers`, so no per-row audit. Maintained by the one-way read-only **`authentik-users`** sync mode (pages `GET /api/v3/core/users/` + `.../groups/`), which writes **no `sync_runs` row** (the mirror is its trail); the portal also re-reads live after a membership write for immediacy. Never hard-deletes a vanished identity — Authentik deactivation flips `is_active`, captured next sync. | `authentik_users` (migration 0036, guard-listed); `upsertAuthentikUsers`/`syncAuthentikUsers`/`listAuthentikDirectory` (`@hnet/domain authentik-users.ts`); `AuthentikReadClient` (`@hnet/authentik`); `authentikPortal.listIdentities`/`.refresh`; `SYNC_RUN_KINDS += authentik-users` | Read-only against Authentik. `listAuthentikDirectory` LEFT-joins the mirror to app users (by `lower(email)`), roles, and live pending assignments for the roster. |
| T-134 | OWUI Group Pre-creation | (ADR-045 C-03 / DESIGN-023 D-03/D-06 / R-146, PLAN-026) The step where provisioning a Synced Tier (T-129) **also ensures the same-named Open WebUI group exists**. Open WebUI deliberately does **NOT** auto-create groups from OIDC claims (creation stays off there), so the portal must pre-create the group for the `groups`-claim sync to have a target. Issued through the confined `@hnet/openwebui/write` `createGroup` (`POST /api/v1/groups/create`); idempotent (ensure-exists); appends an `ensure_owui_group` row to `authentik_group_audit` after the apply. Reuses the PLAN-021 `OPENWEBUI_URL`/`OPENWEBUI_API_KEY` env — no new secret. | `OwuiGroupReadClient`/`OwuiWriteClient` (`@hnet/openwebui`); `provisionSyncedTier` OWUI branch (`@hnet/domain`); `authentik_group_audit.action 'ensure_owui_group'` | A separate package from the PLAN-021 OWUI **usage** client (different concerns — group management vs chat-usage ingestion; ADR-045 C-11). Per-app entitlements (model access) remain OWUI config, not written here. |
| T-135 | hnet-portal Service Account | (ADR-045 C-08 / DESIGN-023 D-17 / R-150, PLAN-026) The dedicated Authentik **service account** whose API token the portal authenticates with — **never** the shared homepage admin token. Delivered as the **cluster-created** secret `haynesnetwork-authentik-token` (the `haynesnetwork-webhook` precedent; a 1Password/External-Secrets migration is an owner-nicety TODO). If Authentik RBAC can scope it to (user read, group read, membership write) that minimal scope is used; otherwise a full token — the as-executed scope is recorded in OPS-011. Reached **in-cluster** at `authentik-server.network.svc.cluster.local` (bypasses the Cloudflare 1010 Python-UA ban, OPS-001). | `AUTHENTIK_URL`/`AUTHENTIK_API_TOKEN` (env); `assertAuthentikEnv` (`@hnet/authentik config.ts`); secret `haynesnetwork-authentik-token` (cluster) | Token is header-only (`Authorization: Bearer`) — never in a URL or error string (the `assertPlexEnv`/`assertArrEnv` discipline). |
| T-136 | Books Ledger | (ADR-046 / DESIGN-024 / R-151, PLAN-023) The app-owned, one-way SYNCED MIRROR of the two book servers — **Kavita** (Books=EBooks + Comics) and **Audiobookshelf** (Audio Books) — held in a **dedicated `books_items` table** (one row per Kavita series / ABS library item), the substrate the Library Books/Audiobooks/Comics walls read. hard rule 4 EXTENDED: Kavita/ABS are the source of truth for BOOK media; the sync flows strictly IN and there is **NO write-back** (no Fix/Restore/add for books). NOT `media_items` (ADR-046 — books have no monitored/quality/root-folder/Fix semantics; the \*arr-shaped ledger's CHECKs + NOT-NULLs + Fix/Restore/`/ledger` machinery would corrupt): a leaner honest shape keyed by `(source, external_id)` carrying title/sortTitle/author/narrator/series/year/genres/coverRef/deepLink + per-medium metrics (pages/wordCount \| duration/size) + tombstone. Rebuildable read-model (the ai_usage_chats class) — the data of record lives in Kavita/ABS, so the writer appends no audit/ledger row. | `books_items` (migration 0037, guard-listed); `syncBooks` (`@hnet/domain books.ts`, sole writer — upsert + tombstone in one tx); `BOOKS_SOURCES`/`BOOKS_MEDIA_KINDS` (`@hnet/db`) | Contrast ADR-038: ytdl-sub content is read LIVE (no ledger) because Plex is its only index + the owner ruled against a fake ledger; books DO get a ledger by the owner's explicit Q-04 ruling (Kavita/ABS are dedicated servers-of-record with stable identity). |
| T-137 | Books Sync | (ADR-046 / DESIGN-024 / R-152, PLAN-023) The **`books-sync`** mode that ingests the T-136 ledger. It pages Kavita (`POST /api/Series/all-v2` per Books/Comics library — `FilterV2Dto` field 19=Library; total in the `Pagination` header) + ABS (`GET /api/libraries/{id}/items`) READ-ONLY via the new read-only **`@hnet/books`** clients (lazy login + token cache + 401 re-auth; **no write surface** — the strongest arr/plex-write confinement: nothing to confine), NORMALIZES each row (the wire parsing + Kavita author-folder heuristic in `@hnet/sync books.ts`), and hands the snapshot to `syncBooks`, which UPSERTS on `(source, external_id)` + TOMBSTONES vanished rows — scoped to fully-synced sources so a partial run never tombstones the source it couldn't read. Like `ai-usage-sync`: standalone (no `--source`), writes NO `sync_runs` row, bounded, a neither-source-complete run is a `totalFailure`. | `fetchBooksSnapshot`/`BooksSyncBundle` (`@hnet/sync books.ts`); `KavitaClient`/`AudiobookshelfClient` (`@hnet/books/read`); `runSync` `books-sync` block; `SYNC_RUN_KINDS += books-sync`; env `KAVITA_*`/`AUDIOBOOKSHELF_*` (`assertBooksEnv`) | haynes-ops change = image bump + one `sync-books` CronJob block (mirrors `sync-ai-usage`) + `KAVITA_PASSWORD`/`AUDIOBOOKSHELF_PASSWORD` in the app ExternalSecret. Live counts 2026-07-10: 1283 Kavita book series + Comics + ~800 ABS audiobooks. |
| T-138 | Books Section | (ADR-046 C-04 / DESIGN-024 D-07 / R-155, PLAN-023) The **`books`** Section-Permission gating the three Library sub-tabs (Books/Audiobooks/Comics) INSIDE the universal Library section — Library itself has no section id; this is the visibility knob for the books walls only. Defaults to **`disabled`** (`SECTION_DEFAULT_LEVELS.books`) = ships Admin-only (an `is_admin` role implies `edit`); the owner opens a role to `read_only` via `/admin/roles` after his 390px + desktop screenshot review (the ADR-037/038 rollout). `booksProcedure = sectionProcedure('books','read_only')` gates `books.search`/`.filterFacets`; the `/api/books/cover` proxy applies the SAME check — server-authoritative (AC-13), a Disabled caller gets neither tabs, data, nor covers. | `SECTION_IDS += 'books'`; `booksProcedure`/`effectiveSectionLevel` (`@hnet/api middleware/role.ts`); `/api/books/cover` route; `books-browser.tsx`; migration 0037 (section CHECK += 'books') | The two Kavita/ABS `app_catalog` cards are a SEPARATE axis (role_app_grants, ADR-012) — seeded rows, admin-visible; owner grants Default/Family after review. Genre filter chips are a deferred follow-up (the facets endpoint ships + is unit-proven). |
| T-139 | \*arr→Plex Match | (ADR-047 / DESIGN-025 / R-157, PLAN-028) The per-item link from a Library `media_item` to the exact Plex title it corresponds to — `{plex_library, ratingKey}` — resolved by **shared GUID** (the tmdb/imdb/tvdb/musicbrainz ids BOTH the \*arr AND Plex carry: radarr `tmdb`→`imdb`, sonarr `tvdb`→`imdb`, lidarr `musicbrainz`). Stored in a **`media_plex_matches`** rebuildable derived cache (one row per `(media_item, plex_library)` — a title mirrored across libraries gets several rows; the *arrs + Plex are the sources of truth → no audit row). Produced by the **`plex-match`** sync mode: reads live `media_items` + the Plex libraries READ-ONLY (no \*arr call, no Plex write), pages each Movies/TV/Music section, indexes by GUID, matches, then UPSERTS + RECONCILES (a title dropping out of a fully-read library is removed) via `syncPlexMatches`. Reports the per-kind match rate. An UNMATCHED item (present in the \*arr, not yet in Plex) keeps no row → no deep link, and is gated by its kind's HOME library (never hidden by match failure). | `media_plex_matches` (migration 0038, guard-listed INSERT/UPDATE/DELETE); `syncPlexMatches` (`@hnet/domain plex-match.ts`, sole writer); `fetchPlexMatchSnapshot`/`parsePlexGuids` (`@hnet/sync plex-match.ts`); `SYNC_RUN_KINDS += plex-match`; `PLEX_MATCH_GUID_SOURCES` (`@hnet/db`) | `machineIdentifier` is NOT denormalized (joined off `plex_servers`). Match yield depends on GUID overlap; a low-yield kind still ships gating + the links that resolved. Contrast books/AV (existing `deep_link_url`) + ytdl-sub (live ratingKeys) — those need no match. |
| T-140 | Library Access Gate (THE INVARIANT) | (ADR-047 / DESIGN-025 / R-157, PLAN-028) The SERVER-SIDE rule that **a user must NEVER receive — in any tRPC/API payload — a Library item that lives in a Plex library their role cannot access.** `resolveLibraryAccessGate(userId)` composes the **existing ADR-024 effective-library resolver** (`effectiveAllowedLibrariesForUser` — it does NOT reinvent access) with the derived per-`(arr_kind, arr_instance)` **candidate library set** (every library that kind's matched items appear in). An item is accessible iff: **matched** → the role can access AT LEAST ONE of its libraries; **unmatched** → the role can access at least one of its kind's candidates; **admin** → always (`unrestricted`). Enforced as an EXISTS-subquery WHERE predicate (`libraryAccessWhere` — no join, so a multi-library item is not row-multiplied; + a raw variant for facets) on EVERY `media_items` read (search/detail/events/children/wanted/filterFacets/admin browse+count/export) AND the poster proxy (`isMediaItemAccessibleToUser`); ytdl-sub Peloton/YouTube gate on the same resolver by name regex (`accessibleYtdlsubLibraries`). A fully-withheld library's tab hides entirely. Direct-id fetches return NOT_FOUND for a hidden item (never reveal existence). | `resolveLibraryAccessGate`/`isMediaItemAccessible`/`libraryKindKey` (`@hnet/domain library-access.ts`); `libraryAccessWhere`/`libraryAccessConditionRaw`/`itemAccessById`/`isMediaItemAccessibleToUser`/`resolveMediaTabVisibility` (`@hnet/api library-access.ts`); `accessibleYtdlsubLibraries` (`@hnet/api ytdlsub.ts`) | A deliberate, owner-approved TIGHTENING: non-admin Library visibility now follows the role→library grant matrix (admins unrestricted). Cold-start: a kind with 0 matches has no home → deny-by-default (safe direction) until `plex-match` runs. Proof: `packages/api/__tests__/library-access.test.ts`. |
| T-142 | Season/Episode Art (Plex-matched) | (ADR-048 / DESIGN-005 D-22 + DESIGN-017 D-09 / R-158, PLAN-030) The season-POSTER icon in each Season row + the TV episode THUMBNAIL, sourced from the EXACT Plex title the ledger item matched to (T-139 `media_plex_matches` → the show ratingKey → season children [season thumbs] / a season's episode children [episode stills]) and served through the T-120 photo-transcode proxy — NOT a TMDB re-fetch (one source). TV art rides a **signed, item-scoped** `/api/library/plex-art` reference: an HMAC over `(mediaItemId, serverSlug, thumb, size)` minted by the tRPC endpoint (which already passed the T-140 per-item gate to READ the thumb) so the proxy verifies + transcodes WITHOUT re-reading Plex yet cannot be pointed at an inaccessible SIBLING title on the same server (THE INVARIANT, T-140). `ledger.plexSeasons`/`ledger.plexEpisodeArt` re-gate the item (NOT_FOUND) and degrade to `available:false` (no icons) when unmatched / inaccessible / Plex-down. Peloton season posters come from the live k8plex read (the restored PLAN-024 duration posters) via the existing `/api/ytdlsub/poster` — no signing needed (the ytdl-sub section gates as a unit). Episodes correlate to the *arr row by `(season, episode)` NUMBER (`episodeNumber` now on `ledger.children`); a numbering divergence → tinted box, never a wrong thumb. | `resolveArtMatchForItem`/`signPlexArtRef`/`verifyPlexArtRef`/`buildPlexArtUrl`/`resolvePlexArtUpstream` (`@hnet/api library-access.ts` + `library-plex-art.ts`); `ledger.plexSeasons`/`ledger.plexEpisodeArt` (`routers/ledger.ts`); `ytdlsub.detail.seasons[].posterUrl`; `/api/library/plex-art` route; `TvSeasonEpisodes` + `.season__poster` (`item-detail.tsx`, `app.css`) | No migration (derived from `media_plex_matches` + live Plex reads). Reuses the ADR-041 transcode/LRU/ETag path on the MATCHED server (server-scoped ETag/LRU key). Signing secret = `BETTER_AUTH_SECRET` (mint + verify in one Next process). Unmatched TV show → no icon (no TMDB fallback); Specials/Season-0 with no art → no icon. |
| T-141 | Play-Here Deep Link | (ADR-047 / DESIGN-025 / R-157, PLAN-028 — Q-C/Q-D + owner UX ruling 2026-07-11) The app-specific PRIMARY action(s) on a Library item's DETAIL page (the poster ALWAYS opens the detail page — never a jump-out on the wall), each with an external-jump ↗ affordance: **"Watch on Plex — <library>" (ONE per Plex library the caller's role can access — a title in several libraries gets several, gated independently) / "Watch on Plex" (ytdl-sub) / "Read in Kavita" / "Listen on Audiobookshelf"**. Plex = `https://app.plex.tv/desktop/#!/server/<machineIdentifier>/details?key=%2Flibrary%2Fmetadata%2F<ratingKey>` (`buildPlexWebDeepLink`; machineIdentifier joined off `plex_servers`), hands off to the native app where installed; books/AV reuse the existing `books_items.deep_link_url` (Kavita/ABS); ytdl-sub uses the live hayneskube ratingKey. Attached ONLY for a **PRESENT**, access-allowed item (`detail.item.play` is an empty array otherwise). Books/audiobooks/comics get a NEW lean in-app detail page (cover/title/author/series/pages-or-duration/genres/last-synced, no Fix/Force-Search) reached from the wall tiles. Rendered reflow-free (ADR-015), no new hex (tokens only). | `buildPlexWebDeepLink` (`@hnet/domain`); `resolvePlexPlayTargets`/`PlexPlayTarget[]` (`@hnet/api`); `ledger.detail.item.play` (array) + `ytdlsub.detail.show.playUrl` + `books.detail` (`BooksDetailResult`); `item-detail.tsx`/`ytdlsub-item-detail.tsx`/`books/[id]/books-detail.tsx` | Books are always present (synced from the serving app) so always carry the deep link; their gating is unchanged (the `books` section grant, T-138). |
| T-143 | Bulletin View Grant | (ADR-049 C-02 / DESIGN-012 D-09 / R-159, PLAN-027) A per-role **visibility grant for one Bulletin SUB-VIEW** (`feed` or `messages`), a row in **`role_bulletin_view_grants`** (row-per-view, composite PK, FK cascade, CHECK; guard-listed). Layered on the coarse `bulletin` Section-Permission (T-86). **RESOLUTION is default-ON**: a role with NO rows resolves to **BOTH** views ("Bulletin is for everyone", ADR-026 C-02); PRESENT rows are the exact narrowing allowlist; Admin implies both. This DIVERGES from the Message-Action grant (T-87, default-deny) because views gate VISIBILITY of a shipped-visible section, not an opt-in power. Enforced SERVER-SIDE: `communication.feed` needs `feed`, `communication.messages.*` needs `messages` — a role without a view gets FORBIDDEN (not a UI hide) and no such sub-tab. Clearing all views RE-OPENS both (to hide Bulletin, set the section Disabled). The owner's **Default** role is seeded `messages`-only (migration 0039); Family/Friends keep both. | `BULLETIN_VIEWS`/`BULLETIN_VIEW_DEFAULTS` (`@hnet/db enums`); `roleBulletinViewGrants` (schema); `setRoleBulletinViews`/`bulletinViewsForRole`/`resolveBulletinViews` (`@hnet/domain`); `hasBulletinView`/`bulletinViewProcedure` (`@hnet/api middleware/role`); `roles.setBulletinViews`; `role.bulletinViews` (session); the `/admin/roles` Feed/Messages checkboxes + the `/bulletin` sub-tab filter | Migration 0039 (new table + permission_audit `update_bulletin_views` + the Default seed). Admin immutable (ROLE_IMMUTABLE). |
| T-144 | Section Capability Map | (ADR-049 C-01 / DESIGN-004 D-18 / R-159, PLAN-027) The single source of truth (`apps/web/lib/role-sections.ts`) declaring, **per top-level section, which access levels its `/admin/roles` control distinguishes** — DERIVED from the gating code (which procedures pass `minLevel: 'edit'`), not guessed. `'tri'` → **Edit/Read-only/Disabled** (Ledger, Trash — a real `edit` rung exists); `'toggle'` → **Enabled/Disabled** (Bulletin, Metrics, ytdl-sub, Books — only ever gate `read_only`; "Enabled" persists the stored `read_only`, the `SECTION_PERMISSION_LEVELS` enum + DB values UNCHANGED). A UI-PRESENTATION construct — the server gating code stays the behaviour source of truth; a section that later gains a real Edit flips ONE map entry, no grid rewrite. | `SECTION_CONTROL`/`sectionLevelOptions`/`selectValueFor`/`isSectionEnabled` (`apps/web/lib/role-sections.ts`); consumed by `apps/web/app/(app)/admin/roles/page.tsx` | No schema change (presentation only). Mirrors the SECTION_IDS / level literals for the client bundle, like `lib/bulletin.ts` / `lib/trash.ts`. |
| T-145 | Ticket | (ADR-050 / DESIGN-012 D-10 / R-160..R-164, PLAN-034) A household **media-issue report** filed on the Bulletin **Helpdesk** (the rebranded Messages board): a required title + body, an intake **Ticket Category**, an optional linked Media Item (T-21), a **Ticket Status** driven by staff, an append-only **Ticket Event** history, and a flat **Ticket Reply** thread. HOUSEHOLD-VISIBLE (Q-01): everyone with the Bulletin `messages` view grant (T-143) sees ALL tickets. A ticket never mutates media/*arr state — the structured Fix (T-29) stays separate and is only cross-referenced (the repair cue). Purpose: media/playback problems; SITE bugs go to GitHub. | `tickets` (schema/tickets.ts); `createTicket` (`@hnet/domain tickets.ts`); `communication.tickets.*`; the `/bulletin` Helpdesk wall + `/bulletin/ticket/[id]` | Migration 0040 (also DROPS the retired `messages` board — Q-03 test data). Create = message action `post` (T-87); the display name is the `HELPDESK_NAME` constant — **RATIFIED "Tickets" 2026-07-14** (DESIGN-004 D-22; "Helpdesk" was the interim proposal), which now also drives the top-nav entry + section heading. Route/section id stay `bulletin`. |
| T-146 | Ticket Status | (ADR-050 / R-161, PLAN-034) A Ticket's lifecycle state: **`open`** (filed) ⇄ **`in_progress`** (staff picked it up — absorbs the old "Triage") → **`complete`** (TERMINAL — a recurrence is a new ticket) \| **`rejected`** (won't-do/duplicate/GitHub-bound — RE-OPENS to `open`, the hide/restore analog). The allowed edges are the exported `TICKET_TRANSITIONS` matrix, enforced by `transitionTicket` under a row lock (illegal ⇒ `TICKET_INVALID_TRANSITION` → CONFLICT, before any write). Transitions are **staff-only** — the `moderate` message action (T-87, Q-02) — and EVERY one may carry an optional household-visible note. | `TICKET_STATUSES` (`@hnet/db enums`); `TICKET_TRANSITIONS`/`canTransitionTicket`/`transitionTicket` (`@hnet/domain tickets.ts`); `communication.tickets.transition`; the wall's state filter chips + tile puck tones | Replaces the Message moderation statuses (`visible/hidden/deleted`, retired with the board). Chip tones: open=warning, in_progress=info, complete=accent, rejected=muted. |
| T-147 | Ticket Event | (ADR-050 option F / R-161, PLAN-034) One APPEND-ONLY history row on a Ticket: the creation (`from_status` NULL → `open`, actor = author — the timeline's "Filed" entry, written in the SAME tx as the ticket) or a state transition (`from → to`, actor = staff, optional household-visible `note`). Never updated or deleted; `actor_user_id` SET NULL on account deletion so history outlives accounts. The aggregate's own audit record (the BC-03/BC-04 pattern — NOT `permission_audit`). | `ticket_events` (schema/tickets.ts); written only by `createTicket`/`transitionTicket`; surfaced as the detail page's History timeline (`communication.tickets.detail.events`) | Migration 0040; guard-listed. |
| T-148 | Ticket Reply | (ADR-050 / R-162, PLAN-034 Q-02) One flat thread entry on a Ticket (GitHub-issue style — "threaded replies" means a conversation ON the ticket, not nesting): author, body, when. Open to **ANY member with the Bulletin `messages` view grant** — deliberately NOT gated on `post`/`moderate` (staff ask for info, the reporter answers, anyone chimes in). Bumps the ticket's `last_activity_at` (the wall sort key) in the same tx. Immutable v1 (no edit/delete — ADR-050 C-08). | `ticket_replies` (schema/tickets.ts); `addTicketReply` (`@hnet/domain tickets.ts`); `communication.tickets.reply`; the detail page's reply thread + composer | Migration 0040; guard-listed. Closed tickets stay open for conversation. |
| T-149 | Library View | (ADR-051 / DESIGN-026 D-01 / R-165, PLAN-029) A selectable PRESENTATION SHAPE of a Library wall — **flat** (the poster grid), **grouped** (aggregate cards, T-150), or **hierarchy** (TV's Shows → Seasons → Episodes, unchanged). Each kind tab has an opinionated R2 DEFAULT (Movies flat · TV hierarchy · Music Artists · Peloton grouped-by-Exercise · YouTube grouped-by-Channel · Books/Audiobooks grouped-by-Author · Comics grouped-by-Series); a user's choice persists per user (T-153) and is URL-synced (`?view`, a screen-level PUSH per DESIGN-004 D-19). TV is NEVER flattened. | `LibraryView` shape enum; the wall view selector; `?view=flat\|grouped\|hierarchy` URL param | Which alternative shapes a wall offers beyond its default is a build-phase UX call; the defaults are fixed (DESIGN-026 D-11). |
| T-150 | Group View (Grouping) | (ADR-051 / DESIGN-026 D-04 / R-166, PLAN-029) A Library View (T-149) that renders one AGGREGATE CARD per group key (author / series / channel / discipline) — label + item **count** + a stacked-cover motif — drilling into the same wall in flat view pre-filtered to that group (`?group=<key>`, a PUSH). Queries + view shells over the EXISTING three engines (T-151), NO new store: the group keys already exist and are live-verified populated where used (`books_items.author`/`series_name`; Plex channel/discipline tags). Aggregates + covers pass the T-140 access gate — an all-withheld group does not appear. | the group-by dimension per wall; `?group=<key>` URL segment; GROUP BY / distinct-key aggregate over the wall's engine | Group-card art density (how many covers, fan vs stack) is a deferred build UX call (DESIGN-026 D-11). Distinct from Collections (PLAN-037, out of scope). |
| T-151 | Sort/Filter Registry | (ADR-051 / DESIGN-026 D-02/D-03 / R-167, PLAN-029) The per-(wall, view-level) DECLARATION of which sort keys + filter facets a view offers, resolved against its backing engine — the app-side analogue of Plex advertising fields per libtype. Spans the **three-engine seam**: **ledger** (Movies/TV-Shows/Music — extends the DESIGN-008 D-09 `SORT_SPECS`/`buildLibraryWhere`, keyset cursor unchanged), **live-Plex** (TV Seasons/Episodes, Peloton, YouTube — over the live section read), **books** (Books/Comics/Audiobooks — extends the ADR-046 `books.search`/`books.filterFacets`). Encodes Plex-style ASYMMETRY: a level lists ONLY the dimensions it can answer (no Resolution/Duration at Season, no Runtime on Music, per-episode Air Date at Episode, a "Last Episode Added" rollup at TV Shows). Feeds ONE `@hnet/ui` filter/sort UI. Every declared facet is populated-value-gated (an empty facet renders no chip). | `LibraryViewRegistry` (per-view sorts/facets + default sort/dir); extends `LIBRARY_SORT_FIELDS`/`LIBRARY_FILTER_SHAPE` (`@hnet/api ledger-query`) + `BOOKS_SORTS`/`books.filterFacets` | The ONE seam that shares one UI across three engines without forking (ADR-051 C-01). Widening the live-Plex read for Season/Episode rating/genre/resolution is deferred per-need. |
| T-152 | Date Released (`released_at`) | (ADR-051 C-05 / DESIGN-026 D-05 / R-168, PLAN-029) The canonical per-item RELEASE / AIR / PUBLISH timestamp — a first-class sort + range facet, PEER of Date Added. Populated by sync: Radarr `digitalRelease ?? inCinemas ?? physicalRelease`; Sonarr `firstAired`; Lidarr none (artists have no release date); ABS `publishedDate` (audiobooks); live-Plex uses the already-parsed `originallyAvailableAt` (air/upload). Kavita Books/Comics have NO date in the list read → Release Date is honestly ABSENT from their registry (no fabricated column). A null `released_at` sorts NULLS-LAST like every nullable sort. Contrast **Date Added** (`arr_added_at`/Plex `addedAt`/`source_added_at` — live-verified 100%, already a sort field — a pure surfacing task, NOT a sync task). | `media_metadata.released_at` (nullable timestamptz, migration next-free at build); `SORT_SPECS.released_at`; Radarr/Sonarr adapter fields + ABS published-date fetch (`@hnet/arr`/`@hnet/sync`) | The one new sync/schema surface of PLAN-029; additive, bounded. |
| T-153 | Library Preferences | (ADR-052 / DESIGN-026 D-06 / R-169, PLAN-029) The SERVER-SIDE, PER-USER store of the last-chosen view / group-by / sort (+ direction) per Library wall (owner ruling R1 persistence + R6 remember-last-sort). Read on wall load, written on change; session-gated (a user reads/writes only their own row). **Descriptive UI state → NO audit rows** (ADR-052 C-04 — CLAUDE.md rule 6 is for domain mutations, not a sort choice). **URL OVERRIDES for shared links:** an explicit `?view`/`?group`/`?sort` param WINS over the stored default and is NOT written back; a bare URL is filled from the store, falling back to the R2/R6 default. NOT localStorage / cookie (owner ruled server-side, for cross-device parity). | `library_preferences` (per `(user_id, wall)`, migration next-free at build; guard-listed, single-writer); `library.preferences.get`/`set` | The first per-user store in the app (none existed — live-verified). Coordinated with the T-154 map (both new per-user tables). |
| T-154 | Plex Account Map | (ADR-053 / DESIGN-026 D-07 / R-170, PLAN-029) The app-user↔account ATTRIBUTION SEAM for per-user watch/read-state: a per-user table carrying per-source handles — plex.tv NUMERIC id (auto-filled from the existing `resolvePlexIdentity` claim + the `packages/plex` friend matchers), ABS user id, Kavita username (admin-set). REUSES the built identity model (does NOT reinvent it) and mirrors the `users.plex_email`/`plex_username` override pattern. **The Feed-attribution backlog item reuses this seam verbatim** (owner's explicit intent). The map never widens access — it only attributes activity on already-gated content (T-140). Handle entry is admin-only. | the mapping table (per app `user_id`; migration next-free at build; guard-listed, `@hnet/domain` single-writer + resolution helpers) | Auto-populated for video; admin-filled for the book servers. New users invisible until mapped (the accepted cost, ADR-053 C-04). |
| T-155 | Per-User Watch/Read State | (ADR-053 / DESIGN-026 D-07 / R-170, PLAN-029) The per-user watched / in-progress (video) + read / in-progress (audiobooks) signal that backs the registry's per-user facets — resolved through the T-154 map. **Video:** the Tautulli history subset gains `user_id`; the harvest attributes per-user watch state ADDITIVELY into a per-user read-model, leaving the household `play_count`/`last_viewed_at`/`last_watched_*` (the Trash walls depend on them) UNTOUCHED. **Audiobooks:** ABS admin-token reads any user's `mediaProgress[]` via the map (join `books_items.external_id` = ABS libraryItemId). **Kavita read-state DEFERRED** (no admin per-user progress read — needs per-user tokens / OIDC-linked Kavita). Coverage is thin (live-verified household watch: radarr 3.8% / sonarr 18.8% / music 0%) → facets are populated-value-gated. | Tautulli `user_id` subset add + per-user watch read-model; ABS `GET /api/users/{id}` `mediaProgress` in a bounded sync mode (the `ai-usage-sync` shape); the registry's Watched/In-progress/Read facets | Additive, never a replacement (ADR-053 C-03). A per-user RATING dimension (`userRating`) is out of R7 (not harvested). |
| T-156 | MAM Compliance Governor | (ADR-054 / DESIGN-027 / R-172..R-177, PLAN-039) The ~15-min `@hnet/sync` **`mam-governor`** mode that keeps automated MyAnonaMouse (MAM) grabs under the account's unsatisfied-torrent cap. Each run it counts unsatisfied torrents locally (T-157), decides the gate (`unsatisfied ≥ limit − buffer` ⇒ pause), idempotently actuates the MAM Gate (T-158), then in ONE tx upserts `mam_gate_state` and — on a gate transition or T-160 — enqueues a `notification_outbox` row (ADR-034 C-01). Fail-closed (a failed count ⇒ paused). First sight records a baseline and pages nothing. Adds ZERO MAM API surface (counting is qB-local, gating is Prowlarr-local; neither calls MAM). Mirrors the smart-alerts shape (T-101 class). | `evaluateMamGovernor`/`resolveGovernorConfig`/`mamGovernorBundleFromEnv` (`@hnet/domain mam-governor.ts`/`mam-clients.ts`); `mam_gate_state` (migration 0041, guard-listed INSERT/UPDATE); `SYNC_RUN_KINDS += mam-governor`; the `sync-mam-governor` CronJob | v1 visibility = logs + Pushover only (R-177); a Metrics tile is a PLAN-040 companion. |
| T-157 | Unsatisfied Torrent (Local Count) | (ADR-054 / DESIGN-027 D-02 / R-173, PLAN-039) A `books-mam` qBittorrent torrent that has NOT yet met MAM's 72h seed obligation — counted **locally** as: still-downloading (`progress < 1`) OR complete-but-`seeding_time < 72h` (`MAM_SEED_OBLIGATION_SECONDS = 259_200`). `unsatisfied = downloading + seedingUnder72`. Conservative (owner ruling Q-01): a missing/garbage field defaults to the unsatisfied side, so a wire hiccup can only OVER-count (close the gate earlier). qB `seeding_time` (accumulated active seed time) is the direct, conservative proxy for the 72h obligation. NO MAM call, NO qB credential (the WebAPI answers unauthenticated cross-namespace). | `computeUnsatisfied`/`QbittorrentClient.countUnsatisfied` (`@hnet/downloads/read`); `UnsatisfiedCounts` | Live 2026-07-11: 13 complete, all under 72h → 13 unsatisfied (the acceptance fixture). |
| T-158 | MAM Gate (Prowlarr indexer seam) | (ADR-054 C-01 / DESIGN-027 D-04 / R-174, PLAN-039) The pause/resume actuator: the MyAnonaMouse **Prowlarr indexer's `enable` flag** (a GET-then-PUT of the full `/api/v1/indexer/{id}` object changing ONLY `enable`). Prowlarr's LazyLibrarian application (`syncLevel=fullSync`) PROPAGATES the flag to LL's `Torznab_0`, so LL stops/starts querying MAM — verified live (within ~6s LL flips) — with NO Torznab error, so LL's provider-failure blocklist is never tripped. Chosen over the LL-side toggle because Prowlarr's fullSync CLOBBERS a manual LL edit (the LL seam is not durable; the Prowlarr flag is). The write client `@hnet/downloads/write` is import-confined to `packages/domain` (the arr-write guard, extended). | `ProwlarrReadClient.getIndexerEnabled`/`ProwlarrWriteClient.setIndexerEnabled` (`@hnet/downloads`); `PROWLARR_API_KEY` (media-stack) | The one durable seam; blast radius = the MAM indexer. Indexer priority pinned 50 (→ LL dlpriority 1) to keep usenet strictly first. |
| T-159 | Governor Config Seam | (ADR-054 C-07 / DESIGN-027 D-07, PLAN-039/PLAN-040) The SINGLE resolution point for the governor's tuning — `resolveGovernorConfig()` — returning `{ limit, buffer, zeroHeadroomAlertHours }`, called ONCE per run. v1 env-backed (`MAM_UNSATISFIED_LIMIT` 20, `MAM_UNSATISFIED_BUFFER` 5, `MAM_ZERO_HEADROOM_ALERT_HOURS` 48); `buffer` clamped `< limit` so the gate can never wedge permanently closed. The owner manually bumps the limit at each MAM rank promotion. **PLAN-040** adds an audited DB-backed `app_setting` override behind the SAME async call — no rework of the mode. | `resolveGovernorConfig` (`@hnet/domain mam-governor.ts`); `MamGovernorTuning` | The seam that lets PLAN-040 add a DB override + governor-state visibility without touching the mode (owner ruling 2026-07-11). |
| T-160 | Zero-Headroom Stuck Alert | (ADR-054 / DESIGN-027 D-05 / R-176, PLAN-039) The `mam_gate_stuck` Pushover fired ONCE when `headroom (= limit − unsatisfied)` has been pinned at 0 for more than `zeroHeadroomAlertHours` (48h) — i.e. book demand far exceeds the ~limit-per-72h throughput, so the owner may want to prioritise the wanted list or push a rank bump. Tracked by `mam_gate_state.zero_headroom_since` (starts at the first at-cap run, clears when headroom returns) + `pinned_alerted_at` (dedupes within one stuck episode). Distinct from a gate transition (which is about crossing the `limit − buffer` threshold; this is about the HARD cap). | `computeStuck` (`@hnet/domain mam-governor.ts`); `mam_gate_state.zero_headroom_since`/`pinned_alerted_at`; `NOTIFY_OUTBOX_EVENT_TYPES += mam_gate_stuck` | A failed count carries the timer untouched (can't assess headroom). |
| T-161 | Integration | (ADR-055 / DESIGN-028 / R-178, PLAN-044) A PER-USER link between an app user and an external account (`user_integrations`, one row per (user, provider); v1 provider `'goodreads'`). Linking uses a PUBLIC profile reference — a profile URL, a **vanity URL** (resolved server-side to the numeric id by following the redirect), or a bare numeric id — with NO OAuth and NO secret; the shelves must be PUBLIC. link/unlink co-write a `permission_audit` row (`link_integration`/`unlink_integration`); the sync marker (`markIntegrationSynced`) is unaudited. The framework phase (PLAN-043) generalizes to a provider registry. | `linkIntegration`/`unlinkIntegration`/`listLinkedIntegrations` (`@hnet/domain user-integrations.ts`); `user_integrations` (migration 0045, guard-listed); `INTEGRATION_PROVIDERS`/`INTEGRATION_STATUSES`; the `integrations` Section Permission | Ships Admin-only (`integrations` default `disabled`); owner links first, opens per role after review (R-184). |
| T-162 | Shelf Sync | (ADR-055 / DESIGN-028 / R-179, PLAN-044) The read-only `goodreads-sync` `@hnet/sync` mode: for each LINKED integration it pages the public shelf RSS, GB-enriches each item (volume id + comic classification, mandatory retry/backoff), mirrors the shelf into `integration_shelf_items` (upsert + scoped-tombstone, the `books_items` read-model class — no audit), and hands the enriched snapshot to the domain orchestrator. Standalone mode (no `--source`, no `sync_runs` row). NEVER writes LazyLibrarian provider config (Prowlarr fullSync owns it — OPS-013). | `runGoodreadsSync` (`@hnet/sync goodreads.ts`) → `syncGoodreadsIntegration` (`@hnet/domain goodreads-sync.ts`); `upsertShelfItems`; `@hnet/goodreads` (RSS + Google Books); `SYNC_RUN_KINDS += goodreads-sync` | Per-integration isolation — a private/unreachable shelf marks THAT integration `error` and continues. Goodreads is pull-only (we ingest, never push). |
| T-163 | Book Request | (ADR-055 / DESIGN-028 / R-180, PLAN-044) The request/Missing ledger row (`book_requests`, one per shelf want) tracking BOTH formats (`ebook_status`/`audio_status` ∈ requested\|wanted\|grabbed\|landed\|missing), the library match (`matched_books_item_id`, nullable), and the LazyLibrarian book id (`ll_book_id` = the GB volume id). A routable-unmatched want is pushed to LL BOTH formats, PACED: `addBook → queueBook (MANDATORY — addBook alone lands Skipped) → searchBook`; LL statuses reconcile back (`mapLlStatus`, never regressing a positive). ADR-046 STANDS — request state lives HERE, never on the `books_items` mirror. The confined LL write surface `@hnet/lazylibrarian/write` is domain-only (the arr-write guard, extended). | `syncShelfRequests`/`markRequestPushed`/`applyRequestReconcile`/`runManualBookSearch` (`@hnet/domain book-requests.ts`/`goodreads-sync.ts`); `@hnet/lazylibrarian` (read + confined write); `book_requests` (migration 0045); `BOOK_REQUEST_STATUSES` | A manual "Search again" on a Missing request is audited (`request_book_search`) then fires a real LL `searchBook` (T-165). |
| T-164 | Missing (books) / Coverage | (ADR-055 / DESIGN-028 / R-181, PLAN-044) The *arr wanted/missing idiom for books: a shelf want not on disk is a per-format **Missing** entry. **Coverage %** = "we have N% of your shelf" = (requests with a library match OR either format landed) / (live shelf wants), rounded — the Integrations tab's headline stat. Comics count for coverage but never route to LazyLibrarian (T-165). Library matching is normalized title (+author) against `books_items` — an honest MVP limitation (the mirror has no ISBN column; a residual). | `computeCoverage`/`loadLibraryMatcher`/`getBookRequestsForIntegration` (`@hnet/domain book-requests.ts`); `integrations.shelf`/`.requests`/`.search` (`@hnet/api`) | Coverage is over the synced WANT shelf; read/currently-reading + cross-provider coverage are later saga phases (point 3). |
| T-165 | Comic (classification) | (ADR-055 / DESIGN-028 / R-183, PLAN-044; **routing added by ADR-056 / PLAN-046 — T-166**) A shelf want classified as a comic / graphic novel — via the Google Books category "Comics & Graphic Novels" or a matched library row of kind `comic`. Comics are Kapowarr's domain, NOT LazyLibrarian's (owner note 2026-07-13): a comic is NEVER pushed to LL. As of ADR-056 a comic ROUTES to Kapowarr (T-166) rather than merely parking; it stays parked (`unroutable_reason='comic'`, `comic_status='requested'`) only when Kapowarr is unreachable / has no ComicVine match. Classification unreliable-from-RSS-alone is acceptable — absent a GB match the item defaults to the LL route with the caveat noted. | `isComicCategory` (`@hnet/goodreads`); `book_requests.comic_status`/`unroutable_reason` | Classification is the discriminator; the ACQUISITION is T-166. |
| T-166 | Comic Acquisition (Kapowarr) | (ADR-056 / R-185..R-187, PLAN-046) The comics-source leg of the Book Request ledger: a comic-classified want (T-165) resolves to a **ComicVine volume** via Kapowarr's own search (`pickBestVolume` — rank by shared title tokens, prefer the ORIGINAL edition `translated=false`), is ADDED **monitored** (the *arr "Wanted") to Kapowarr, and its per-volume state reconciles back into `book_requests.comic_status` (∈ requested\|wanted\|grabbed\|landed\|missing; `kapowarr_volume_id` the reconcile + force-search key; `comic_status IS NOT NULL` is the durable "is a comic" signal). Kapowarr acquires from ITS OWN GetComics DDL sources — NEVER MAM/qBittorrent/Prowlarr/the governor (C-06). The force-search (`integrations.search`) DISPATCHES: a comic → Kapowarr's `auto_search` task; a book/audiobook → LL `searchBook` — both audited `request_book_search`. Absent `KAPOWARR_API_KEY` / no ComicVine match ⇒ parked (honest degradation). | `pickBestVolume`/`mapKapowarrVolumeStatus`/`markComicRouted`/`applyComicReconcile`/`runComicVolumeSearch` (`@hnet/domain book-requests.ts`/`goodreads-sync.ts`); `@hnet/kapowarr` (read + confined `./write`); `book_requests.comic_status`/`kapowarr_volume_id`/`comicvine_id` (migration 0046); `BOOK_REQUEST_FORMATS += comic` | Kapowarr monitors at VOLUME (series) granularity; the ComicVine title-resolve is best-effort (a residual). Confined write surface `@hnet/kapowarr/write` domain-only (the arr-write guard, extended). |
| T-167 | Integrations Hub / Provider Sub-Section | (ADR-057 / DESIGN-029 / R-189, PLAN-045) `/integrations` is a HUB of provider CARDS (the Trash-Overview whole-card-button idiom); each provider gets a SUB-SECTION (`/integrations/goodreads`) a card PUSHES into (D-19), navigated by `?tab=` (the Metrics/Trash precedent): **Overview** — the stats page (link card, the WANT-SHELF headline coverage per Q-02, per-shelf breakdown cards, request-PHASE summary tiles) — and **Items** — a REAL library wall over every synced shelf (`MediaPoster` grid, cover-proxy art where matched, designed fallback tiles, corner-puck state badges, the shared filter/sort chrome). The v0.49.0 flat Requests & Missing wall folded in here. ALL FOUR shelves sync AND acquire (`GOODREADS_SHELVES`, migration 0047 — the owner's A1-overruled ruling); an absent CUSTOM shelf 404 reads as EMPTY (A3, `isAbsentCustomShelfError`). | `integrations.overview`/`.items` (`@hnet/api`); `computeShelfStats`/`getShelfWallItems`/`requestPhase` (`@hnet/domain book-requests.ts`); `apps/web/app/(app)/integrations/**` | The hub scales to the saga's future providers as sibling cards. |
| T-168 | Shelf Chips | (ADR-057 / DESIGN-029 D-06 / R-190, PLAN-045) The Goodreads items wall's shelf filter — the Helpdesk ticket state-chip SEMANTICS ported verbatim (DESIGN-012 D-12): multi-select additive-OR toggles (the visible set is the union), a SUPERSET "All" that lights when every chip is on, counts always visible, `aria-pressed`, recolor-only (ADR-015); URL = repeated `?shelf=` params via replace, canonical default (ALL populated shelves) writes NO param, deliberate-empty writes the `shelf=none` sentinel; chips populated-value-gated (an absent/empty shelf grows no chip — A3/ADR-051 C-06). | `shelfSelectionFromParams`/`shelfParamsForSelection`/`toggleShelf` (`apps/web/lib/goodreads-shelf-wall.ts`) | The unit tests mirror the Helpdesk chip spec. |
| T-169 | Library-Wanted (books) | (ADR-057 / DESIGN-029 D-09 / R-191, PLAN-045) Household **Wanted** tiles on the Books/Audiobooks/Comics walls, COMPOSED from the `book_requests` ledger (never mirror rows — ADR-046): format decides the wall (ebook ⇒ Books, audiobook ⇒ Audiobooks, comic ⇒ Comics); a request composes while that format hasn't landed and the want is unmatched; deduped per distinct book with requesters aggregated. Rendered as a clearly-badged Wanted STRIP above the wall grid + the registry-declared `?wanted=1` state filter (ADR-051 row, value-gated). Books-section-gated (Q-01 — household visibility); per-viewer `canSearch`/`canOpenRequest` computed server-side (ownership + the `integrations` section). Tiles carry the FORCE-SEARCH button (the dispatching `integrations.search`, T-166) with PLAN-015-style reserved-slot feedback, and deep-link to the request's sub-section context (`?focus=`). | `books.wanted` (`@hnet/api`); `getWantedBookRequests`/`isRequestSearchable` (`@hnet/domain book-requests.ts`); `WantedStrip`/`RequestSearchButton` (`apps/web`) | The *arr monitored-but-missing idiom for books; synthetic tiles never fake covers and hide under facets they can't answer. |

## Activity & in-flight

| ID   | Term | Definition | Code identifier | Notes |
| ---- | ---- | ---------- | --------------- | ----- |
| T-170 | Activity | (ADR-059 / DESIGN-030 / R-192, PLAN-048) The cross-library Library sub-tab (the Trash→Activity idiom) showing every media item actively in the acquisition pipeline — grabbing/searching, downloading (with progress), importing, import-FAILED, or just-completed — across all source families, filtered by Helpdesk-idiom stage + kind chips with server counts. Read LIVE per source (ADR-059 Q-01: live poll-through + short in-process cache, NOT a synced snapshot — queues change by the second), gated on the LIST resolver by each item's `section` (a `book` item shows only when `books ≥ read_only`; the future *arr items ride the universal walls). | `activity.list` (`@hnet/api`); `ActivityItem`/`ActivitySourceAdapter`/`aggregateActivity` (`@hnet/domain activity`); `apps/web/app/(app)/library/activity-panel.tsx` + `ActivityCard` (`@/components/cards`) | The always-on Library sub-tab (My Fixes precedent); source-agnostic — the *arr + Kapowarr adapters fill the same contract (DESIGN-030 D-08). |
| T-171 | In-Flight | (ADR-059 / DESIGN-030 D-03 / R-193, PLAN-048) The pipeline STAGE of an activity item — `searching`/`downloading`(+%)/`importing`/`failed`/`completed` (`ActivityStage`) — surfaced on a wall poster as the leading badge in the ADR-058 caption badge row via the typed `inFlight` prop on `MediaCard`/`BookCard` (NOT a new poster anatomy — pucks stay reserved to Trash/Ticket; tokens-only, recolor-not-reflow per ADR-015). A stage change recolors the badge in place; when the item lands the badge clears on the next live poll. | `ActivityStage`/`activityStageBadge` (`@hnet/domain activity` / `@/components/cards`); `MediaCard`/`BookCard` `inFlight` prop | Books walls consume it in SLICE 1; the *arr walls consume the same prop when their adapter lands. |
| T-172 | Import Failure | (ADR-059 / DESIGN-030 D-04/D-07 / R-194, PLAN-048) A stuck acquisition surfaced as a durable, actionable failure — classed `stranded_import` (download completed but never imported — the OPS-013 §11 42-download incident), `postprocess_failed` (LazyLibrarian `Failed`/`DLResult`), `download_failed` (SAB dead nzb / par2), or `import_blocked` (`ActivityFailureKind`). Persisted in `activity_import_failures` (the ONLY persisted activity state — ADR-059) by the `activity-scan` sync mode, which enqueues one `activity_import_failed` `notification_outbox` row per NEW failure in the same transaction (ADR-034 C-01). Each has a failure DETAIL page (the #264 wanted-detail idiom) with the reason + role-controlled actions (R2): Admin (or a granted role) can Retry import / Force re-search / open downstream; others read. | `activity.failure`/`.retryImport`/`.forceSearch` (`@hnet/api`); `activity_import_failures` (migration 0048); `evaluateActivityFailures` + `activityActionProcedure` + `role_activity_action_grants` (`@hnet/domain`/`@hnet/db`) | Actions ride the ADR-023 grant machinery (openable to roles later); the LL confined writes (`forceProcess`/`searchBook`) fire after the audit commit. |
| T-173 | Email Channel | (ADR-060 / DESIGN-031 / R-195..R-197, PLAN-035) The second `notification_outbox` delivery channel (`channel='email'`): rows carry their resolved recipient in `payload.to` (resolved at ENQUEUE time, same-tx — ADR-060 C-02), render via `renderOutboxEmail` (plain text, the Pushover-renderer idiom), and deliver over nodemailer SMTP submission using the F-04 `SMTP_*` contract. Per-channel disabled-safe: no credentials ⇒ the channel's rows WAIT (no attempts). | `NOTIFY_OUTBOX_CHANNELS` (`@hnet/db` enums, migration 0049); `renderOutboxEmail` + `smtpSenderFromEnv` + per-channel `deliverOutbox` routing (`@hnet/domain` notify-outbox) | Beachhead for estate email (ADR-060 C-07); trash/digest emails are follow-ups. |
| T-174 | Notification Preference | (ADR-060 C-05 / DESIGN-031 D-01 / R-196, PLAN-035) A user's own notification opt-ins — first field `email_ticket_updates` (default OFF): email me when MY tickets get replies or status changes (never for my own action). Descriptive per-user state — single-writer, NO audit row (the `library_preferences` precedent, hard rule 6 exempt). | `notification_preferences` (migration 0049); `setNotificationPreference` (`@hnet/domain`); `profile.notificationPreference`/`.setNotificationPreference` (`@hnet/api`) | Room for sibling columns if Q-01 granularity is ever revisited. |
| T-175 | Ticket Locator | (ADR-061 / DESIGN-032 / R-199..R-201, PLAN-038) The nullable qualifier on a ticket's media link naming the EXACT target inside the title's hierarchy: `target_kind` (`season|episode|album|track`; NULL = the whole title), the *arr `target_child_id`, the human `target_season`/`target_episode`, and the SNAPSHOTTED `target_label` ("S06E02 · Rich") so the ticket renders forever without a live child read (ADR-061 C-01/C-06). Filed via the compose drill under the leaf-or-scope rule (R-200). | `tickets.target_*` (migration 0051); `CreateTicketInput.target` (`@hnet/domain`); `ledger.albumTracks` + lidarr `listTracks` (`@hnet/api`/`@hnet/arr`) | ytdlsub/books targets deferred (DESIGN-032 Q-04 — foreign id spaces). |

## Changelog

| Date       | Author     | Change                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ---------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-07-03 | Tom Haynes | Initial glossary seeded from PRD-001 (Accepted). T-01..T-40 assigned.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 2026-07-03 | Tom Haynes | DESIGN-005: added T-41 Tombstone, T-42 Sync Cursor, T-43 Fix Lifecycle; amended prescriptive identifiers on T-24..T-27, T-30, T-34 to the DESIGN-001 D-15 reserved names and snake_case enum convention.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 2026-07-03 | Tom Haynes | DESIGN-005 Fix-flow UX pass: added T-44 Force Search (search-only action for missing content, `search_requested` Ledger Event; migration 0004).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 2026-07-03 | Tom Haynes | DESIGN-005 media-hierarchy actions: added T-45 Action Scope (episode/season/show/album/artist/item roll-ups; `fix_requests.target_scope`+`target_season`, migration 0006; SeasonSearch/ArtistSearch).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 2026-07-04 | Tom Haynes | Reconciled code identifiers to shipped schema: T-02/03/04 Role literals are capitalized `Member`/`Admin` (`enums.ts` ROLES); T-09/10 table is `app_catalog`, flag `default_visible`; T-13 `user_app_grants`/`tag_app_grants`; T-15 `tag_app_grants`, `tags.is_family`; T-16 `EffectiveApp`+`effectiveAppsForUser()` over the `effective_app_grants` view; T-39 names the generic `permission_audit` table alongside `user_role_transitions`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 2026-07-05 | Tom Haynes | ADR-012 unified Role model: Role (T-02) is now a DB-backed admin-managed row (`roles`, `users.role_id`), one per user; Member (T-03) and Tag (T-14) superseded; Admin (T-04) is the superuser role, Family (T-05) an example admin-created role; Default-Visible (T-10), App Grant (T-13), Permission Bundle (T-15), Effective Permissions (T-16) re-expressed onto `role_app_grants` / `effectiveAppsForUser` (view dropped); T-20/T-21 family gating attaches to a role; T-39 new action set + `role_id`. Added T-46 Role App Grant, T-47 System Role, T-48 Default.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 2026-07-05 | Tom Haynes | ADR-013 arbitrary catalog URLs: R-14 host allow-list reversed. T-09 App URL is now any normalized `http(s)` URL (no `*.haynesnetwork.com`/`*.haynesops.com` rule; DB CHECK relaxed to scheme-only `app_catalog_url_scheme`, migration 0008); T-11 Tile links to "its configured URL". Domain `ForbiddenHostError`→`InvalidCatalogUrlError` / `CATALOG_URL_INVALID`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 2026-07-05 | Tom Haynes | ADR-012 shipped-after refinements: added T-49 All-Apps Role (`roles.grants_all` — every app, incl. future ones, without admin access; effective apps = all when `is_admin OR grants_all`); Default (T-10/T-48) now seeds four apps (adds PlexOps); Family (T-05) is now seeded by migration 0007 as a normal role granting every app except Tautulli; T-16 references `grants_all`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 2026-07-06 | Fable 5    | ADR-021/022 + DESIGN-009 Ledger section (PLAN-005): added T-64 Ledger Section, T-65 Section Permission (`role_section_permissions`, migration 0013; Edit/Read-Only/Disabled; `sectionProcedure`), T-66 Fileless Set (filter state, not an import — reframes PLAN-005's draft fileless-backlog-import placeholder). Amended T-35 Restore (now the failsafe special case of the generalized `executeArrAdd`; `restore_runs.reason`, migration 0014) and T-58 Filter Engine (Ledger reuses the shared browse DSL). Backend vertical shipped; the `/ledger` page + `/admin/roles` section editor land as a follow-up UX change.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 2026-07-06 | Fable 5    | ADR-016 Subtitle Fix via Bazarr: added T-50 Bazarr (subtitle manager for the Radarr/Sonarr estate) + T-51 Subtitle Fix (FixPath `bazarr_subtitle`, migration 0009); amended T-30 to record the per-kind offer rule (`missing_subtitles` offered for Sonarr/Radarr only, not Music).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 2026-07-06 | Fable 5    | ADR-017 / DESIGN-007 Plex library self-service (built): finalized T-17..T-21 in place — Plex Server (`plex_servers`, owner slugs, seeded migration 0010), Plex Library (identity `(server_id, section_key)`, Q-03 resolved, soft-state `available`), Library Grant (redefined Role→Library `role_library_grants`; `library_grants`/`tag_libraries` retired), Allowed Library Set (`effectiveAllowedLibrariesForUser`, no `grants_all` short-circuit), Family-Only Library (a Family-role grant, no `is_family_only` column). Reconciled T-15 to `role_library_grants`. Added T-52 Plex Share, T-53 Library Registry Refresh, T-54 Machine Identifier.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 2026-07-06 | Fable 5    | ADR-020 keyless image signing: added T-59 Keyless Signing, T-60 Fulcio, T-61 Rekor (transparency log), T-62 Attestor/Subject (Kyverno), T-63 Admission Verification — pure ops/supply-chain vocabulary, no new bounded context.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 2026-07-06 | Fable 5    | DESIGN-007 D-12 (live-validated fix): amended T-53 Library Registry Refresh to be **per-server, not all-or-nothing** — each server refreshes in its own tx and a Plex failure is folded into a per-server summary (`{ ok, servers: [...] }`) instead of aborting the request. Also corrected the haynestower registry `base_url` to its external ingress (`https://plex.haynesnetwork.com` — no in-cluster Service; migration 0011) and added `PlexNetworkError` so network failures stay inside the `PlexError` taxonomy (no bare `fetch failed` leak).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 2026-07-06 | Fable 5    | ADR-018/019 + DESIGN-008 Library metadata enrichment: added T-55 Media Metadata (`media_metadata` 1:1 sibling, migration 0012, multi-source harvest with per-source degradation), T-56 Poster Proxy (authed streaming route, no storage), T-57 Watch Stats (cross-server Tautulli SUM/MAX), T-58 Filter Engine (shared filter/sort primitives + the `ledger.search` NULLS-LAST keyset contract). Backend vertical shipped; the `@hnet/ui` filter-engine port (D-10) + `/library` grid (D-11) land as a follow-up UX change on the same branch.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 2026-07-06 | Fable 5    | ADR-024 role-scoped all-libraries Plex self-service (backend; live-validated): added T-67 Server All-Grant (`role_plex_server_all_grants`, migration 0015). Amended T-20 Allowed Library Set (now unions explicit grants + all-of-all-granted-servers) and T-52 Plex Share (a share can be all-libraries; two states self-toggled; per-section add/remove refused while all with `PLEX_ALL_STATE` — supersedes ADR-017 C-14, no silent demotion). New share events `share_all_enabled`/`share_all_disabled`; `setServerAllShare`/`plex.setServerAll` + `plex.myLibraries` `allGranted`/`allActive`. The My Plex all-toggle UI + role matrix editor land as a follow-up UX change.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 2026-07-06 | Fable 5    | ADR-023 / DESIGN-010 Trash section (PLAN-006): added T-68 Trash Section, T-69 Deletion Candidate, T-70 Exclusion/Whitelist/Save (+ the cross-server watch guardian), T-71 Recently Deleted, T-72 Trash Action Grant (`role_trash_action_grants`, migration 0016; per-action fine-grained grants + `SessionRole.trashActions`/`trashActionProcedure`), T-73 Expedite, T-74 Maintainerr Safety Audit. (Numbered T-68..T-74: ADR-024 landed first on main and holds T-67.) Amended T-65 Section Permission (for Trash it gates VIEW; writes are T-72). Resolves DDD-002 BC-03 Q-04. Backend vertical shipped; the Trash UX lands as a follow-up.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 2026-07-07 | Fable 5    | ADR-025 / DESIGN-011 Trash CURATION PIPELINE (PLAN-012): added T-75 Curation Batch (`trash_batches`/`trash_batch_items`, migration 0017; state machine + poster review), T-76 Leaving Soon (Plex-visible manual Maintainerr collection, v3.17.0 `POST /api/collections`), T-77 Save Window (`window_days`; `save_leaving_soon` grant; `trash_batch_saves` tuning dataset), T-78 Gate Skip (audited `gate_skipped` escape), T-79 Deletion Snapshot (`trash_batch_items.deleted_*`, PLAN-013 source), T-80 App Setting (`app_settings` generic audited key/value store, reused by PLAN-010/013/014). Amended T-72 Trash Action Grant (grows `save_leaving_soon` + `manage_batches`). New `trash-batch-sweep` sync mode; `trash_batch_transition` ledger event. Backend vertical shipped; the poster-wall UX lands as a Fable follow-up.                                                                                                                                                                                                                                                                                                                            |
| 2026-07-07 | Fable 5    | ADR-027 / DESIGN-004 D-15 Dashboard Message-of-the-Day (PLAN-010): added T-89 MOTD — the optional admin-set dashboard banner (severity-coded, optionally time-windowed, per-user dismissible). REUSES the App Setting store (T-80) under key `motd` rather than a bespoke table (Open decision #1 resolved to reuse; migration 0019 relaxes the `app_settings.key` CHECK). Written by `setMotd`/`clearMotd` → `setAppSetting` (audited `update_app_setting` same-tx); read via `getActiveMotd` (enabled + time-window). No new token/hex (reuses `--color-info`/`--color-warning` via `color-mix`), no new audit action, no new e2e stub. PRD R-105.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 2026-07-07 | Fable 5    | ADR-026 / DESIGN-012 Bulletin — Feed + Messages (PLAN-009, backend built): added T-81 Bulletin Section (`SECTION_IDS += 'bulletin'`, default read_only; migration 0018), T-82 Notification (widened `notifications` store), T-83 Notification Source (`maintainerr`/`seerr`/`tautulli`; Overseerr folds into `seerr`), T-84 Feed (`communication.feed`, keyset), T-85 Message (`messages`), T-86 Message Status (`visible`/`hidden`/`deleted`, soft), T-87 Message Action Grant (`role_message_action_grants`; `post`/`moderate`; `SessionRole.messageActions`/`messageActionProcedure`; `update_message_actions` audit), T-88 Webhook Receiver (`POST /api/webhooks/<source>`, per-source secret). New BC-05 Media Communication (DDD-002). Backend vertical shipped; the Feed/Messages UX lands as a Fable follow-up.                                                                                                                                                                                                                                                                                                                                          |
| 2026-07-07 | Fable 5    | ADR-028 / DESIGN-005 D-20/D-21 Live *arr action feedback (PLAN-015, backend built): added T-90 Action Progress Phase (the derived nine-phase projection, `computeFixProgress`/`computeSearchProgress`, `fix.progress`/`fix.searchProgress`), T-91 Download Queue (`getQueue()` read client + `queueRecordBaseSchema`; server-side parent filter verified live 2026-07-07), T-92 Found-Nothing/Stalled (the 15m/45m never-stuck terminals), T-93 Action Feedback (the status-back-up contract, R-106/R-107). Amended T-43 Fix Lifecycle + T-44 Force Search (the stored status is unchanged; the live phase is a DERIVED read-only projection — no migration, no enum growth). Backend vertical shipped; the progress UI (chips/bars, live My Fixes) lands as a Fable follow-up.                                                                                                                                                                                                                                                                                                                                                                                  |
| 2026-07-07 | Fable 5    | ADR-029 / DESIGN-007 D-14 Plex server-owner recognition (fix): added T-94 Server Owner (Plex) — the token account owns every library and is never in its own friend list, so `plex.myLibraries` recognizes it (plex.tv `GET /api/v2/user`, `getOwnerAccount`/`getOwnerEmail`, new `plexAccountSchema`) and returns an `owner` state instead of the wrong "not a friend" error. Amended T-52's friend precondition (owner exempt). Read-only; owner lookup failure degrades to the friend flow. Live-diagnosed on staging v0.15.0 (owner `manofoz@gmail.com` owns all three servers, absent from all friend lists).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 2026-07-07 | Fable 5    | DESIGN-002 D-15 RP-initiated logout (fix): added T-98 Sign Out (RP-initiated logout) — Sign out now ends the upstream Authentik SSO session too, not just the app Session (T-07), so the next Log In shows the login page instead of silently re-authenticating (owner-reported bug). `GET /api/auth/logout` redirects through the issuer's OIDC `end_session_endpoint` (`resolveSignOutRedirect`/`buildEndSessionUrl`, `@hnet/auth`); degrades to a local `/login` when the issuer advertises no `end_session_endpoint` (stub OIDC). Authentik provider gained two `logout`-type redirect URIs (OPS-001). No migration/enum change.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 2026-07-07 | Fable 5    | ADR-030 / DESIGN-013 / OPS-007 Disk-utilization + reclaim metrics (PLAN-013, backend + Grafana built): added T-95 Space Target (`app_settings.space_targets`, migration 0021; `storage.targets`), T-96 Fill/Drain Rate (exportarr freeSpace series, deep-linked Grafana dashboard `media-storage-utilization`), T-97 Reclaim Attribution (`getReclaim`/`storage.reclaim` over the T-79 deletion snapshots — category × resolution, cumulative curve, per-batch admin attribution). HYBRID surface (ADR-030): native in-app for utilization (`storage.utilization`, *arr `GET /diskspace` = source of record; `getDiskSpace()` + `diskSpaceSchema`) + reclaim, deep-linked Grafana for the trend. Direct-expedite **forward-capture** — `expediteOneSurvivor` now freezes `{sizeBytes,resolution,imdbRating,tmdbRating}` into the `trash_expedited` payload + deletion-audit notification (no new table). Amended T-79 Deletion Snapshot (now consumed by the reclaim page) + T-80 App Setting (grows `space_targets`). Backend + dashboard shipped; the native Storage page UX lands as a Fable follow-up.                                                       |
| 2026-07-07 | Fable 5    | ADR-031 / DESIGN-014 Space-driven batch policy (propose-only) + rules tuning (PLAN-014): added T-98 Space Policy (`app_settings.space_policy`, migration 0022, DEFAULT OFF; the `space-policy` sync mode PROPOSES a draft batch via `createBatchFromPending` for an over-target opted-in array — never greenlights/sweeps; `evaluateSpacePolicy`/`getSpacePolicyStatus`, `storage.policy.*`, `trash_space_policy` event + `space_policy` notification, one-open-per-kind + cooldown + minCandidates guards) and T-99 Rules Tuning / Graduation (`getTuningReport`/`trash.tuning` — rescue-vs-delete by resolution/rating-band/collection, `save-rate = rescued/(rescued+deleted)`; the skip-gate graduation readiness bar ≥3 completed policy batches / ≤10% save-rate / 0 restores — gives T-78 teeth, resolves ADR-025 C-07; REPORT only, no auto-tuning). New `space-policy` sync kind + `trash_space_policy` ledger event (migration 0022 CHECK relaxes). Amended T-78 Gate Skip (graduation criteria recorded) + T-80 App Setting (grows `space_policy`). Storage-page card + tuning block shipped. The `haynes-ops` CronJob (suspended) is owner-deployed. |
| 2026-07-07 | Fable 5    | ADR-032 / DESIGN-004 D-16 Universal top nav + role-gated user menu (owner-directed IA): the top row becomes the universal section rail (Home · Library · Trash · Bulletin); My Plex and the Ledger move into the user menu (role-gated); the Trash Rules tab + settings card re-home to `/settings/trash` (gated at trash Edit). Amended T-64 Ledger Section (user-menu placement) + T-65 Section Permission (**ledger no-row default flips read_only → disabled** — code default only, no migration; a live role's stored row keeps its level). No new terms, tables, or enums.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 2026-07-07 | Fable 5    | ADR-033 Fold the Batches tab into the per-kind Movies/TV tabs (owner-directed UX restructure): the T-75 Curation Batch is now surfaced AS a property of its media kind — the Movies/TV tab renders no-batch / admin_review / leaving_soon / terminal off `trash.batches.list`, with a Past-batches strip for terminals and an admin-only "new candidates since this batch" strip. Owner wall refinement: one **unified tap-toggle** wall grammar across the live-candidates and curation walls (`trash·shield·check·eye·skip·gone`), `/library` nav moved to a corner icon, per-item Expedite relocated to the item page; the context-aware `?from=` back-link (DESIGN-005 D-17). **UI/design only — no domain-term shift, no table/enum/wire change** (T-75/76/77 unchanged); recorded for traceability.                                                                                                                                                                                                                                                                                                                                                        |
| 2026-07-08 | Fable 5    | ADR-034 / DESIGN-015 Pushover batch-lifecycle notifications (PLAN-016): added T-100 Notification Outbox (`notification_outbox`, migration 0024 — transactional outbox enqueued same-tx by the batch writers on `batch_created`/`batch_leaving_soon`(+`_reminder`)/`batch_swept`; the `notify-outbox` sync mode drains due rows to Pushover, deep-linking `…/trash?tab=<movies\|tv>`; disabled-safe no-op without `PUSHOVER_*` env; `enqueueOutbox`/`deliverOutbox`/`pushoverSenderFromEnv`) and T-101 Delivery Window (`app_settings['notify_window']` = `{startHour,endHour,tz}`, default 18–22 America/New_York; `getNotifyWindow`, `computeEarliestSend`/`computeReminderSend`; `storage.notify.window.*`; `/admin/storage` Notifications card). New `notify-outbox` sync kind + `notify_window` app-setting key + `notification_outbox` table (migration 0024 CHECK relaxes + CREATE). Amended T-80 App Setting (grows `notify_window`). PRD R-115/R-116. Distinct from the T-82 in-app Notification feed (inbound) — this is the outbound push queue. The `haynes-ops` CronJob is owner-deployed.                                                           |
| 2026-07-09 | Fable 5    | ADR-025 errata / DESIGN-011 D-11 (build B) — requested items start saved (overridable): added T-102 Requested Auto-Save (`trash_batch_items.saved_reason`/`requested_override`, migration 0026; person-shield glyph; the sweep guardian honors an explicit human un-save override — requested+never-unsaved → kept, requested+explicitly-unsaved → deleted). Amended T-70/T-77 (a system requested auto-save writes no exclusion + no `trash_batch_saves` row; un-savable by any saver) and T-99 (excludes requested auto-saves from the human rescue stats). No wire change (fields ride the existing `trash.batches.*` shapes). Also DESIGN-004 D-16a — `/settings/trash` becomes a tabbed hub (General/Storage/Reclaim/Rules) and `/admin/storage` retires (redirects to `?tab=storage`); UI-only, storage routers unchanged. |
| 2026-07-09 | Fable 5    | ADR-035 / DESIGN-010 D-11 — Trash candidate read-model: added T-103 Candidate Snapshot (`trash_candidates`/`trash_candidates_state`, migration 0027; snapshot-backed `trash.pending`/`trash.pendingCandidates`/Overview counts with live page-scoped exclusion checks; full/incremental sync post-step + rule-edit triggers + manage-gated `trash.refreshCandidates`; prod serve-stale + background refresh, non-prod inline read-through; expedite/sweep drop deleted ids). Live profile: the paged Maintainerr content API cost 0.4–5.8 s PER CALL (742 movies = 15 serial pages ≈ 6–9 s per crawl, ×4 concurrent per tab load) — first wall tile was 9.1 s; snapshot-served pages make zero collection calls. T-69 Deletion Candidate remains the LIVE notion every destructive flow re-reads. |
| 2026-07-09 | Fable 5    | DESIGN-010 D-12 — cross-server watch VISIBILITY on the trash walls (owner-directed, option B — INFO not protection): added T-104 Watch Visibility and amended T-57 Watch Stats with the display pair `last_watched_at`/`last_watched_server` (migration 0028 — the MAX last-watch across all three Tautulli histories + its estate slug, computed in the same `mergeWatchContributions` pass, no extra Tautulli reads). A watched-longer-ago slated tile shows a MUTED meta-line eye + `Last watched on <server> · <Mon YYYY>` tooltip and stays fully actionable (an ever-watched-but-not-recent item is still sweep-deletable); a recently-watched item keeps its inert `eye` corner; requested still wins the corner glyph. NO guardian/keep/candidate-filter change (T-70/T-73 unchanged). |
| 2026-07-09 | Fable 5    | ADR-030 C-04 amendment / DESIGN-013 D-07 — native free-space trend (owner-greenlit): the Storage tab's Grafana deep-link card is RETIRED as the user-facing fill/drain surface (`grafana.haynesops.com` is LAN-only — a dead link off-LAN) and replaced by an in-app chart: `storage.trend({window: 7d\|30d\|90d\|365d})` queries the in-cluster Prometheus (`packages/api/src/prometheus.ts`, `PROMETHEUS_URL` with the `prometheus-operated.observability` in-cluster default in code — no new helmrelease env needed) for the exportarr series, grouped onto the SAME `STORAGE_ARRAYS` as `getUtilization`, with the T-95 Space Target re-expressed as a dashed free-bytes floor (`targetFreeBytes = total × (1 − target/100)`). Prometheus down ⇒ `unavailable` degrade. Amended T-96 Fill/Drain Rate (native chart; Grafana demoted to the LAN-power-tool footnote). No table/enum/migration change; one new read procedure + e2e stub (`stub-prometheus.ts`). |
| 2026-07-09 | Fable 5    | ADR-036 / DESIGN-010 errata — Maintainerr aging-invariant safeguard (incident: both prod rule pools were `deleteAfterDays:60`/`arrAction:0`, ~16 TB, defused to 9999): added T-105 Aging Invariant / Delete-After Horizon. `auditMaintainerr` now asserts, per ACTIVE collection, that a rule pool has `deleteAfterDays ≥ AGING_HORIZON_MIN_DAYS (3650)` AND `arrAction === 0`, and an app-managed Leaving-Soon collection has `arrAction === 4` — a breach folds into the existing `safe` gate (blocks Expedite + sweep) with a specific banner reason; a collections read failure fails closed. `maintainerrCollectionSchema` extended with `arrAction`/`manualCollection` (were stripped); audit wire gains `agingViolations`; `SafetyBanner` renders the reasons. Reinforces T-76 (Leaving-Soon protection is its `arrAction:4`, not `deleteAfterDays:null` — Maintainerr's worker has no null/0 guard). No table/enum/migration change. |
| 2026-07-10 | Fable 5    | ADR-037 / DESIGN-016 (PLAN-017) — Metrics section foundation: added T-106 Metrics Section (top-level nav after Bulletin + Overview shell; `metrics` Section Permission, `disabled` default ⇒ ships Admin-only), T-107 Metrics Level (`roles.metrics_level` full\|limited, migration 0031; `limited` hides user-aware metrics; audited `setRoleMetricsLevel` → `update_role_metrics_level`; server-side payload-shape enforcement), T-108 Prometheus Reader (new read-only `@hnet/metrics` package — instant+range client against `kube-prometheus-stack`, no secret, no write surface), and T-109 WAN Usage-vs-Capacity (`unpoller_site_*_rate_bytes{subsystem="wan"}` usage vs admin-editable `upload_capacity_mbps`/`download_capacity_mbps` App Settings, meter idiom). Migration 0031 relaxes the `permission_audit`/`role_section_permissions`/`app_settings` CHECKs (+ `roles.metrics_level` column). Hardware ungated; no device identities at any level. Reuses T-95 `getUtilization` for the storage snapshot; Grafana stays deep-linked. |
| 2026-07-10 | Fable 5    | ADR-038 / DESIGN-017 (PLAN-022) — ytdl-sub Library sub-tabs: added T-110 ytdl-sub Library (the k8plex `HOps Peloton`/`HOps YT` libraries surfaced as Library sub-tabs, read DIRECTLY from Plex via new `PlexReadClient.listSectionContents`/`sectionContentsSchema` — no ledger sync; sections resolved by title), T-111 Time-based Show (seasons/episodes encode duration/grouping; poster tile + "N seasons · M episodes" caption), T-112 Durable Poster (resilient display only this round — authed Plex-thumb proxy + `MediaPoster` fallback; durable store DEFERRED to owner Q-06). New `ytdlsub` T-65 Section Permission (`SECTION_IDS += 'ytdlsub'`, `disabled` default ⇒ ships Admin-only; migration 0032 rebuilds ONLY the `role_section_permissions` section CHECK — no new column/table/audit-action, reuses `setSectionPermission`). New `ytdlsubRouter` (`ytdlsub.list`/`.libraries`/`.access`, `ytdlsubProcedure`) + the session- + section-gated Plex-thumb proxy `apps/web/app/api/ytdlsub/poster/route.ts` (`resolveYtdlsubThumbUpstream`). UI reuses the Library `.library-tabs`/`.poster-grid`/`MediaPoster`/`@hnet/ui` filter engine (no new hex; `KindIcon` gains a `show` glyph). Read-only; no write-back. PRD R-121..R-124 + Q-06. |
| 2026-07-10 | Fable 5    | ADR-039 / DESIGN-019 (PLAN-020) — Metrics → Network sub-tab: added T-114 Network Metrics (WAN usage-vs-capacity + 7-day history at `limited`; infra grain at `full`), T-115 Infra-device Performance (UniFi gateway/switch/AP CPU·mem·load — infrastructure names, never clients; no per-client rows, only the aggregate station COUNT), T-116 Allow-listed PromQL Module (`network.ts` + `NETWORK_ALLOWLIST_QUERIES`, unit-test-proven that no query names `unpoller_client_*`/`_remote_user_`/`*_info` — the machine-checked "no client identities at any level" invariant). New `getNetworkMetrics`/`NetworkMetrics` read model (REUSES `getNetworkOverview` for the WAN meters — one denominator) + `metrics.network` procedure (`metricsProcedure`, disjoint `limited`/`full` shape via `includeInfra`) + `network-tab.tsx` (reuses the 017/018 meter/tile/group idioms; the only new geometry is a token-only SVG sparkline). e2e `stub-prometheus.ts` gains the network instant + WAN-history range series. **No migration / no new column / no new table / no write surface** — rides the T-106 `metrics` section + T-107 level + T-109 capacity `app_settings`. Grafana deep-links to the UniFi-Poller boards (Client-Insights board deliberately unlinked). PRD R-127..R-128. |
| 2026-07-10 | Fable 5    | ADR-041 / DESIGN-017 D-07..D-09 (ytdl-sub UX package) — added T-120 Thumb Variant (the allow-listed `grid`/`still` WebP sizes the ytdl-sub Plex-thumb proxy now serves via k8plex's photo-transcode endpoint, memoized in an in-process byte-capped `ThumbLruCache` + strong `(size, thumb)` ETag/304s; transcode miss → original art → fallback tile). Amended T-111 (the "no drill-in" remark is superseded: poster tiles now click through to the read-only show → seasons → episodes drill-in — `ytdlsub.detail`/`ytdlsub.episodes`, `getMetadataItem`/`listMetadataChildren` reads, section-confined by `librarySectionID`, lazily loaded per expanded season). Library tab order is Movies \| TV \| Music \| Peloton \| YouTube \| My Fixes. No migration / no new table / read-only unchanged. PRD R-131..R-132. |
| 2026-07-10 | Fable 5    | DESIGN-018 / OPS-008 (PLAN-018) — Metrics → Apps sub-tab: added T-113 Apps Metrics (\*arr + downloaders). New `getAppsMetrics`/`AppsMetrics` read model in `@hnet/metrics` (four groups — Collection / Acquisition pipeline / Download clients / Indexers — reading the live exportarr + first-party series; each field degrades to null independently) + `metrics.apps` procedure (`metricsProcedure`, shaped by `effectiveMetricsLevel`; a present-but-empty full-only branch keeps the R-118 seam). New `apps/web/app/(app)/metrics/apps-tab.tsx` (mirrors the Overview poll/tile idioms; muted per-group Grafana deep-links). e2e `stub-prometheus.ts` extended with canned app series. **No migration / no new column / no guard edit / no ADR** — rides the 017 `metrics` section + `metrics_level`; the deep-link contract reuses ADR-030 C-04 / ADR-037 C-09. OPS-008 documents the two curated Grafana boards (`arr-library-overview`, `downloads-clients-indexers`) + the bazarr exportarr sidecar. Both-levels (no user-aware \*arr/downloader series). Read-only. PRD R-125..R-126. |
| 2026-07-10 | Fable 5    | ADR-040 / DESIGN-020 (PLAN-019) — Metrics → Hardware sub-tab + SMART alerting: added T-117 Hardware Metrics / NVMe Endurance (ungated Hardware tab — NVMe endurance panel per-pool [Cache-apps mirror vs Cache-staging expendable] with wear odometer + projection-to-90 % + EOL signals; Drive-health table [sleeping disks "asleep", never red]; Node load; T-119 Proxmox showcase), T-118 SMART Alert Transition (the `smart-alerts` sync mode + `evaluateSmartAlerts` single-writer + `smart_drive_state` table [migration 0033, guard-listed] — critical-only, transitions-only paging via the T-100 outbox; **baseline-on-first-sight never pages the known staging state**; enqueue + state update same-tx; `NOTIFY_OUTBOX_EVENT_TYPES += smart_degraded/smart_recovered`), T-119 Proxmox Showcase (`pve_*` host→VM tiles, in-place expansion). New `getHardwareMetrics`/`getDriveSmartReadings`/`NVME_POOLS`/`projectWear` (`@hnet/metrics hardware.ts`) + `metrics.hardware` procedure (no level shaping — ungated) + `hardware-tab.tsx` (wires the reserved shell branch; token-only PoolCard/StatusPill). New `smart-alerts` sync kind + `smart_drive_state` table (migration 0033 CREATE + `sync_runs.run_kind`/`notification_outbox.event_type` CHECK relaxes). `@hnet/sync` gains a read-only `@hnet/metrics` dep. Sources: pve-exporter + node-exporter + smartctl (all already scraped — haynes-ops change = image bump only); glances deferred. PRD R-129..R-130 (erratum: originally cited R-131..R-132; the merged PRD rows are R-129..R-130). |
| 2026-07-10 | Fable 5    | ADR-042 / OPS-009 (PLAN-011) — Authentik hardening (config-as-code + native MFA), executed owner-present: added T-121 Blueprint Baseline / Config-as-Code (the login estate as declarative Authentik blueprints in `haynes-ops` — one file per concern, kustomize `configMapGenerator` → chart `blueprints.configMaps` → worker mount + discovery; drift-zero baseline; `!Find`-over-`!KeyOf` + pk-pin + `Importer.validate()`-before-commit conventions; the `40`-owns-the-stage ownership rule), T-122 MFA Exemption Group (`mfa-exempt` + fail-closed `hnet-mfa-exempt-skip` policy — `hnet-e2e`/`hnet-e2e-member` skip the challenge), T-123 Native-Account MFA (WebAuthn/TOTP validate + enroll on the username+password path of `default-authentication-flow`; Plex-source logins unaffected — login-only source flow; owner ruling `thaynes`' Plex path accepted). **No app-repo code / no migration / no write surface** — config lives in the `haynes-ops` GitOps repo (blueprints), delivered by haynes-ops PR #2014 (baseline) + `a8bd665b`/`42347d80`/`58355768` (MFA promote + fixes). Resolves the original Q-01 (API vs blueprints, DESIGN-002 Part B / `authentik-apply-seed/APPLY.md` §8) in favor of blueprints. Provider/app stay API-managed (Q-11). PRD R-133..R-136. |
| 2026-07-10 | Fable 5    | ADR-043 / DESIGN-021 / OPS-010 (PLAN-024) — Peloton poster guard: added T-124 Poster Guard (the `poster-guard` sync mode + `runPelotonPosterGuard` single-writer — resolves HOps Peloton by title, maps show→series art / season index→duration art by live title/index, re-applies ONLY drifted targets via the confined `PlexWriteClient.uploadPoster`, appends one `poster_guard_applications` row per re-apply [drift baseline + audit]; no `sync_runs` row [smart-alerts precedent]; bounded ~14 reads + drift-gated writes; hourly haynes-ops CronJob mirroring `sync-smart-alerts`; unmapped targets reported never guessed) and T-125 Peloton Override Poster (the 13 series + 10 duration durable PNGs, versioned in the app image at `packages/sync/assets/peloton-posters/` — ADR-043 chose git-in-image + a DB ledger over the owner's DB-`bytea` lean, respecting ADR-019/038/041; index 60 → the clean `60-minutes.png`, not the 60+ bucket). New `poster_guard_applications` table (migration 0034, guard-listed) + `SYNC_RUN_KINDS += poster-guard`; new confined `uploadPoster` write (raw-body `PlexHttp` path). haynes-ops change = image bump + one `sync-poster-guard` CronJob block. Answers ADR-038 C-09 / PRD Q-06 / PLAN-022 Q-01. PRD R-137..R-140. |
| 2026-07-10 | Fable 5    | ADR-044 / DESIGN-022 (PLAN-021) — AI usage metrics: added T-126 AI Usage Sync (the `ai-usage-sync` mode + `syncAiUsage` single-writer — polls Open WebUI's admin API read-only [`/api/v1/chats/all/db` + `/api/v1/users/`], UPSERTS the `ai_usage_chats` mirror; *arr-ledger pattern, no live cross-DB read; no audit/`sync_runs` row; bounded haynes-ops CronJob mirroring `sync-smart-alerts`), T-127 Image-Generation Count (assistant-message image-file heuristic; user uploads never counted; verified 27/27 live), T-128 AI Usage Attribution Level (level-gated `metrics.aiUsage`: `limited` = aggregate counts + trend only, no identity; `full`/admin = per-model + per-user detail; server-authoritative seam). New `ai_usage_chats` table (migration 0035, guard-listed) + `SYNC_RUN_KINDS += ai-usage-sync` + new AI sub-tab on the Metrics section. haynes-ops change = image bump + one `sync-ai-usage` CronJob block + `OPENWEBUI_API_KEY` in the ExternalSecret. PRD R-141..R-143. |
| 2026-07-10 | Fable 5    | ADR-045 / DESIGN-023 / OPS-011 (PLAN-026) — Authentik role portal: added T-129 Synced Tier (`roles.synced_tier` — a Role projects to an Authentik group; Family backfilled on), T-130 Owned-Groups Allowlist (`authentik_owned_groups` + `assertGroupOwned` — the guardrail; the app never touches admin-managed groups/flows/stages, no group deletion), T-131 Authentik Group Portal (the group-membership write surface `@hnet/authentik/write` + `@hnet/openwebui/write`, import-confined like `@hnet/plex/write`; exclusive-tier membership; enforcement-never-decision), T-132 Pending Role Assignment (`pending_role_assignments` — parked for Authentik-only identities, consumed on first login via the Better Auth `session.create.after` hook; email join because the OIDC sub is a `hashed_user_id`), T-133 Authentik Directory Mirror (`authentik_users` + the read-only `authentik-users` sync mode; no `sync_runs` row; the `/admin/users` roster read-model), T-134 OWUI Group Pre-creation (the portal pre-creates the same-named OWUI group since OWUI doesn't auto-create from claims), T-135 hnet-portal Service Account (a dedicated token, cluster-created secret `haynesnetwork-authentik-token`, never the homepage token). New tables `authentik_users` / `pending_role_assignments` / `authentik_group_audit` + `roles.synced_tier` (migration 0036, guard-listed) + CHECK relaxes (`sync_runs.run_kind += authentik-users`, `permission_audit.action += assign_pending_role`, `app_settings.key += authentik_owned_groups, authentik_group_map`). Two new packages `@hnet/authentik` + `@hnet/openwebui`; the import-guard test extended. haynes-ops change = image bump + one `sync-authentik-users` CronJob block + the `haynesnetwork-authentik-token` secret. Delivers BC-02's R-30 "push permissions into Authentik" intent. PRD R-144..R-150. |
| 2026-07-10 | Fable 5    | ADR-046 / DESIGN-024 (PLAN-023) — Books & Audiobooks Library ledger: added T-136 Books Ledger (the app-owned `books_items` MIRROR of Kavita [Books+Comics] + Audiobookshelf [Audio Books] — a DEDICATED table, NOT `media_items`, because books have no monitored/quality/root-folder/Fix semantics; hard rule 4 EXTENDED, one-way sync IN, no write-back), T-137 Books Sync (the read-only `books-sync` mode + the new read-only `@hnet/books` clients [no write surface] + `syncBooks` single-writer — upsert + scoped-tombstone in one tx; no `sync_runs` row; standalone mode like `ai-usage-sync`), T-138 Books Section (the `books` Section-Permission gating the three Library sub-tabs, `disabled` default = ships Admin-only; `booksProcedure` + the `/api/books/cover` proxy gate server-side). New `books_items` table (migration 0037, guard-listed) + `SYNC_RUN_KINDS += books-sync` + `SECTION_IDS += books` + two seeded `app_catalog` cards (Kavita, Audiobookshelf) + `kavita`/`audiobookshelf` icon keys. New package `@hnet/books` (read-only). Three new Library walls (Books · Audiobooks · Comics, after YouTube, before My Fixes) + the authed cover proxy. haynes-ops change = image bump + one `sync-books` CronJob block + `KAVITA_PASSWORD`/`AUDIOBOOKSHELF_PASSWORD` in the ExternalSecret. Diverges from ADR-038 (read-live) by the owner's explicit Q-04 ruling (full ledger integration). PRD R-151..R-156. |
| 2026-07-10 | Fable 5    | DESIGN-004 D-17 (owner design review of the live MOTD): amended T-89 MOTD — the message is now a **sanitized markdown subset** (links `[text](https://…)`, bold/italic/code, breaks/lists, bare-URL autolink; budget 280 → 500) rendered React-element-only by `apps/web/lib/motd-markdown.tsx` (no HTML, no `dangerouslySetInnerHTML`, http(s)-only hrefs — ADR-027's no-injection-surface property preserved; plain-text messages render unchanged, no migration), the severity glyph is a themed inline SVG (never an OS emoji), and `/admin/motd` previews through the real `<MotdSurface>` with a Markdown affordance + Insert-link helper. |
| 2026-07-11 | Fable 5    | ADR-047 / DESIGN-025 (PLAN-028) — Library "Watch/Listen/Read here" access-aware deep links: added T-139 \*arr→Plex Match (the `media_plex_matches` rebuildable cache + the read-only `plex-match` sync mode — resolves each `media_item` to its exact Plex `{library, ratingKey}` by shared GUID [tmdb/imdb/tvdb/musicbrainz], upsert + reconcile via `syncPlexMatches`; reports the per-kind match rate; no \*arr call, no Plex write, no `sync_runs` row), T-140 Library Access Gate / THE INVARIANT (a user NEVER receives an item from a Plex library their role can't access — enforced SERVER-SIDE by `resolveLibraryAccessGate` REUSING the ADR-024 effective-library resolver + a derived per-kind home library; `libraryAccessWhere` applied to every `media_items` read + the poster proxy + ytdl-sub per-library; a withheld library's tab hides; direct-id fetches NOT_FOUND), T-141 Play-Here Deep Link (the app-specific "Watch on Plex"/"Listen on Audiobookshelf"/"Read on Kavita" PRIMARY action on a PRESENT item's drill-in; `buildPlexWebDeepLink` for Plex, the existing `deep_link_url` for books/AV, live ratingKeys for ytdl-sub; reflow-free, no new hex). New `media_plex_matches` table (migration 0038, guard-listed INSERT/UPDATE/DELETE) + `SYNC_RUN_KINDS += plex-match` + `PLEX_MATCH_GUID_SOURCES`. `@hnet/sync` gains a `@hnet/plex` dep. A deliberate owner-approved tightening: non-admin Library visibility now follows the role→library grant matrix (admins unrestricted). haynes-ops change = image bump + one `sync-plex-match` CronJob block (no new secret — reuses the Plex tokens + DATABASE_URL). PRD R-157. |
| 2026-07-11 | Fable 5    | ADR-049 / DESIGN-004 D-18 + DESIGN-012 D-09 (PLAN-027) — Roles-grid clarity + Bulletin Feed/Messages visibility: added T-143 Bulletin View Grant (the `role_bulletin_view_grants` row-per-view table + `setRoleBulletinViews` single-writer, audited `update_bulletin_views` in-tx, guard-listed; `bulletinViewProcedure` gates `communication.feed` on `feed` + `communication.messages.*` on `messages` — a role without a view gets FORBIDDEN, not a UI hide; RESOLUTION default-ON [no rows ⇒ both, the "Bulletin is for everyone" ADR-026 C-02 pattern — DIVERGES from message-actions' default-deny], present rows narrow, Admin implies both; the `/bulletin` client renders only granted sub-tabs) and T-144 Section Capability Map (`apps/web/lib/role-sections.ts` — per-section, which levels the `/admin/roles` control offers, DERIVED from the gating code: `'tri'` Edit/Read-only/Disabled for Ledger+Trash [real `edit` rung], `'toggle'` Enabled/Disabled for Bulletin/Metrics/ytdl-sub/Books; stored enum + DB values UNCHANGED, "Enabled" persists `read_only`). New `role_bulletin_view_grants` table + `BULLETIN_VIEWS` enum + permission_audit `update_bulletin_views` (migration 0039, seeds the Default role `messages`-only). The `/admin/roles` Bulletin cell = Enabled/Disabled dropdown + Feed/Messages checkboxes (greyed when Disabled) + the message-action badge; reflow-free (ADR-015), no new hex. haynes-ops change = image bump ONLY (no new secret/CronJob). PRD R-159. |
| 2026-07-11 | Fable 5    | ADR-048 / DESIGN-005 D-22 + DESIGN-017 D-09 amend (PLAN-030) — Season posters in season rows + TV episode thumbnails: added T-142 Season/Episode Art (Plex-matched) — the season-poster icon per Season row (TV via the ADR-047 match → the show's Plex season art; Peloton via the live k8plex duration posters) + TV episode stills (the ADR-041 `still` variant), one Plex source (no TMDB). TV art rides a SIGNED, ITEM-SCOPED `/api/library/plex-art` proxy reference (`signPlexArtRef`/`verifyPlexArtRef` HMAC over `(mediaItemId, serverSlug, thumb, size)`) upholding THE INVARIANT (T-140) — a caller can't fetch an inaccessible sibling title's art on the same server. New `ledger.plexSeasons`/`ledger.plexEpisodeArt` (re-gate + degrade to no-icons), `ytdlsub.detail.seasons[].posterUrl`, `episodeNumber` on `ledger.children`, the `TvSeasonEpisodes` lazy per-season art load. NO migration (derived from `media_plex_matches` + live Plex reads); reuses the ADR-041 transcode/LRU/ETag path on the MATCHED server. haynes-ops change = image bump ONLY (no new CronJob, no new secret — signs with the existing `BETTER_AUTH_SECRET`). PRD R-158. |
| 2026-07-11 | Fable 5    | ADR-050 / DESIGN-012 D-10..D-13 (PLAN-034) — the Helpdesk: added T-145 Ticket (the household media-issue report — required title/body + intake category + optional linked Media Item; household-visible per Q-01; created via the `post` grant; `messages` board DROPPED per Q-03), T-146 Ticket Status (`open ⇄ in_progress → complete \| rejected`; complete terminal, rejected re-opens; staff-only via `moderate`, optional household-visible note on EVERY transition — the `TICKET_TRANSITIONS` matrix enforced under a row lock), T-147 Ticket Event (the append-only creation+transition history, the aggregate's own audit record) and T-148 Ticket Reply (the flat thread, open to any `messages`-view member per Q-02, bumps `last_activity_at` same-tx). New `tickets`/`ticket_events`/`ticket_replies` tables + `ticket_created` outbox event (same-tx admin ping, ADR-034 C-01) in migration 0040. PRD R-160..R-164. |
| 2026-07-11 | Fable 5    | ADR-051/052/053 / DESIGN-026 (PLAN-029) — Library views, grouping & the Sorting/Filtering overhaul: added T-149 Library View (selectable flat/grouped/hierarchy per wall, R2 defaults, URL-synced, per-user-persisted; TV keeps Shows→Seasons→Episodes), T-150 Group View (aggregate cards → count + stacked covers → drill-in; queries over the existing engines, no new store; gated by T-140), T-151 Sort/Filter Registry (the per-(wall, view-level) capability declaration across the THREE-ENGINE seam — ledger [extends DESIGN-008 D-09 `SORT_SPECS`/`buildLibraryWhere`] / live-Plex / books [extends ADR-046 `books.search`] — with Plex-style asymmetry: a level offers only the sorts/facets it can answer), T-152 Date Released (`released_at` — the canonical release/air/pub timestamp added to sync [Radarr digitalRelease/inCinemas, Sonarr firstAired, ABS publishedDate; live-Plex `originallyAvailableAt`; Kavita honestly absent], peer of the already-present Date Added), T-153 Library Preferences (`library_preferences` — server-side per-user last view/sort, R1/R6; URL overrides for shared links; no audit rows), T-154 Plex Account Map (the app-user↔account attribution seam reusing `resolvePlexIdentity` + the friend matchers; auto-filled for video, admin-filled for books; the Feed-attribution backlog reuses it verbatim) and T-155 Per-User Watch/Read State (Tautulli per-user re-key ADDITIVE to the household aggregate; ABS admin-read audiobooks; Kavita read-state DEFERRED). Live-verified this session (read-only SELECTs): Date Added 100%, household watch sparse (radarr 3.8%/sonarr 18.8%/music 0%), ABS facets rich vs Kavita sparse, no prior per-user store. ONE new sync/schema surface (`released_at`) + two new per-user tables (prefs + map); migrations take next-free numbers at build. Pixel-level UX (view selector, group-card art, jump-bar placement, facet buckets) DEFERRED to the build UX pass (DESIGN-026 D-11). PRD R-165..R-171. |
| 2026-07-11 | Fable 5    | ADR-054 / DESIGN-027 / OPS-013 §10 (PLAN-039) — MAM compliance governor: added T-156 MAM Compliance Governor (the ~15-min `mam-governor` sync mode + `evaluateMamGovernor` single-writer — count unsatisfied locally, gate near the cap, upsert `mam_gate_state` + same-tx transition outbox; fail-closed; baseline-on-first-sight; smart-alerts shape, no `sync_runs` row), T-157 Unsatisfied Torrent / Local Count (`books-mam` still-downloading OR complete-but-<72h-seeded — conservative, ZERO MAM API surface, no qB credential), T-158 MAM Gate / Prowlarr indexer seam (toggle the MAM Prowlarr indexer's `enable` via GET-then-PUT — Prowlarr's fullSync LazyLibrarian app PROPAGATES it to LL so LL stops querying MAM with no Torznab-error/blocklist; chosen over the LL-side toggle because fullSync CLOBBERS a manual LL edit — verified live; the `@hnet/downloads/write` surface is import-confined), T-159 Governor Config Seam (`resolveGovernorConfig` — the ONE limit/buffer/stuck-hours resolution point, env-backed in v1, PLAN-040 adds a DB override behind the same call), T-160 Zero-Headroom Stuck Alert (`mam_gate_stuck` once after 48h of headroom pinned at 0 — demand exceeds the ~limit-per-72h throughput). New `@hnet/downloads` package (qBittorrent read + Prowlarr read/write, the write confined). New single-row `mam_gate_state` table (migration 0041, guard-listed INSERT/UPDATE) + `SYNC_RUN_KINDS += mam-governor` + `NOTIFY_OUTBOX_EVENT_TYPES += mam_gate_paused/mam_gate_resumed/mam_gate_stuck`. Live-verified this session (read-only + one reversible toggle cycle): qB unauth count = 13 unsatisfied; Prowlarr indexer 17 enable→false propagates to LL Torznab_0 in ~6s and back. haynes-ops change = image bump + one `sync-mam-governor` CronJob block + `PROWLARR_API_KEY` template line (already `extract`ed from media-stack — no new 1Password item). PRD R-172..R-177. |
| 2026-07-14 | Fable 5    | ADR-055 / DESIGN-028 (PLAN-044) — Goodreads requests MVP (the Integration Tab Saga's MVP): added T-161 Integration (per-user link to a PUBLIC external account — `user_integrations`; vanity URL resolved server-side via redirect; NO OAuth/secret; link/unlink audited), T-162 Shelf Sync (the read-only `goodreads-sync` mode + `@hnet/goodreads` RSS/Google-Books clients — mirror the shelf into `integration_shelf_items`, GB-enrich with mandatory retry/backoff + comic classification; standalone, no `sync_runs` row; NEVER writes LL provider config), T-163 Book Request (the `book_requests` request/Missing ledger — both formats, matched-library-id, LL push GB-volume→addBook→queueBook[mandatory]→searchBook via the confined `@hnet/lazylibrarian/write`; reconcile back; ADR-046 mirror stays pure), T-164 Missing (books) / Coverage (the *arr wanted/missing idiom + coverage % = have/shelf; normalized-title match — no ISBN column, a residual), T-165 Comic (unroutable) (comics parked out of LL — `unroutable_reason='comic'`, Kapowarr's domain; a saga residual). New packages `@hnet/lazylibrarian` (read + confined write) + `@hnet/goodreads` (read-only RSS + GB). New tables `user_integrations`/`integration_shelf_items`/`book_requests` (migration 0045, guard-listed) + `SECTION_IDS += integrations` (ships Admin-only) + `SYNC_RUN_KINDS += goodreads-sync` + `PERMISSION_AUDIT_ACTIONS += link_integration/unlink_integration/request_book_search` + `BOOK_REQUEST_STATUSES`/`INTEGRATION_PROVIDERS`/`INTEGRATION_STATUSES`. The arr-write import guard extended for `@hnet/lazylibrarian/write`. New Integrations tab (link card + coverage % + requests/Missing wall + audited manual re-search). Hermetic build (stub Goodreads RSS + stub LL); LIVE acceptance owner-present after deploy. Owner's real account: vanity `goodreads.com/haynesnetwork` → id 202652880; to-read shelf public + parsing (6 items incl. comics). haynes-ops change (later) = image bump + one `sync-goodreads` CronJob + `LAZYLIBRARIAN_API_KEY`/`GOOGLE_BOOKS_API_KEY` in the ExternalSecret. PRD R-178..R-184. |
| 2026-07-14 | Opus 4.8   | ADR-056 (PLAN-046) — Kapowarr comic acquisition (the comics-source leg): added T-166 Comic Acquisition (Kapowarr) and revised T-165 (Comic classification → now ROUTES, not merely parks). A comic-classified Book Request resolves to a ComicVine volume via Kapowarr's own search (`pickBestVolume` — shared-title-token rank, prefer the ORIGINAL edition `translated=false`), is added MONITORED to Kapowarr, and reconciles its volume state into the new `book_requests.comic_status` (+`kapowarr_volume_id`/`comicvine_id`; migration 0046; `BOOK_REQUEST_FORMATS += comic`). New `@hnet/kapowarr` package (read + confined `./write` — the arr-write import guard extended for `@hnet/kapowarr/write`). The `integrations.search` force-search DISPATCHES: comic → Kapowarr `auto_search` task; book/audio → LL `searchBook` — both audited `request_book_search`. Kapowarr acquires from ITS OWN GetComics DDL — NEVER MAM/qBittorrent/Prowlarr/the governor. Absent `KAPOWARR_API_KEY` / no ComicVine match ⇒ parked (honest degradation). Hermetic build (`stub-kapowarr`). LIVE (owner-directed, 2026-07-14): ComicVine confirmed operable; both mis-routed shelf comics un-wanted in LL (`unqueueBook`→Skipped) + rows flipped to comic routing; the two shelf comics added monitored WANTED in Kapowarr (cv 25478 Scott Pilgrim→vol 1; cv 121720 Legend-of-Batman partwork→vol 2). haynes-ops change (later) = image bump + `KAPOWARR_API_KEY` in the ExternalSecret (the goodreads-sync CronJob gains the comic leg). PRD R-185..R-187. |
| 2026-07-14 | Fable 5    | ADR-057 / DESIGN-029 (PLAN-045) — Integrations hub + Goodreads library-idiom redesign: added T-167 Integrations Hub / Provider Sub-Section (provider cards → `?tab=` sub-section with the stats Overview + the Items library wall; ALL FOUR shelves sync AND acquire — `GOODREADS_SHELVES` default + migration 0047 backfill, the owner's A1-overruled ruling; absent-custom-shelf 404 tolerance A3), T-168 Shelf Chips (the Helpdesk D-12 semantics ported verbatim — union multi-select, superset All, counts, `?shelf=` replace params + `none` sentinel, populated-value-gated), T-169 Library-Wanted (books) (household Wanted tiles composed from `book_requests` onto the Books/Audiobooks/Comics walls — books-section-gated per Q-01, `books.wanted` + the registry `?wanted=1` row, force-search parity via the dispatching `integrations.search` + PLAN-015 reserved-slot feedback, deep-link `?focus=`). New domain reads `computeShelfStats`/`getShelfWallItems`/`getWantedBookRequests` + the shared `requestPhase`/`isRequestSearchable`; `isAbsentCustomShelfError` (`@hnet/goodreads`). The mirror stays pure (ADR-046); pacing/backoff paths reused verbatim. PRD R-188..R-191. |
| 2026-07-14 | Opus 4.8   | ADR-059 / DESIGN-030 (PLAN-048 SLICE 1) — Activity / In-Flight, the pipeline made visible: added T-170 Activity (the cross-library Library sub-tab — live poll-through per source per ADR-059 Q-01, Helpdesk stage+kind chips with counts, LIST-resolver section gating; `activity.list`, `ActivityItem`/`ActivitySourceAdapter`), T-171 In-Flight (the `ActivityStage` surfaced as the typed `inFlight` badge on `MediaCard`/`BookCard` — no new poster anatomy, ADR-058 stands), T-172 Import Failure (`ActivityFailureKind` incl. `stranded_import` = the OPS-013 §11 42-download incident; persisted in `activity_import_failures` (migration 0048 — the ONLY persisted activity state), the `activity-scan` sync mode enqueues `activity_import_failed` outbox same-tx, a #264-idiom failure detail page with role-gated Retry-import/Force-research). New grant seam `role_activity_action_grants` + `ACTIVITY_ACTIONS` + `activityActionProcedure` (rides the ADR-023 machinery; the LL confined `forceProcess`/`searchBook` fire after the audit commit). BOOKS adapter (LL + SAB) is SLICE 1; the *arr + Kapowarr adapters fill the same contract (DESIGN-030 D-08). New `SabnzbdReadClient` (`@hnet/downloads/read`) + LL wanted-table read + `forceProcess` write. PRD R-192..R-194, US-12. |
| 2026-07-14 | Opus 4.8   | DESIGN-004 D-22 (owner-ratified from an approved mockup) — the NAV RESTRUCTURE: the universal top row slims to FOUR (Home · Library · Tickets · Trash); **Metrics + Integrations move into the role-gated user menu**, each gated exactly like its former tab. **`HELPDESK_NAME` ratified "Tickets"** — it now drives the top-nav entry, the `/bulletin` section heading, and the lead sub-tab (`[Tickets] [Feed]`); user-visible "Helpdesk"/"Bulletin" section-brand strings swept to "Tickets" (nav, headings, empty-state/intake copy, back-links). No route/section-id/grant/migration change (route stays `/bulletin`, section id `bulletin`, `/admin/roles` column "Bulletin"); ADR-015 untouched. Amended T-81 Bulletin Section, T-106 Metrics Section (placement), T-145 Ticket (display name). No new terms/enums/tables. |
| 2026-07-15 | Fable 5    | ADR-060 / DESIGN-031 (PLAN-035) — Ticket email notifications: added T-173 Email Channel (`notification_outbox` grows `channel='email'` — recipient resolved at enqueue-time into `payload.to` same-tx (ADR-060 C-02); `renderOutboxEmail` plain-text sibling renderer; nodemailer SMTP submission from the F-04 `SMTP_*` contract; per-channel disabled-safe drainer routing) and T-174 Notification Preference (`notification_preferences.email_ticket_updates`, default OFF, single-writer, no audit — the library-preferences precedent). Enqueue points: `createTicket` → admin email (`TICKET_ADMIN_EMAIL`, unconditional); `addTicketReply`/`transitionTicket` → ticket author when opted in and not the actor. New event types `ticket_replied`/`ticket_status_changed` (migration 0049). PRD R-195..R-197. Owner defaults 2026-07-15: one opt-in switch / trash email follow-up / admin immediate. |
| 2026-07-15 | Fable 5    | ADR-060 follow-up / DESIGN-031 amendment (PLAN-048 tail) — the nightly admin failure digest: new `failure-digest` sync mode (nightly CronJob) reads OPEN `activity_import_failures` and enqueues ONE `activity_failure_digest` email-channel outbox row (count + oldest 20, deep link `/library/activity`); a clean ledger sends nothing. Amends T-172 (the anticipated digest consumer of `activity_import_failed` outbox rows now exists) and T-173 (the email channel's second consumer). Migration 0050 (event-type + run-kind CHECK relaxes). PRD R-198. |
| 2026-07-15 | Fable 5    | ADR-061 / DESIGN-032 (PLAN-038) — ticket media precision: added T-175 Ticket Locator (nullable `target_*` columns on `tickets`, snapshotted label, migration 0051 — which also deletes the pre-locator seed tickets per owner ruling Q-03). Compose gains the in-place drill (TV season→episode via `ledger.children`; Music album→track via NEW lidarr `listTracks` + `ledger.albumTracks`) under the LEAF-OR-SCOPE rule (R-200; Q-01 category nudge = default focus only). Q-02 art inheritance (episode still, else parent poster). ytdlsub/books linking deferred (Q-04). PRD R-199..R-201. |
