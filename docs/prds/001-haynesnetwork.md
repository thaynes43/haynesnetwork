# PRD-001: haynesnetwork — SSO front door and media service hub

- **Status:** Accepted
- **Owner:** Tom Haynes
- **Last updated:** 2026-07-11 (R-160..R-164 Helpdesk — the Bulletin Messages board becomes a media-issue ticket system: poster wall, state machine + history, household replies, admin pings; ADR-050 / DESIGN-012 D-10..D-13, PLAN-034)

## Summary

Users of the Haynes Plex ecosystem have no single place to discover the self-hosted apps
they may use, no self-service control over their Plex libraries, and no way to act on
broken media without asking the admin. haynesnetwork is a responsive web app at
`haynesnetwork.com` where users sign in once through Authentik (authenticating with their
Plex account) and get: a permissioned dashboard of `*.haynesnetwork.com` apps, self-service
Plex library management across the three Plex servers, media lookup with a one-click "fix"
that forces the right *arr to re-pull broken content, and a durable ledger of the media
estate (requests, deletions, wants) that doubles as a disaster-recovery source for
rebuilding a lost *arr instance.

## Goals

- One login (Authentik/Plex) → one personal hub for everything on `*.haynesnetwork.com`.
- Give the admin lightweight, centralized control over who sees/uses what.
- Let users help themselves: fix broken content, manage their Plex libraries.
- Keep an independent, queryable record of the media estate and its history.

## Non-goals

- Replacing Seerr for content requests (we link to it and read from it).
- Enforcing app access inside Authentik — follow-on (R-30); hiding links is the accepted start.
- Being a media player, download client, or *arr configuration UI.
- The *arr/Seerr k8s migration itself (owner handles in haynes-ops, in parallel).

## Actors & roles

> **Amended by ADR-012 (2026-07-05):** roles are now **DB-backed, admin-managed rows**
> (`roles` table), not a fixed `Member`/`Admin` enum, and each user has **exactly one**.
> Migration 0007 seeds **three** roles: the **Admin** (superuser) and **Default** (new-user)
> system roles, plus a normal, editable/deletable **`Family`** starter role (every app except
> Tautulli). "Family" is no longer a built-in designation — it is just a seeded example role,
> and its Phase-3 family-library meaning (R-26/R-27) will attach to a **role attribute** rather
> than the removed `users.is_family` flag. A role may instead grant **all apps** via
> `roles.grants_all` ("All apps") without admin access. See ADR-012 and DDD-001 T-02..T-05, T-49.

| Role                          | Description                                                                                                                                                                                                                                                                                                                                                                      |
| ----------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Admin                         | Superuser role (`roles.is_admin`): full control of catalog, users, roles, restore; **implicitly sees every app**. Immutable (no rename/edit/delete). First login by an allowlisted email is assigned this role.                                                                                                                                                                  |
| Default                       | The role assigned to every successful Authentik login (`roles.is_default`). Its editable app set is what basic users see; manages own Plex libraries within allowed set; can report fixes. Cannot be renamed or deleted (app set is editable).                                                                                                                                   |
| Family (seeded example)       | A `Family` role is now **seeded by migration 0007** — a normal, editable/deletable role granting every catalog app **except Tautulli**. It ships as the starter example and is the home for the Phase-3 family-library grant: `HNet Home Videos` + `HNet Photos` are ordinary libraries granted only to this role (R-26/R-27, ADR-012 C-09/C-12, ADR-017 — built by DESIGN-007). |
| _(other admin-created roles)_ | Admins create additional roles with their own editable app sets (or the "All apps" grant, `roles.grants_all` — every app, incl. future ones, without admin access; ADR-012 C-11) and assign users.                                                                                                                                                                               |

## Requirements

### Identity & access

| ID   | Requirement                                                                                                                                                                                                                     | Priority |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| R-01 | Sign-in exclusively via Authentik OIDC (`authentik.haynesnetwork.com`); Plex is offered as the credential inside Authentik. No local passwords or invite tokens.                                                                | Must     |
| R-02 | First login whose email (case-insensitive) is on the `BOOTSTRAP_ADMIN_EMAILS` allowlist is idempotently promoted to Admin, with an audit row.                                                                                   | Must     |
| R-03 | Every other first login auto-creates an account with default permissions — no approval gate. _(Amended by ADR-012: the new user lands in the **Default role**, DB-defaulted on `users.role_id`; "Member" is no longer a role.)_ | Must     |
| R-04 | All role/permission changes write audit rows (who, what, when, initiator kind) in the same transaction.                                                                                                                         | Must     |

### Dashboard & app catalog

| ID    | Requirement                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Priority |
| ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------- |
| R-10  | Authenticated users see a tile dashboard of apps they're permitted to open; tiles link out to their configured URL.                                                                                                                                                                                                                                                                                                                                                                                          | Must     |
| R-11  | The catalog is DB-backed and admin-editable (name, description, icon, URL, default-visible flag, display order) — no redeploy to change a link.                                                                                                                                                                                                                                                                                                                                                              | Must     |
| R-12  | Default-visible tiles: Seerr (`overseerr.haynesnetwork.com` until the owner's Seerr cutover), Plex (`plex.haynesnetwork.com`), K8Plex (`k8plex.haynesnetwork.com`). _(Amended by ADR-012: "default-visible" is now the **Default role's app set** (`role_app_grants`), seeded by migration 0007 to **Seerr, Plex, K8Plex, and PlexOps** (`plexops` — basic users get PlexOps too); the per-app `app_catalog.default_visible` flag is gone. Admins change what basic users see by editing the Default role.)_ | Must     |
| R-13  | Admin-grantable tiles (seeded hidden): plexops, Immich, Open WebUI, Paperless, Tautulli — extensible via R-11.                                                                                                                                                                                                                                                                                                                                                                                               | Must     |
| R-14  | ~~**User-facing links must never point at `*.haynesops.com`** (LAN-only ingresses). Enforced by validation on catalog writes.~~ _(Amended by ADR-013: the host restriction is retired — the catalog is admin-curated and accepts **any normalized `http(s)` URL** entered as a plain string; the only floor is http(s)-only, no embedded credentials. No host allow/deny list.)_                                                                                                                             | Must     |
| R-15  | ~~Admins can grant/revoke individual apps per user.~~ **SUPERSEDED by ADR-012:** there are no per-user app grants. Access is **role-based** — an app is granted to a _role_ (`role_app_grants`), and a user gets it by being assigned that role. To give one user a bespoke app set, an admin creates or reuses a role.                                                                                                                                                                                      | ~~Must~~ |
| R-105 | Admins may set an optional **Message-of-the-Day** banner (severity-coded `info`/`warning`, optionally time-windowed, per-user dismissible) shown at the top of the dashboard to every signed-in user. Composed/cleared from `/admin/motd`; the write is audited (ADR-027 / DESIGN-004 D-15).                                                                                                                                                                                                                 | Could    |

### Tags & permission bundles

> **Amended by ADR-012 (2026-07-05):** "tags" as a distinct mechanism are gone —
> the permission-bundle concept **collapsed into the Role**. A Role _is_ the bundle (a named,
> admin-managed app set); assigning a user a role is how bundled permissions are applied.
> Read "tag" as "role" below. The Phase-3 allowed-library / family part of the bundle will
> attach to a role attribute (R-26/R-27, ADR-012 C-09).

| ID   | Requirement                                                                                                                                                                                                                                                                                                       | Priority |
| ---- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| R-20 | Admins can create/edit/delete **roles**; a role bundles a set of permissions (app grants; Phase-3: allowed Plex libraries, family designation). _(Amended by ADR-012: role replaces tag; `roles` + `role_app_grants`. Admin/Default are system roles — see Actors table.)_                                        | Must     |
| R-21 | Assigning/changing a user's **role** applies its bundled permissions; a user has exactly one role. _(Amended by ADR-012: "per-user permissions without tags" is removed with R-15 — bespoke access = a dedicated role.)_                                                                                          | Must     |
| R-22 | Effective permissions = **the user's role's apps** (or all apps if the role is the Admin superuser). Provenance is simply the role — there is no multi-source union to attribute. _(Amended by ADR-012: the `effective_app_grants` view and per-source provenance are gone; computed by `effectiveAppsForUser`.)_ | Should   |

### Plex library self-service (Phase 3)

| ID   | Requirement                                                                                                                                                                                                         | Priority |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| R-25 | Users can add/remove Plex libraries on their own account, per server, across all three servers (`haynestower`/`haynesops`/`hayneskube`), within the set their **Role** allows them (ADR-017).                       | Must     |
| R-26 | Default allowed set = the **Default role's** library grants (all libraries except `HNet Home Videos` and `HNet Photos`); those two are the **Family role's** exclusive grants (ADR-017 — no `is_family_only` flag). | Must     |
| R-27 | Admins set **per-Role** allowed-library sets; Family is just a Role whose grants include the family libraries (ADR-012 C-09/C-12; ADR-017).                                                                         | Must     |
| R-28 | Library changes are applied through the Plex sharing API using each server's owner token (1Password) and are audit-logged (ADR-017 / DESIGN-007).                                                                   | Must     |
| R-30 | Follow-on: sync app permissions into Authentik via its API once Authentik-side enforcement is wanted.                                                                                                               | Later    |

### Media ledger & fix (Phase 2)

| ID    | Requirement                                                                                                                                                                                                                                                                                                                                | Priority |
| ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------- |
| R-40  | The app maintains a ledger synced FROM Sonarr, Radarr, and Lidarr (they are the source of truth): all monitored media, on-disk state, quality profile, root folder, and *arr tags per item. The ledger also mirrors *arr-provided **metadata** (ratings, votes, genres, runtime, images) alongside these fields — see R-67 and DESIGN-008. | Must     |
| R-41  | The ledger records history: who requested what (attributed via Seerr API), what was deleted when, grabs/imports (from *arr history). Maintainerr integration is a follow-on once it's in k8s.                                                                                                                                              | Must     |
| R-42  | Wanted-but-not-on-disk items are captured and browsable.                                                                                                                                                                                                                                                                                   | Must     |
| R-43  | Users can search/browse the ledger and trigger **Fix** on an item they believe is broken.                                                                                                                                                                                                                                                  | Must     |
| R-44  | Fix = mark the offending release failed in the *arr (blocklisting it) + trigger a new search; falls back to delete-file + search when no grab history exists.                                                                                                                                                                              | Must     |
| R-45  | Every Fix requires a reason from a taxonomy (e.g. won't play / corrupt, wrong language, wrong version or quality, missing subtitles, wrong content entirely) or "Other" + free text. Reasons are stored for analysis.                                                                                                                      | Must     |
| R-46  | Fix requests are tracked (requester, item, reason, *arr actions taken, outcome) and visible to admins; users see their own fix history and status.                                                                                                                                                                                         | Must     |
| R-47  | Rate/abuse guard: sensible per-user limits on fix actions (admin-configurable later).                                                                                                                                                                                                                                                      | Should   |
| R-106 | Every user-triggered downstream *arr action (Fix, Force Search, incl. roll-ups) gives **live status feedback**: a wire-ack the request landed ("searching"), download progress (queued → downloading% → importing → complete), and a **terminal** for the empty/failed/stalled cases — no silent buttons, no stuck states.                 | Must     |
| R-107 | Feedback **cascades to every child** a roll-up touched (per-season episode / per-artist album phases), and each authoritative step (grab → import → complete / fail) is reflected in the shared item **History** so others can see what was done.                                                                                          | Should   |
| R-67  | The ledger stores per-item metadata harvested from the *arrs: IMDb/TMDb rating, IMDb/TMDb vote counts, Rotten Tomatoes tomatometer + audience (popcorn), runtime, resolution, genres, added date.                                                                                                                                          | Must     |
| R-68  | Watch-stats (play count, last-viewed) are harvested from Tautulli — across all three estate instances, unified per item — and stored.                                                                                                                                                                                                      | Must     |
| R-69  | Posters are served small and server-side (proxied, not hot-linked) and shown in Library.                                                                                                                                                                                                                                                   | Must     |
| R-70  | Users can sort and filter the library by any stored metadata field.                                                                                                                                                                                                                                                                        | Must     |
| R-71  | Metadata is refreshed periodically (ratings/votes/watch-stats are volatile); a per-item source set + fetched-at is recorded.                                                                                                                                                                                                               | Must     |
| R-72  | Ledger-only / deleted items (absent from every *arr) get metadata via the *arr lookup-by-external-id endpoints (and a direct TMDB/TVDB fallback), without re-adding the item.                                                                                                                                                              | Should   |

> **Note (2026-07-06) — metadata enrichment, posters & shared filters (R-40, R-67..R-72):** the
> ledger is enriched with multi-source metadata (the *arrs, then Tautulli watch-stats across the
> three estate instances, then Maintainerr, with a direct TMDB/TVDB fallback for holes), held in a
> separate 1:1 `media_metadata` table (**ADR-018**) refreshed by a 6-hourly `metadata-refresh`
> sync job with per-source degradation. Posters are served through an authed **proxy** — never
> stored, never hot-linked (**ADR-019**). Library gains sort + filter over the metadata via a
> shared filter/sort engine reused by the Ledger and Trash sections. See DESIGN-008. (The Library
> grid + filter UX lands as a follow-up UX change on the same branch; the backend contract ships
> first.)

> **Note (2026-07-05) — library browse & fix-history UX (R-43/R-46):** the `/library` page presents
> browse as **Movies · TV · Music** sub-tabs (default Movies, no "All"), each scoping the search to
> that one category (movies→Radarr, TV→Sonarr, music→Lidarr). A fourth **My Fixes** sub-tab hosts
> the caller's own fix/force-search history (R-46) — **relocated out of the account/user menu** into
> Library; the old `/my-fixes` route redirects to `/library?tab=my-fixes`. See DESIGN-005 D-15/D-17
> and DESIGN-004 D-08. No change to the requirements themselves — this records how they are surfaced.

> **Note (2026-07-06) — subtitle Fix routes to Bazarr (R-44/R-45):** the Fix reason
> `missing_subtitles` does **not** take the R-44 mark-failed/blocklist or delete-file path and does
> **not** trigger an *arr re-grab — the media file is untouched. Instead it triggers a **Bazarr**
> subtitle search for the movie (Radarr) / episode (Sonarr). Because Bazarr covers movies/TV only,
> the reason is **not offered for Music** (Lidarr). No change to the requirements themselves — see
> ADR-016 / DESIGN-005 D-19.

> **Note (2026-07-07) — live action feedback (R-43/R-46/R-47, R-106/R-107):** every Fix / Force
> Search now reports live status back (**ADR-028**): a wire-ack ("searching") → download progress
> (queued → downloading% → importing → complete) → a never-stuck terminal (`nothing_found` after a
> 15-min window, `stalled` after 45 min or on an *arr download error, `failed`), cascading per-child
> for season/artist roll-ups. The phase is **DERIVED read-only** on demand from the *arr download
> queue + the sync-ingested ledger milestones — no new stored state, no migration, no new *arr write
> surface. Poll-on-demand (no background poller v1). The R-47 one-open-fix guard is now **surfaced**
> in the UI (the Fix button disables + shows the in-flight phase while an open fix targets the same
> grain, instead of erroring). The backend contract (`fix.progress`/`fix.searchProgress`) ships
> first; the progress UI (chips/bars, live My Fixes) lands as a Fable follow-up. See DESIGN-005
> D-20/D-21.

### Failsafe restore (Phase 2)

| ID   | Requirement                                                                                                                                                       | Priority |
| ---- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| R-50 | Admin can diff the ledger against a live Sonarr/Radarr/Lidarr instance and see what's missing.                                                                    | Must     |
| R-51 | Admin can re-add missing items to the matching *arr (monitored, with recorded quality profile/root folder/tags) to recover a lost DB or bootstrap a fresh server. | Must     |
| R-52 | Restore is explicitly admin-initiated, previewed before execution, and audit-logged. Sync direction is otherwise strictly *arr → app.                             | Must     |

> **Amendment (2026-07-06, ADR-023 / PLAN-006).** The Restore capability is **retired as an
> Admin nav item**: `/admin/restore` now redirects to `/trash` and the Recently-Deleted → Restore
> re-add re-homes into the **Trash** section (R-84). The underlying `restoreRouter` +
> `executeRestore` stay callable and unchanged (Trash's Restore reuses them); the diff/re-add UI
> re-home into the Ledger section is PLAN-005's scope.

### Ledger section (Phase 5)

| ID   | Requirement                                                                                                                                                                                                                                                                                                                                                                                     | Priority |
| ---- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| R-74 | A top-level **Ledger** section with Movie/TV/Music sub-tabs, spreadsheet-style over the high-powered filters (R-67..R-72 / DESIGN-008), listing **every** ledger row (live + tombstoned) with all harvested metadata.                                                                                                                                                                           | Must     |
| R-75 | From a filtered Ledger set, an authorized user can **bulk Add & search** the items in the matching *arr — absent items added monitored (recorded quality profile / root folder / tags), **present-but-unmonitored items set monitored**, then a search command triggered. _Generalizes R-50..R-52: Restore is the admin-only, diff-driven, searches-off special case of this action (ADR-022)._ | Must     |
| R-76 | The filtered Ledger set is **exportable to disk** as an emergency Radarr/Sonarr/Lidarr import list (deterministic JSONL) for catastrophic-failure recovery.                                                                                                                                                                                                                                     | Must     |
| R-77 | The prior "import the radarr fileless backlog as tombstoned rows" (draft R-56) is **reframed**: those titles already exist as live *arr rows, so the backlog is met by the Ledger filters (unmonitored + no-file + vote tiers) plus R-75's bulk action — **no import** (ADR-022 C-04). The _filterable state_ (on-disk none + unmonitored) is the **Fileless Set** (T-66).                      | Must     |
| R-78 | **Section-level role access**: each role has an access level per top-level section — **Edit** (full), **Read-Only** (browse/export, no mutations), **Disabled** (section hidden). Applies to Ledger now; Trash reuses it (PLAN-006). Server-enforced, not client-hidden only.                                                                                                                   | Must     |

> **Note (2026-07-06) — R-56 reframe (Fable 5).** Draft **R-56** proposed importing a 4,008-row
> "radarr fileless backlog" snapshot into the ledger as tombstoned rows. A live probe found all
> 4,008 ids already exist as live Radarr `media_items` rows (3,910 unmonitored, 3,971 no-file),
> with `imdb_votes` present for ~99%. The import is therefore **dropped**; its need is fully met
> by **R-74** (browse/filters), **R-75** (bulk Add-&-search), and **R-77** (the Fileless-Set
> filter state). See **ADR-022 C-04** for the evidence.

### Trash & retention (Phase 5.5)

Governed by **ADR-023** / **DESIGN-010** (PLAN-006). Maintainerr is the deletion system of record;
Trash is a read-through front-end + a confined write surface.

| ID   | Requirement                                                                                                                                                                                                                                            | Priority |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------- |
| R-79 | A top-level **Trash** section integrates Maintainerr (rules, pending-deletion collections, exclusions, deletion execution) and **replaces the Admin → Restore nav item**; `/admin/restore` redirects to `/trash`.                                      | Must     |
| R-80 | A **Rules editor** mapped to Maintainerr rule groups; role access is graded Edit / Read-Only / Disabled (section level) plus an explicit `edit_rules` action grant.                                                                                    | Must     |
| R-81 | **Movies** and **TV** pending-deletion tables (separate tabs, never combined), each row showing the scheduled-delete date + space it frees, plus a **total-space** figure for the whole set, filterable via the shared filter engine (DESIGN-008/009). | Must     |
| R-82 | **Save/whitelist** an item so Maintainerr never deletes it (add to its exclusion list); also a perma-save from the Library page (Movies + TV only, never music).                                                                                       | Must     |
| R-83 | **Expedite** deletion — the whole pending list or one item — destructive, confirmed.                                                                                                                                                                   | Must     |
| R-84 | **Recently Deleted** list (our tombstoned ledger rows) with **Restore** (reuses the failsafe re-add).                                                                                                                                                  | Must     |
| R-85 | **Fine-grained per-role permissions**: the whole tab is disablable (then hidden), and each user-action (Save, Expedite item/all, Edit rules, Restore) is an explicit per-role grant on top of the section level. Server-enforced.                      | Must     |
| R-86 | **Music is never deletable** via Trash (no Lidarr deletion surface); watched/requested media is auto-protected across all three Plex servers before it can be deleted.                                                                                 | Must     |
| R-87 | Before any destructive action is available, Maintainerr integration health + a safe, no-imminent-deletion state are **audited and surfaced**; destructive actions refuse on an unsafe verdict.                                                         | Must     |

#### Curation pipeline (PLAN-012 — ADR-025 / DESIGN-011)

Batches become the deletion unit: rules propose, an admin curates a poster wall, green-lighting
publishes a Plex **Leaving Soon** collection where users rescue titles within a window, and window
expiry deletes the survivors — every R-86/R-87 safety layer still applying.

| ID   | Requirement                                                                                                                                                                                                | Priority |
| ---- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| R-88 | A **batch** is the deletion unit: a frozen snapshot of the current pending set for ONE media kind (never mixed; never music), created on demand from the pending set; at most one open batch per kind.     | Must     |
| R-89 | An **admin poster review**: a phone-first poster wall where each item toggles X (will delete) ⇄ lock (saved) in place; every save/unsave is durably recorded (the rules-tuning dataset).                   | Must     |
| R-90 | A batch **never deletes without a green-light** (the human gate) — unless an **audited skip-gate** setting is on, which auto-green-lights and records `gate_skipped` with system attribution.              | Must     |
| R-91 | Green-light publishes a **Leaving Soon** Plex collection (driven through Maintainerr) that users can browse; a **role-gated** save exercise runs there within a **configurable window** (default 21 days). | Must     |
| R-92 | **Window expiry deletes the batch server-side, one item at a time**, re-running the live-exclusion + guardian + SAFE-audit checks per item at deletion time; every deletion audited.                       | Must     |
| R-93 | Every batch **state transition** and every **save/unsave** is durably recorded; per-deleted-item **size/resolution/rating snapshots** are captured at deletion time (PLAN-013 depends on this).            | Must     |
| R-94 | Batch management (create/green-light/cancel/expire) and the windowed user rescue are **distinct per-role grants** (`manage_batches`, `save_leaving_soon`), neither seeded; server-enforced.                | Must     |
| R-95 | Admin-tunable operational settings (skip-gate, default window) live in an **audited settings store**; every change writes an audit row.                                                                    | Should   |
| R-96 | Batch cadence / scheduled creation is **configurable** (manual-first v1; scheduled creation future).                                                                                                       | Should   |

#### Space & reclaim metrics (PLAN-013 — ADR-030 / DESIGN-013)

Governed by **ADR-030** / **DESIGN-013**. Make the space story measurable: current utilization per
media array against a **space target**, the fill/drain **trend** over time, and **reclaim attribution**
from the curation pipeline's deletions (R-93's snapshots). HYBRID surface — native in-app for
point-in-time utilization + reclaim, **deep-linked Grafana** for the historical trend. Consumes R-93;
does not change any deletion behavior. Native reads are admin-gated for v1.

| ID    | Requirement                                                                                                                                                                                                                                                                                                                                                                                                          | Priority |
| ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| R-108 | **Disk utilization per media array is visible** — current free/total + percent-used per physical array (HaynesTower, Music/CephFS), read from the *arr `GET /diskspace` API (the only source with a total). A downed *arr yields a partial result, never a broken page.                                                                                                                                              | Should   |
| R-109 | **Fill/drain trend over time** is surfaced so the owner can judge steady-state against the target ("draining toward the floor, or still filling?"). Sourced from the exportarr `*_rootfolder_freespace_bytes` series in Grafana, **deep-linked** (not embedded) so it survives the `haynesnetwork.com` cutover and stays excellent on a phone.                                                                       | Should   |
| R-110 | **Reclaim attribution** from the deletion snapshots (R-93): reclaimed bytes by **category** (Movies vs TV) AND by **resolution** (the bang-for-buck view — "3 × 4K = 90% of the reclaim"), a cumulative-reclaim curve, and a per-batch rollup attributed to the green-lighting admin, over a selectable window. Batch-swept deletions are the clean dataset; direct-expedites are a best-effort second-class series. | Should   |
| R-111 | A **configurable space target** (percent-used ceiling per Plex server, e.g. HaynesTower < 80%) lives in the **audited settings store** (R-95's `app_settings`); the metrics surface displays it as a reference line. This plan **stores + displays** the target; enforcement/action on it is a later plan (PLAN-014).                                                                                                | Should   |

### Space policy + rules tuning (PLAN-014)

> Close the space loop the owner described (2026-07-06): ACT on the space target (R-111) and the
> save-data (R-92) — but conservative-first. **The system proposes; the human disposes.** No autonomous
> deletion, no automatic rule edits; the save-data becomes tuning evidence, and the pipeline earns
> hands-off operation only against an explicit, evidence-backed bar.

| ID    | Requirement                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Priority |
| ----- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| R-112 | When a media array is **over its space target** (R-111), the system may **PROPOSE a deletion batch** for the kind(s) that array backs — it lands in **admin review** exactly like a manual batch and **never deletes on its own**. The behavior is **opt-in and default-off**, per-array, with an anti-spam cooldown; enabling it never bypasses the admin gate, the windowed sweep, or any R-88..R-91 safety layer. Each proposal records **why** (array, %-used, target, candidate count) in the activity feed. | Should   |
| R-113 | A **rules-tuning report** turns the pipeline outcomes (what the family **rescued** vs. what was **deleted**, R-92) into per-**resolution** / per-**rating-band** / per-**collection** save-rate stats — the labelled false positives the owner reads to tune the Maintainerr rules **by hand**. The report **never edits a rule automatically** (out of scope — this estate optimizes for trust).                                                                                                                 | Should   |
| R-114 | **Skip-admin-gate graduation criteria** (giving R-96's audited skip-gate teeth): the report surfaces a **graduation-readiness** readout against an explicit bar — enough completed policy-proposed batches, a low enough save-rate, and no restores of swept items — after which the **owner may** flip the audited skip-gate. Flipping it stays an owner action; the criteria and the live numbers are recorded.                                                                                                 | Should   |

#### Batch-lifecycle push notifications (ADR-034 / DESIGN-015)

> The owner acts on batches from his phone (2026-07-08): _"integrate pushover so I get a
> notification when a new batch is posted and the day it will leave — with a configurable time
> range the notification can be sent, like 6PM-10PM when I am home and can spend time saving
> items."_ Durable + disabled-safe: the push is enqueued in the same transaction as the state
> change, delivered by a periodic drainer, and no-ops cleanly when credentials are absent.

| ID    | Requirement                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Priority |
| ----- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| R-115 | The owner receives a **push notification** (Pushover) at the two moments a batch is worth acting on: **when a batch is posted** (manual OR space-policy-proposed — "New Movies batch: 17 items, 114 GB — review it") and **as it leaves** — a Leaving-Soon notice carrying the **deadline date** plus a reminder the **day before** expiry ("still slated — save the ones you want"). A swept-summary ping closes the loop. Each push **deep-links into the relevant Trash tab**. Enqueue is **same-transaction** with the batch transition (never lost, never phantom), and delivery is **disabled-safe** — with no Pushover credentials the sender no-ops and the build/app are unaffected. | Should   |
| R-116 | A **configurable delivery window** (start hour, end hour, timezone; default **6 PM–10 PM ET**) governs WHEN pushes arrive — a notification raised inside the window sends promptly; one raised outside is **held until the next window opens**; the day-before-leaves reminder targets that window on the day before expiry. The window is **admin-editable and audited** (the settings store), on the Storage admin page.                                                                                                                                                                                                                                                                    | Should   |

### Bulletin — Feed + Messages (Phase 2.5 / stretch)

Governed by **ADR-026** / **DESIGN-012** (PLAN-009). A top-level **Bulletin** section with two
sub-tabs: an aggregated third-party notification **Feed** and a user-driven **Messages** board. The
backend vertical (store + receiver + Feed/Messages API + permission model) ships first; the Feed +
Messages UI is a separate UX follow-up.

| ID    | Requirement                                                                                                                                                                                                                                                                                                               | Priority        |
| ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------- |
| R-97  | A top-level **Bulletin** section with **Feed** + **Messages** sub-tabs; role-gated visibility per the Section-Permission model (`bulletin` section). The Feed defaults **Read-Only for everyone** (a member reads it out of the box); Disabled hides the whole section.                                                   | Should          |
| R-98  | Inbound webhooks from **Seerr/Overseerr, Tautulli, and Maintainerr** are ingested into ONE durable `notifications` store and shown in the Feed; the Trash **Activity** tab is a `source='maintainerr'` view of the same store.                                                                                            | Should          |
| R-99  | The Feed is **filterable** (source, event type, media-link presence) and keyset-paginated (newest-first by source event time).                                                                                                                                                                                            | Should          |
| R-100 | Notification events are **attributed to app users** where the payload carries requester/viewer identity (Seerr requester email → user; Tautulli account email best-effort), reusing the single email-only attribution path; else shown **unattributed**. Events link to a ledger **Media Item** when tmdb/tvdb ids match. | Should          |
| R-101 | Ingest is **idempotent**: a `(source, source_event_id)` dedupe index makes webhook re-delivery a no-op.                                                                                                                                                                                                                   | Must            |
| R-102 | Users can **post durable Messages** (subject optional, body required), optionally **linked to a Media Item**; a Message **complements** (never replaces) the structured Fix flow. Authors may **edit their own** Messages.                                                                                                | Should          |
| R-103 | Moderators can **triage Messages** — hide / delete / restore (soft status transitions that **preserve content** for audit) with a moderation trail. **Read**, **post**, and **moderate** are distinct per-role grants (`bulletin` section level + `post`/`moderate` action grants); server-enforced.                      | Should          |
| R-104 | The webhook receiver (`POST /api/webhooks/<source>`) is **session-unauthenticated but per-source shared-secret-gated** and reachable only in-cluster (no public exposure; works before the R-64 public cutover).                                                                                                          | Must (security) |

### Metrics section (Phase 6)

Governed by **ADR-037** / **DESIGN-016** (PLAN-017). A top-level **Metrics** section (nav position after
Bulletin) with a sub-tab shell — an **Overview** ships now; Apps/Hardware/Network drill-ins follow in
PLAN-018/019/020. The estate's numbers come from the in-cluster `kube-prometheus-stack` Prometheus
(read-only), with Grafana deep-linked (never embedded) as the LAN power tool. Member-facing, so it carries
a per-role **Full/Limited** access model — distinct from the admin-only storage tooling (R-108..R-111).

| ID    | Requirement                                                                                                                                                                                                                                                                                                             | Priority |
| ----- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| R-117 | A top-level **Metrics** section (nav after Bulletin) with a sub-tab shell; you land on an **Overview** of the most useful numbers. Visibility is role-gated per the Section-Permission model (`metrics` section) and ships **Admin-only** — a role row opts other roles in. Apps/Hardware/Network sub-tabs are reserved for later plans (placeholders until they land). | Should |
| R-118 | Each role carries a **metrics access level** — **`full`** or **`limited`** (default role → `limited`; Admin implies `full`). `limited` **hides user-aware metrics** (who requested what, per-user space, any username-bearing label). Level flips are **admin actions, audited in the same transaction**, and enforced **server-side in the payload shape** (never client-hidden only). | Should |
| R-119 | Access-level scoping (owner ruling): **hardware is ungated** (both levels see cluster node load + memory); **network at `limited` is upload/download usage-vs-capacity ONLY**, with finer per-uplink performance at `full`; and **no device identities / "what is on the network" at ANY level**. | Should |
| R-120 | The **Overview** headline is **WAN upload/download consumption vs capacity** (the ~300 Mbps upload cap is the practical Plex-server limit), rendered with the storage meter idiom; plus a cluster load + memory tile and a storage-utilization snapshot (**reusing** R-108's `getUtilization`, not duplicated). The upload/download **capacity is admin-editable + audited** (the R-95 `app_settings` store; upload seeded 300 Mbps). Meters/tiles **update in place (no reflow, ADR-015)**, poll bounded, and **degrade to a note** when Prometheus is unreachable — never a broken page. | Should |
| R-125 | A **Metrics → Apps** sub-tab (governed by **DESIGN-018** / **OPS-008**, PLAN-018) rides the R-117 shell + the in-cluster Prometheus read path: a **curated, phone-friendly** view of the media-automation apps grouped into four panel groups — **Collection** (`radarr`/`sonarr`/`lidarr` library totals · monitored · missing · upgrades-pending), **Acquisition pipeline** (per-\*arr queue depth · grabs/hr · system-health issues), **Download clients** (SABnzbd `sabnzbd` vs `sabnzbd-fast` lane speed · 24h volume · queue, plus qbittorrent/slskd reachability tiles), and **Indexers/Prowlarr** (fleet enabled/unavailable · per-indexer avg response time · query rate). Each group carries a muted **"Open in Grafana ↗"** deep-link to its matching curated board (`d/arr-library-overview`, `d/downloads-clients-indexers`; LAN-only footnote — Grafana is the verbose layer). Tiles **update in place (no reflow, ADR-015)**, poll **bounded (stops when the sub-tab is inactive/hidden)**, and each panel **degrades to a muted "unavailable"** when its series can't be read — never a broken tab. | Should |
| R-126 | The Apps sub-tab inherits the R-118 **Full/Limited** model but is **visible at BOTH levels in full**: a label audit found **no \*arr/downloader series carries user/requester identity** (only `job`/`indexer`/`path`/`download_state` labels), so nothing here is user-aware. The level seam is nonetheless **kept plumbed** (the router shapes the payload by `effectiveMetricsLevel`; a full-only branch is **present-but-empty** today) so a future user-aware panel (e.g. Seerr requesters) slots into the full-only branch **without a refactor** — same never-fetch/never-serialize invariant the R-118 `network.wanLinks` seam holds. The **SABnzbd fast-lane split** frames *aggregate* human-request throughput (no individuals named) and stays both-levels pending owner veto (Q-01). | Should |
| R-127 | A **Metrics → Network** sub-tab (governed by **ADR-039** / **DESIGN-019**, PLAN-020) rides the R-117 shell + the in-cluster Prometheus read path with the R-118 **Full/Limited** model. **`limited`** sees ONLY the **WAN upload/download usage-vs-capacity** meters (reusing R-120's admin-editable capacity denominators — not duplicated) **plus a 7-day WAN throughput history sparkline** (its only value-add over the Overview). **`full`** ADDS **infrastructure-performance** groups — per-gateway/switch/AP CPU·memory·load, **WAN health** (gateway speedtest + internet-path latency), per-uplink caps, and site rollup COUNTS (APs/switches/gateways/connected-device count). Each group carries a muted **"Open in Grafana ↗"** deep-link (UniFi-Poller Network Sites / USW / UAP boards; the Client-Insights board is **deliberately not linked**). Tiles **update in place (no reflow, ADR-015)**, poll **bounded**, and each group **degrades to a muted "unavailable"** — never a broken tab. | Should |
| R-128 | **Hard privacy invariant (owner ruling, non-negotiable): NO client identities at ANY level.** No client hostname/MAC/IP, no per-client or per-device row, at `limited` OR `full` (a UniFi AP/switch/gateway **name** is infrastructure, not a client — allowed at `full`; the only client-adjacent number is the aggregate connected-device COUNT). This is enforced **by construction**: an **allow-listed PromQL module** (`network.ts`) is the single place any `unpoller` series is named and structurally never references `unpoller_client_*` / `_remote_user_` / `*_info`; a **unit test proves** every query matches the allow-list and none matches the deny substrings, and the `limited`/`full` payload is disjoint (`limited` never fetches or receives the infra grain). A reviewer must be able to open the Network tab at BOTH levels and confirm no client identity renders anywhere. | Must (privacy) |
| R-129 | A **Metrics → Hardware** sub-tab (governed by **ADR-040** / **DESIGN-020**, PLAN-019) rides the R-117 shell + the in-cluster Prometheus read path and showcases the homelab's physical capability and load: (a) an **NVMe endurance** panel — the headline — per-pool framed (the appdata **mirror** vs the expendable **staging** pool) with a wear odometer (`smartctl_device_percentage_used`), a wear-rate **projection** to 90% from Prometheus history (graceful **"insufficient history"** until it accrues), and the real end-of-life signals (`available_spare` vs its threshold, `media_errors`, `critical_warning` bits); (b) a **Drive health** table across every reporting SMART device (in-cluster NVMe + the NAS pool) with a healthy/warn/fail pill, temperature, wear, power-on hours, media-error count — a sleeping array disk that emits **no series is shown "asleep", never red**; (c) **Node load** per node (load-per-core, memory used/installed, hottest temperature); and (d) a **Proxmox showcase** — host tiles (CPU/mem/uptime) each expanding **in place** (ADR-015 allowed exception) to their VMs. Each group carries a muted **"Open in Grafana ↗"** deep-link (the Unraid NAS board, node-exporter, smartctl boards). **Ungated (owner ruling): both `full` and `limited` see everything** — hardware is not user-aware. Tiles **update in place (no reflow, ADR-015)**, poll **bounded (stops when the sub-tab is inactive/hidden)**, and each group **degrades to a muted "unavailable"** — never a broken tab. | Should |
| R-130 | **SMART health alerting (owner ruling — critical-only, transitions-only).** The app pushes a phone alert (via the R-115 `notification_outbox` + R-116 delivery window) **only on a critical transition since the last check**: `smart_status` pass→FAIL, `media_errors` 0→n, `available_spare` crossing its threshold margin, a **NEW** `critical_warning` bit, or the **critical appdata pool** wear crossing **80%** or **90%**. A drive's known state at first sight (the expendable staging pool is already past 100% wear with `critical_warning` bit 2 set) is **recorded as a baseline and NEVER pages** — only NEW deterioration on those drives does. Temperature + warn-tier stay **dashboard-only**. Detection runs in a **`smart-alerts` sync mode** (following the `notify-outbox`/`space-policy` CronJob-mode precedent — no `sync_runs` row); the per-drive last-known state persists in a **`smart_drive_state`** table written **only** by a `@hnet/domain` single-writer that enqueues the outbox row in the **same transaction** as the state update (the outbox row IS the durable transition record). Delivery is **disabled-safe** — absent `PUSHOVER_*` env, the enqueue still records the transition and the drainer no-ops. | Should |

### Library — ytdl-sub content (Phase 6)

Governed by **ADR-038** / **DESIGN-017** (PLAN-022), refined by **ADR-041** + DESIGN-017 D-07..D-09
(the owner's 2026-07-10 morning-review UX package: R-131 poster streamlining, R-132 drill-in + tab
order). Two new **Library** sub-tabs — **Peloton** and
**YouTube** — surface the k8plex (HAYNESKUBE) **ytdl-sub** libraries (`HOps Peloton` / `HOps YT`), read
**directly** from the Plex server via `@hnet/plex` (no ledger sync — this content has **no \*arr**, so
hard-rule 4 is N/A; Plex is the source of record). Ships **Admin-only** for a morning screenshot review,
then opens per role. Reuses the Library look (poster grids, the R-70 filter engine, `MediaPoster`).

| ID    | Requirement                                                                                                                                                                                                                                                                                                             | Priority |
| ----- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| R-121 | The two k8plex **ytdl-sub** libraries (Peloton, YouTube — "TV Show by Date" libraries) are browsable as **poster grids** under new **Library** sub-tabs, read **directly** from the Plex server via `@hnet/plex` (`/library/sections/{key}/all`). This content is **not synced** into `media_items`/ledger (it has no \*arr; hard-rule 4 does not apply). Sections are resolved by **library title** (not a hardcoded id); a library the server doesn't report shows a clean **empty-state**, never a crash. | Should |
| R-122 | Visibility is role-gated by a new **`ytdlsub` Section-Permission** (the R-63 model) with a **`disabled`** no-row default, so the sub-tabs ship **Admin-only** — a role row opts other roles in. Gating is **server-authoritative**: a non-permitted caller sees **neither** the tabs (route resolves the level server-side) **nor** the data (the read procedures + the poster proxy reject them). The owner flips visibility **per role** in the existing role editor after his review. | Should |
| R-123 | The grids reuse the existing Library idioms — the `.library-tabs` sub-tab grammar, the **poster grid** + `MediaPoster`, and the R-70 `@hnet/ui` filter/sort engine (client-side over the small show set). Posters stream through an **authed Plex-thumb proxy** (extending the R-69 ADR-019 pattern; session- + `ytdlsub`-gated, the Plex token never reaches the browser) with a **graceful fallback tile** when Plex art is missing. Interactions are **reflow-free (ADR-015)**, reads are **bounded**, and an unreachable k8plex **degrades to a note** — never a broken page. | Should |
| R-124 | (Durable posters — resilient now, durable **deferred**.) The owner keeps **losing posters** for the time-based "show" series and has replacements on hand. The display is made **resilient** now (proxy + fallback tile), but the **durable poster store** (a repo, a PVC, or a Kometa-style overlay re-applied after each ytdl-sub write; serve-vs-reapply) is **deferred to Q-06** — it depends on where the replacement files live. No poster-override table is built this round. | Could |
| R-131 | (Owner review 2026-07-10, **ADR-041**.) The ytdl-sub walls must **paint like the Movies\|TV\|Music walls**: the Plex-thumb proxy serves **grid-sized WebP variants** via k8plex's own photo-transcode endpoint (an **allow-listed** `size=grid\|still` — never arbitrary dimensions), memoized in an **in-process byte-capped LRU** (not a store — nothing persisted, ADR-019 posture intact) with a **strong ETag** keyed on the self-versioning thumb path (`…/thumb/{lastWrite}`) so repeat visits are 304s. A transcode miss falls back to the **original art**, then the fallback tile — never a broken wall. Tiles **fade in over the reserved 2:3 box** (reflow-free, ADR-015). Measured: the Peloton wall drops 29.3 MB → 46 KB, YouTube 6.9 MB → 856 KB. | Should |
| R-132 | (Owner review 2026-07-10.) A ytdl-sub poster tile is a **click-through to a read-only drill-in** — show → seasons (collapsible, the `/library/[id]` season grammar) → episodes (title, air date, duration, a small 16:9 still via the `size=still` proxy variant), read live from k8plex via `@hnet/plex` children reads, **lazily per expanded season**. Same server-authoritative `ytdlsub` gating as the walls, plus **section confinement**: a ratingKey outside the two resolved ytdl-sub sections renders the not-found state (no cross-library probing). **No ledger involvement, no actions, no write surface.** Library tab order is **Movies \| TV \| Music \| Peloton \| YouTube \| My Fixes** (My Fixes last). | Should |

### Authentik hardening — config-as-code + native MFA (Phase 6)

Governed by **ADR-042** / **OPS-009** (PLAN-011, executed 2026-07-10). The Authentik login estate
(brand, flows, sources, and native-account MFA) is managed as **config-as-code blueprints** in the
`haynes-ops` GitOps repo, and **native (internal-type) accounts** now carry **MFA on the
username+password path**. This does **not** change hard-rule 5 (Authentik OIDC is still the sole
sign-in method) — it hardens the credential surface *inside* Authentik and does not add a password
surface to this app.

| ID    | Requirement                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Priority |
| ----- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| R-133 | **Native-account MFA.** Every native (internal-type) Authentik account — `thaynes`, `akadmin`, any hand-created local user — must present a **WebAuthn passkey OR a TOTP code** on the **username+password** login path, enrolling a factor on first challenge (`not_configured_action: configure`; device classes `[totp, webauthn]`). The owner's primary factor is a **1Password passkey** on `thaynes` with a **TOTP backup**; enrollment is an owner-present action. No session is issued on the password path without a factor (proven on a throwaway account). MFA is enforced by the `40-hnet-mfa` blueprint's order-30 authenticator-validation stage of `default-authentication-flow`. | Must     |
| R-134 | **Plex-source pass-through — never double-gated.** "Log in with Plex" accounts (and even a native user who signs in via the Plex button) traverse `default-source-authentication`, a **login-only** source flow with no password/MFA stage, so this app **never** applies a second MFA gate — their MFA posture is whatever their Plex account enforces. **Owner ruling (2026-07-10):** `thaynes`' own Plex-source login path is accepted as-is (Plex 2FA covers it); native MFA guards the **password path only**. Verified live: a Plex-source login sees no challenge. | Must     |
| R-135 | **MFA exemption group (automation).** An **`mfa-exempt`** group (members `hnet-e2e`, `hnet-e2e-member`) **skips** the MFA challenge so the Playwright automation and other non-interactive accounts stay green. The skip policy is **fail-closed** — an unresolved pending user enforces MFA rather than skipping it — and the exempt policy-binding is `failure_result: true` (a policy error enforces, never exempts). ak-outpost service accounts run no interactive flow and are unaffected. This is the only sanctioned exception to R-133. | Must     |
| R-136 | **Authentik config-as-code (GitOps).** The login estate is expressed as **Authentik blueprints** in `haynes-ops` (one file per concern — brand, flows, sources, MFA), delivered via a kustomize `configMapGenerator` → chart `blueprints.configMaps` → worker mount + discovery, and reconciled by Authentik. The baseline is **drift-zero** (reproduces the live estate with no observable change on apply) so it is a faithful, reviewable, revertible mirror; MFA rides on top of it. The OIDC provider/application stay API-managed for now (Q-11); provider secrets stay in 1Password. Rollback is a commit revert plus a fast live `ak`-shell unbind. | Must     |

### Peloton poster guard (PLAN-024)

(The owner's durable Peloton override posters — the PLAN-022 Q-01 / Q-06 tail. ADR-043 / DESIGN-021 /
OPS-010; glossary T-124..T-125.)

| ID   | Requirement                                                                                                                                                                                                                                        | Priority |
| ---- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| R-137 | **Durable Peloton posters survive re-scans.** The owner's override art for the k8plex **HOps Peloton** shows (class-type series art) and seasons (per-duration "N MINUTES" art) must be **kept applied** despite `peloton-config-manager` re-downloads / Plex re-scans. Realized by a `poster-guard` sync mode that re-applies drifted posters on the server (not just in-app), so native Plex clients see them too. The durable assets are **versioned in the app repo/image** (ADR-043 — not a ConfigMap, not DB `bytea`, not an NFS mount); the mapping resolves by **live show title + season index** so it survives Plex re-indexing. | Should |
| R-138 | **Re-apply only on drift; never destructive.** The guard re-applies a poster **only** when the live art differs from the baseline it recorded at last apply (or the mapped asset's bytes changed, or it was never applied) — a steady state does nothing. It **never deletes** gallery art (`POST …/posters` selects the new poster; Plex keeps the prior one in the item's gallery), so every apply and the one sanctioned drift-test are reversible. | Should |
| R-139 | **Auditable + single-writer.** Each re-apply appends one row to an **append-only `poster_guard_applications`** ledger (the drift baseline + `reason` ∈ initial/drift/asset-updated + previous thumb), written **only** by a `@hnet/domain` single-writer in the same transaction (guard-listed). Like the `smart-alerts` mode it writes **no `sync_runs` row** (the ledger is its trail); the poster upload is a **confined** Plex write (`@hnet/plex` write subpath, domain-only — ADR-017). | Should |
| R-140 | **Bounded + unmapped-safe.** The guard is bounded (~1 section list + one children read per show ≈ 14 reads, writes only on drift) and runs **hourly-or-less** as a haynes-ops CronJob mirroring `sync-smart-alerts`. Targets with **no mapping** (an unknown show, a season index with no asset — e.g. `0` Specials, `75`) are **reported, never guessed**; a mapped asset missing from the image is reported and skipped, not fatal. | Should |

#### AI usage metrics (PLAN-021 — ADR-044 / DESIGN-022)

(A usage view distinct from the PLAN-019 hardware/perf metrics: how much the self-hosted AI — Open WebUI
— is being used. ADR-044 / DESIGN-022; migration 0035; glossary T-126..T-128.)

| ID   | Requirement                                                                                                                                                                                                                                        | Priority |
| ---- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| R-141 | **AI usage sub-tab.** A **Metrics → AI** sub-tab rides the R-117 shell and surfaces **Open WebUI usage over time**. The data is **synced from Open WebUI's admin API** (`OPENWEBUI_API_KEY`; `GET /api/v1/chats/all/db` + `/api/v1/users/`) by an **`ai-usage-sync`** sync mode into an app-owned mirror (`ai_usage_chats`) — the *arr-ledger pattern, **never a live cross-DB read** and **read-only against Open WebUI** (ADR-044 C-01/C-04). Tiles + per-day **sparklines** (chats, image generations), a range control (7/30/90d/all); tiles update **in place (no reflow, ADR-015)**, poll **bounded**, and degrade to a muted note — never a broken tab. Ships **Admin-only by default** like the other metrics tabs (owner opens it via `/admin/roles`). | Should |
| R-142 | **Aggregate-for-everyone.** A viewer at the **`limited`** metrics level sees **aggregate-over-time counts only**: **# chats** and **# image generations** (+ # messages) and their trend. **No user identity** is present in a `limited` payload — the user-aware-metrics gating rule (ADR-037) — the server omits the per-user/model keys and the active-user count entirely (server-authoritative, not client-hidden). An **image generation** is counted from an **assistant-message image file** (ADR-044 C-02) — user uploads never inflate it. | Should |
| R-143 | **Admin sees the backend detail.** A **`full`/admin** viewer additionally sees **who used it, how long, and for what model**: a **by-model** breakdown and a **by-user** table (chats, image generations, total time, models) plus the distinct-active-user count. This per-user attribution is **full-only** (ADR-044 C-03) — proven by unit + tRPC tests. If Open WebUI can't supply a field, the surface **degrades gracefully** (attribution unresolved that cycle; counts still shown) rather than blocking. | Should |

### Authentik role portal (PLAN-026 — ADR-045 / DESIGN-023)

(haynesnetwork becomes the **role portal** for the whole Authentik ecosystem: assigning a Role writes
the matching **Authentik group membership**, which propagates to every Authentik-backed app — Open WebUI
today, Kavita/Audiobookshelf next. The BC-04 Plex-sharing pattern applied to identity; delivers the R-30
"push permissions into Authentik" intent. ADR-045 / DESIGN-023 / OPS-011; migration 0036; glossary
T-129..T-135.)

| ID   | Requirement                                                                                                                                                                                                                                        | Priority        |
| ---- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------- |
| R-144 | **Single-pane user/role management.** An admin manages **every Authentik identity** — including external Plex-source and never-logged-into-this-app accounts — from one `/admin/users` roster, reading a synced **directory mirror** (`authentik_users`) of the whole Authentik directory. Each identity carries a **source badge** (Plex-external / native / service-account) and its current app role (or a parked pending role). This is the single pane across every Authentik-backed app (Open WebUI today, Kavita/ABS later) — no more per-app hand-editing. | Should          |
| R-145 | **Write-back group membership on role assignment.** Assigning a Role writes the identity's **Authentik group membership** (the cross-app role primitive, delivered over the OIDC `groups` claim). Assignment is **exclusive across owned tier groups** — the subject joins its role's group and **leaves every other owned group** (e.g. Friends ⇒ in `friends`, out of `family`). The mutating surfaces (`@hnet/authentik/write` group create + membership, `@hnet/openwebui/write` group pre-create) are **import-confined to `packages/domain`** exactly like `@hnet/plex/write` (ADR-017) — no other code path may flip a membership. Assignment is **enforcement, never decision**: the Role (BC-02) decides; the client applies. | Should          |
| R-146 | **Synced-tier opt-in + auto-create.** A Role opts into projecting to a group via a **`synced_tier`** flag (the seeded **Family** role is backfilled on; Admin/Default stay app-local). Creating or flipping on a synced tier **auto-creates** the Authentik group (name = role name lowercased) **AND** ensures the same-named **Open WebUI** group exists (OWUI deliberately does not auto-create groups from claims — the portal pre-creates the claim's target). The tier's **existence** propagates automatically; per-app **entitlements** (OWUI model access, Kavita libraries) stay per-app config — the creation UX states this. Idempotent (ensure-exists). | Should          |
| R-147 | **Owned-groups guardrail — never touch admin-managed groups; no deletion.** The app writes membership **only** for a group on a positive **owned-groups allowlist** (`app_settings` `authentik_owned_groups`, ships `['family']` + auto-created tiers); `assertGroupOwned` throws (→ FORBIDDEN) **before any external call**. The app **NEVER** creates/modifies/deletes Authentik **flows, stages, policies, providers, brands**, or the `authentik Admins` / `mfa-exempt` groups — those stay owned by the ADR-042 blueprints and the admin. **Group deletion is out of scope**: deactivating a tier only removes its group from the allowlist (stops managing it) — **non-destructive**, the group and its memberships are left intact. | Must (security) |
| R-148 | **Pending assignment for Authentik-only identities.** Assigning a role to an identity with no app row (someone who only ever logged into OWUI/Kavita) writes the group membership **immediately** and **parks** a `pending_role_assignments` row, **consumed lazily on that identity's first haynesnetwork login** (the Better Auth `session.create.after` hook applies the app role + stamps the row consumed in one transaction — the same thin-hook / guarded-writer split as bootstrap-admin). The join key is **email** (case-insensitive) because the OIDC `sub` is a `hashed_user_id` the app can't pre-compute (the same email-match precedent as Plex accounts, ADR-017). | Should          |
| R-149 | **The `authentik-users` read sync.** A one-way, read-only **`authentik-users`** sync mode pages the live Authentik directory (`GET /api/v3/core/users/` incl. external + never-logged-in identities, `.../groups/`) and upserts the `authentik_users` mirror the roster reads (the *arr-ledger pattern). Like `ai-usage-sync` it writes **no `sync_runs` row** — the mirror is its trail — and runs as a bounded haynes-ops CronJob. The portal **also re-reads live** after a membership write so the roster reflects a change immediately. **Read-only against Authentik** (never mutates directory structure). | Should          |
| R-150 | **Dedicated service-account credential — never the homepage token.** The app authenticates to Authentik as a dedicated **`hnet-portal`** service account with its own API token, delivered as the **cluster-created** secret `haynesnetwork-authentik-token` (the `haynesnetwork-webhook` precedent; a 1Password migration is a follow-up nicety). It **never** uses the shared homepage admin token. If Authentik RBAC can scope the token to (user read, group read, membership write) that minimal scope is used; otherwise a full token is used — the as-executed scope is recorded in OPS-011. The token is header-only, never echoed. Reached **in-cluster** at `authentik-server.network.svc.cluster.local` (bypasses the Cloudflare 1010 UA ban). | Must (security) |

### Books & Audiobooks — Library ledger (PLAN-023 — ADR-046 / DESIGN-024)

| ID    | Requirement | Priority |
| ----- | ----------- | -------- |
| R-151 | **Books in the ledger (owner ruling — full ledger integration in v1).** The app maintains a one-way SYNCED MIRROR of the two book servers — **Kavita** (Books=EBooks + Comics) and **Audiobookshelf** (Audio Books) — as first-class Library content. Kavita/ABS are the **source of truth for book media** (CLAUDE.md hard rule 4 EXTENDED to a new server class); the sync flows strictly IN and there is **NO write-back** (no Fix, no Restore, no add — those are \*arr-only). Books live in a **dedicated `books_items` table**, NOT `media_items` (ADR-046 — books have no monitored/quality/root-folder/Fix semantics; overloading the \*arr-shaped ledger would corrupt its invariants and drag books into the /ledger admin's nonsensical Fix/bulk-add surfaces). | Should |
| R-152 | **`books-sync` ingestion — read-only, bounded, tombstoning.** A **`books-sync`** sync mode pages Kavita (`/api/Series/all-v2` per Books/Comics library) + ABS (`/api/libraries/{id}/items`) READ-ONLY via a new read-only **`@hnet/books`** client (lazy login + token cache + 401 re-auth; **no write surface**), normalizes each series/item, and UPSERTS the snapshot into `books_items` via the `@hnet/domain syncBooks` single-writer, TOMBSTONING rows no longer served (`deleted_at` set — never hard-deleted). Tombstoning is **scoped to fully-synced sources**, so a partial run (one server down) never wrongly tombstones the source it couldn't read. Like `ai-usage-sync` it writes **no `sync_runs` row** (the mirror is its trail) and runs as a bounded haynes-ops CronJob; a run where NEITHER server could be fully read exits nonzero (visible in the CronJob history). | Should |
| R-153 | **Library Books/Audiobooks/Comics walls.** Three new **Library** sub-tabs render poster-card grids over `books_items`, reusing the existing poster-grid + `@hnet/ui` filter/sort ENGINE + `MediaPoster` idioms (a leaner books-specific `books.search` — title/author query, per-medium sort, offset paging — over its OWN contract, not the \*arr-shaped D-09 wire). Tab order is **Movies · TV · Music · Peloton · YouTube · Books · Audiobooks · Comics · My Fixes** (My Fixes LAST; the Books tabs sit after YouTube). Rows **deep-link OUT** to the item in Kavita/ABS (public URLs, new tab) — books have no in-app detail page. Reflow-free (ADR-015), no new hex (hard rule 2). | Should |
| R-154 | **Authed cover proxy.** Book cover art is served through a session- AND `books`-section-gated **`/api/books/cover`** proxy that streams the Kavita series-cover / ABS item-cover with the credential applied **server-side** (apiKey query param / bearer header — never the browser; ADR-019 posture), keyed by a closed `source` enum + a format-validated `id` (not an open image proxy), with a strong `(source, id, coverVersion)` ETag/304. A miss → the KindIcon fallback tile. No image storage, no transcode (covers are already thumbnail-sized). | Should |
| R-155 | **Ship Admin-only, owner opens per role.** The three walls gate on a new **`books` Section-Permission** defaulting to **`disabled`** — at deploy only Admin sees them; the owner opens the section to a role (`read_only`) via the existing `/admin/roles` editor after a 390px + desktop **screenshot review** (the ADR-037/038 rollout). Server-authoritative (AC-13) — a non-permitted caller gets neither the tabs, the data (FORBIDDEN), nor the covers (404), even calling directly. | Should |
| R-156 | **Catalog cards for the two servers.** **Kavita** (`https://kavita.haynesnetwork.com`) and **Audiobookshelf** (`https://audiobookshelf.haynesnetwork.com`) are seeded as `app_catalog` cards (ADR-013 arbitrary http(s) URLs; new `kavita`/`audiobookshelf` icon keys). No role grants are seeded — Admin sees them implicitly; the owner grants them to Default/Family via `/admin/roles` after review (adding a new app needs NO schema change — ADR-012). | Should |
| R-158 | **Season posters in season rows + TV episode thumbnails (PLAN-030; ADR-048 / DESIGN-005 D-22, DESIGN-017 D-09).** Every Season row on a show-detail page shows the season POSTER as a small icon (reserved dimensions, ADR-015 reflow-free; a season with no art keeps the pre-030 no-icon row) — for **TV** from the season art of the exact Plex title the ledger item matched to (ADR-047 `media_plex_matches` → the show's ratingKey → its season children), for **Peloton** from the live k8plex season art (the restored PLAN-024 5/10/…/120-minute duration posters, now VISIBLE in the app). A TV show's expanded season rows gain **episode thumbnails** too — the SAME 16:9 `still` treatment the Peloton/YouTube drill-in already has (ADR-041 `still` variant), from the Plex episode art via the match, merged onto the *arr episode row by `(season, episode)` number. ALL art goes through the authed transcode proxy; the TV art path is a **signed, item-scoped** `/api/library/plex-art` reference so it **respects THE INVARIANT** (R-157): season/episode art for a title in a Plex library the caller can't access is NEVER served (the proxy verifies the item-bound signature AND re-checks per-item access — a caller can't fetch an inaccessible sibling title's art). One art SOURCE (Plex via the match — **no** TMDB re-fetch); an unmatched TV show (in the *arr, not yet in Plex) shows no season icon (no fallback); Specials/Season-0 with no art → no icon. Read-only. | Should |
| R-159 | **Roles-grid clarity + Bulletin Feed/Messages visibility (PLAN-027; ADR-049 / DESIGN-004 D-18, DESIGN-012 D-09).** (a) `/admin/roles` stops offering no-op permission levels: a per-section **capability map** (derived from the gating code) renders a 2-state **Enabled / Disabled** control for sections with no meaningful Edit (**Bulletin, Metrics, ytdl-sub, Books**) and keeps **Edit / Read-only / Disabled** only where a real `edit` rung exists (**Ledger** = bulk monitor-and-search; **Trash** = Maintainerr rule editing + Trash settings). The stored `SECTION_PERMISSION_LEVELS` enum + DB values are UNCHANGED (Enabled persists `read_only`); a section that later gains a real Edit flips one map entry. (b) The **Bulletin** section's two views — **Feed** and **Messages** — are **separately grantable per role** via a new `role_bulletin_view_grants` table (row-per-view, single-writer `setRoleBulletinViews`, audited `update_bulletin_views` in-tx, guard-listed; migration 0039), surfaced as **[Feed] [Messages] checkboxes** under the Bulletin dropdown (greyed when Disabled). Enforced **server-side** (AC-13): `communication.feed` requires the `feed` grant, `communication.messages.*` the `messages` grant — a role without a view gets FORBIDDEN (not a UI hide) and sees no such sub-tab. **Resolution is default-ON** (no view rows ⇒ both views — "Bulletin is for everyone", ADR-026 C-02; present rows narrow; Admin implies both), so only the **Default** role is narrowed (seeded `messages`-only by 0039 — the Feed is Family/Friends-oriented); Family/Friends/custom keep both, none silently lose the Feed. Reflow-free (ADR-015), no new hex (hard rule 2). | Should |
| R-160 | **Helpdesk rebrand + tab order + poster-wall UX (PLAN-034; ADR-050 / DESIGN-012 D-10..D-12).** The Bulletin **Messages board becomes the "Helpdesk"** — a household **media-issue ticket system** (report problems with media or playback; SITE bugs go to GitHub and the intake copy says so — the MOTD already links it). The Helpdesk is the **FIRST** tab under Bulletin (Feed second); the retired `?tab=messages` deep-links alias to it. Tickets render as a **poster wall** in the Library/Trash grammar: a linked title's poster (ADR-019 authed proxy) or — for non-media tickets — an **intake-driven category icon** (playback/audio/subtitles/quality/missing/other) in the same 2:3 tile, with the STATE **baked onto the tile** (a colored corner puck + status badge, the Trash-wall idiom), reply count, and last-activity time; the wall sorts by last activity. **Compose NEVER stacks above the list**: a "New ticket" button opens a multi-field Modal (title, category icon-grid, optional linked-title search, details) — mobile-first, clean at 390px, both themes; success lands on the new ticket's detail page. The display name is ONE string constant (`HELPDESK_NAME`) — a Helpdesk↔Tickets rename touches no stored value. | Must |
| R-161 | **Ticket state machine + full transition history, staff-driven (PLAN-034; ADR-050).** Every ticket carries a state: **`open → in_progress → complete \| rejected`** — `open ⇄ in_progress` move freely, either closes to `complete` (TERMINAL — a recurrence is a new ticket) or `rejected` (**re-openable** to open, the old hide/restore analog); illegal edges (incl. anything out of `complete`) are refused server-side (CONFLICT) BEFORE any write. **Only staff transition** — the existing `moderate` message-action grant (Q-02); members never drive state, not even on their own tickets. EVERY transition may carry an optional **household-visible reason/comment**, and every one (creation included, as `null → open`) appends an **append-only `ticket_events`** history row (actor, from/to, note, when) — the detail page's timeline is a total record. Today's "Triage" concept is absorbed by `in_progress`. | Must |
| R-162 | **Threaded replies, open to the household (PLAN-034; ADR-050, Q-02).** Every ticket carries a flat reply THREAD (GitHub-issue style): **ANY member with the Bulletin `messages` view grant may reply** on ANY ticket — no post/moderate grant needed (staff ask for info, the reporter answers, others chime in). Tickets are **household-visible** (Q-01): everyone with the view sees ALL tickets and their full timeline/thread — no hidden rows, no moderator-only fields. Replies bump the ticket's wall position (`last_activity_at`, same tx) and are immutable v1. Create = the existing `post` grant. Zero new grant tables — the ADR-026/049 machinery carries the whole matrix. | Must |
| R-163 | **Ticket detail drill-in + state filters (PLAN-034; ADR-050 / DESIGN-012 D-12).** Clicking a ticket opens a rich detail page (`/bulletin/ticket/[id]`, the movie-detail idiom): hero with poster/category tile, title, state + category badges, the linked title's static repair cue (open fix / repairs recorded) and "open in Library" deep link, the report body, the **History timeline** (Filed + every transition with actor/when/reason), the reply thread + composer, and — for staff — the transition buttons the CURRENT state allows, each through a Modal carrying the optional reason (ADR-014; never window.confirm). The wall's filters are the **STATES** (All · Open · In progress · Complete · Rejected, counts baked in) — replacing All/Visible/Hidden/Deleted. Navigation per DESIGN-004 D-19: tab switches + drill-ins push history entries; state chips replace. | Must |
| R-164 | **New-ticket admin ping + the test-data drop (PLAN-034; ADR-050 / DESIGN-012 D-13, Q-03/Q-04).** Filing a ticket enqueues ONE `notification_outbox` row (new event type `ticket_created`, payload: ticket id/title/category/author/linked title) **in the SAME transaction** as the ticket + creation-event inserts (ADR-034 C-01 — no phantom ping, no silent ticket), delivered to the admins by the existing `notify-outbox` Pushover CronJob with a deep link to the ticket; requester-facing pings wait for email (PLAN-035). Migration 0040 **DROPS the `messages` table** — its rows were owner-ruled TEST DATA, not household history; after deploy a few realistic example tickets (real library titles, varied states, a staff reply + resolution comment) are filed through the app's own writers and LEFT in prod as onboarding examples. | Must |
| R-157 | **Access-aware "Watch/Listen/Read here" deep links (PLAN-028; ADR-047 / DESIGN-025).** A Library poster ALWAYS opens the item's DETAIL page (never a wall jump-out); the detail carries the app-specific PRIMARY deep link(s) with an external-jump ↗ affordance — for Movies/TV/Music **one "Watch on Plex — <library>" button per Plex library the caller's role can access** (a title mirrored in several libraries gets several, each gated independently); books/audiobooks/comics get a NEW lean in-app detail page with **"Read in Kavita" / "Listen on Audiobookshelf"** (from `books_items.deep_link_url`; no Fix/Force-Search). Plex links resolve via a **`plex-match`** sync that matches each \*arr ledger `media_item` to ALL its exact Plex `{library, ratingKey}` tuples by shared GUID (tmdb/imdb/tvdb/musicbrainz), stored in a rebuildable **`media_plex_matches`** cache — one row per `(item, library)` (single-writer `syncPlexMatches`, guard-listed; run by a haynes-ops CronJob). A present-but-not-yet-in-Plex item gets NO link (never hidden by match failure). **THE INVARIANT (Must, security):** a user must NEVER receive — in any tRPC/API payload — a Library item that lives in a Plex library their role cannot access. Enforced SERVER-SIDE (never UI filtering) by reusing the ADR-024 effective-library resolver: gate ALL Plex-backed content — Movies/TV/Music (item→Plex library; unmatched items gated by their kind's home library), Peloton/YouTube (k8plex per-library grant) — across search / detail / events / children / wanted / filterFacets / admin browse+count / export AND the poster proxy; a fully-withheld library's tab hides entirely. Books/audiobooks/comics stay gated by their `books` section grant (R-155) — this adds only their deep-link button, no new gating. A deliberate, owner-approved tightening: non-admin Library visibility now follows the role→library grant matrix (admins unrestricted). | Must (security) |

### Library views, grouping & sorting/filtering (PLAN-029 — ADR-051/052/053 / DESIGN-026)

| ID | Requirement | Priority |
| --- | --- | --- |
| R-165 | **Selectable per-tab Library VIEWS with opinionated defaults (owner ruling R2).** Each Library kind tab renders its wall in one of three **view shapes** — **flat** (the current poster grid), **grouped** (aggregate cards keyed by a dimension → drill-in), or **hierarchy** (TV's existing Shows → Seasons → Episodes, UNCHANGED) — with a **view selector** to switch shapes. Opinionated defaults per tab: **Movies** flat · **TV** hierarchy · **Music** flat (Artists) · **Peloton** grouped-by-Exercise · **YouTube** grouped-by-Channel · **Books** grouped-by-Author · **Comics** grouped-by-Series · **Audiobooks** grouped-by-Author. TV is NOT flattened — it already embodies the hierarchy (owner clarification). Which ALTERNATIVE shapes a wall offers beyond its default is a build-phase UX call; the defaults here are fixed. Reflow-free (ADR-015). | Should |
| R-166 | **Group-view aggregate cards (owner ruling R2).** A grouped wall renders one **aggregate card per group key** (author / series / channel / discipline) showing the group label, an item **count**, and a small **stacked-cover** motif; tapping a card drills into the same wall in flat view pre-filtered to that group (a `?group=` URL segment; a screen-level PUSH per DESIGN-004 D-19). Grouping is queries + view shells over the EXISTING engines (no new store) — the group keys already exist and are live-verified populated where used (author 96–100% on the author-grouped walls). Aggregates + covers pass the ADR-047 access gate; an all-withheld group does not appear. Reserved dimensions, KindIcon fallback (ADR-015). | Should |
| R-167 | **Per-view, per-engine sort/filter REGISTRIES — different views expose different dimensions (owner ruling R5, "Episodes ≠ Shows").** Sort/filter capability is DECLARED per (wall, view-level) in a registry, resolved against the view's backing engine — the **three-engine seam**: the **ledger** engine (Movies / TV-Shows / Music, extending the DESIGN-008 D-09 `SORT_SPECS`/`buildLibraryWhere` in place, keyset cursor unchanged), the **live-Plex** engine (TV Seasons/Episodes, Peloton, YouTube), and the **books** engine (Books/Comics/Audiobooks, extending the ADR-046 `books.search`/`books.filterFacets`). Plex's asymmetry is honored — a level offers ONLY the sorts/facets it can answer (no Resolution/Duration at Season; no Runtime on Music; per-episode Air Date at Episode; a "Last Episode Added" rollup at TV Shows). A rich facet set (R5/R8): year/decade, rating thresholds, **genre chips everywhere feasible (the shipped `books.filterFacets` finally gets UI)**, author/series, narrator (audiobooks), format (epub/cbz) + length buckets, and per-user watch/read-state (R-170) — each populated-value-gated so an empty facet shows no dead chip. An **A–Z letter jump bar** on big walls sorted A–Z. | Should |
| R-168 | **Date Added + Date Released as peer dimensions everywhere feasible (owner ruling R5 must-haves).** Both must appear as sort + filter dimensions where the data supports them. **Date Added** already exists everywhere (live-verified 100% — `arr_added_at` / Plex `addedAt` / `source_added_at`; already a sort field) — a pure surfacing task. **Date Released** is the real gap: a new canonical **`released_at`** timestamp is added to the ledger walls via sync (Radarr `digitalRelease ?? inCinemas ?? physicalRelease`; Sonarr `firstAired`; Lidarr none), plus a `SORT_SPECS.released_at` + a Release-Date range facet; live-Plex walls already carry `originallyAvailableAt` (air/upload date); Audiobooks use ABS `publishedDate`. Kavita Books/Comics have no date in the list read → Release Date stays honestly absent from their registry (no fabricated column). A null `released_at` sorts NULLS-LAST like every nullable sort. | Should |
| R-169 | **Server-side per-user Library preferences — remember last view + last sort (owner rulings R1 + R6).** A user's chosen **view / group-by / sort (+ direction)** persists **SERVER-SIDE, per user, per wall** (a new `library_preferences` table; session-gated read/write; descriptive UI state, no audit rows) so the same account gets the same default on any device and a choice survives reload. Sensible per-kind DEFAULT the first time (R6: Movies/TV/Peloton/YouTube recently-added; Books/Comics/Audiobooks A–Z within their grouping). **The URL overrides for shared links** — an explicit `?view`/`?group`/`?sort` param WINS over the stored default and is NOT written back (a shared link shows the sharer's intent, not the recipient's saved preference); a bare URL is filled from the store, falling back to the default. | Should |
| R-170 | **Per-user watch/read-state facets (owner ruling R7 — "the big option", IN scope now).** Per-user **watched / in-progress** facets on the video walls and **read / in-progress** on Audiobooks, built on a reusable **app-user↔account mapping seam** (a new per-user table carrying plex.tv numeric id — auto-filled from the existing `resolvePlexIdentity` claim + friend matchers — plus ABS user id and Kavita username; mirrors the `users.plex_email`/`plex_username` override pattern). Video: the Tautulli history subset gains `user_id`, and the harvest attributes per-user watch state **ADDITIVELY** — the household `play_count`/`last_viewed_at`/`last_watched_*` the Trash walls depend on are UNTOUCHED. Audiobooks: ABS admin-token reads any user's `mediaProgress[]` via the map. **Kavita per-user read-state is DEFERRED** (no admin per-user progress read exists — needs per-user tokens / OIDC-linked Kavita); Books/Comics ship without read facets. Coverage is thin (live-verified household watch is sparse — radarr 3.8% / sonarr 18.8% / music 0%) so the facets are populated-value-gated. The mapping seam is the one the **Feed-attribution backlog reuses verbatim**. Per-user state never widens access (it is a facet on already-gated content, R-157). | Should |
| R-171 | **View/grouping is URL-synced, shareable, back/forward-restored (owner ruling R4; DESIGN-004 D-19).** The DESIGN-008 D-11 Library URL contract gains `?view` + `?group` dimensions (plus the new registry sort keys on `?sort`), so a view is a shareable deep link. History semantics ride D-19: **view switches + group drill-ins PUSH** (Back restores the prior view with its URL-synced filter/sort state), **refinements (sort, filter chips, A–Z jump, search, pagination) REPLACE**, and **canonicalizing redirects REPLACE**. Switching KIND tabs keeps only `?tab` (view/group/sort do not leak across kinds; the per-user preference refills the cleaned URL). | Should |

### MAM compliance governor (PLAN-039 — ADR-054 / DESIGN-027)

| ID | Requirement | Priority |
| --- | --- | --- |
| R-172 | **Cap-aware torrent-fallback pacing (owner: "how do we follow the seeding rules and update as my account level advances").** A governor (a ~15-min `@hnet/sync` CronJob) keeps automated MyAnonaMouse (MAM) grabs under the account's **unsatisfied-torrent cap** (New Member 20 → User 50 → PU 100 → VIP 150; exceeding it blocks downloads up to 24h — `.agents/context/2026-07-11-mam-rules-scrape.md`). MAM stays a rating-governed FALLBACK behind usenet; continuous 24/7 seeding raises the rank until the cap stops binding. | Must |
| R-173 | **Count unsatisfied LOCALLY from qBittorrent — ZERO new MAM API surface (compliance invariant).** The governor counts torrents in category `books-mam` that are still-downloading OR complete-but-`seeding_time < 72h` (conservative — owner ruling); it never calls MAM. qBittorrent's WebAPI answers unauthenticated from the cluster (no credential). Automation stays "Prowlarr search + `dynamicSeedbox.php`" only (OPS-013 §6). | Must |
| R-174 | **Gate the MAM fallback near the cap and re-enable when headroom returns.** When `unsatisfied ≥ MAM_UNSATISFIED_LIMIT − buffer` (limit default 20, buffer default 5) the governor PAUSES MAM grabs; when it drops below, it RESUMES. It gates by toggling the **MyAnonaMouse Prowlarr indexer's `enable` flag** (Prowlarr's fullSync application propagates it to LazyLibrarian, so LL stops querying MAM without any Torznab-error / provider-blocklist side-effect); usenet keeps flowing throughout (ADR-054). The limit/buffer resolve through ONE seam (`resolveGovernorConfig`), env-backed now; the owner bumps the limit at each MAM rank promotion (PLAN-040 makes it an audited admin setting). | Must |
| R-175 | **Fail-closed.** If the count cannot be obtained (qBittorrent unreachable), the governor treats the account as AT-CAP and pauses (fail-closed), so a counting outage can never let grabs breach the cap. | Must |
| R-176 | **State + transition audit.** A single-row `mam_gate_state` records the current gate + counts + limit/buffer; a `packages/domain` single-writer enqueues a Pushover event (via the existing `notification_outbox`, same-tx with the state write) ONLY on a gate transition (paused/resumed) or when headroom stays pinned at 0 for > 48h. First run records a baseline and pages nothing. | Must |
| R-177 | **v1 visibility = logs + Pushover only (owner ruling Q-03).** The governor surfaces its state through structured CronJob logs and the transition/stuck Pushover pings; an in-app Metrics tile is deferred (a PLAN-040 companion). | Should |

### Integrations tab — Goodreads requests MVP (PLAN-044 — ADR-055 / DESIGN-028)

| ID | Requirement | Priority |
| --- | --- | --- |
| R-178 | **Link an external account per-user with NO secret (owner ruling R1/R2).** A user links a **public Goodreads profile** — a profile URL, a vanity URL (`goodreads.com/<name>`, resolved server-side to the numeric id by following the redirect), or a bare numeric id. No OAuth, no token; the shelves must be PUBLIC. Linking is per-user (`user_integrations`, one row per (user, provider)); link/unlink are audited (`permission_audit`). The owner links first; the sister + members link after screenshot review. | Must |
| R-179 | **App-side end-to-end shelf sync (owner ruling R2).** A `goodreads-sync` `@hnet/sync` mode polls each linked user's public shelf RSS read-only, mirrors it (`integration_shelf_items`), matches each want against the `books_items` library mirror (normalized title/author — `books_items` has no ISBN column, a documented residual), and NEVER writes LazyLibrarian provider config (Prowlarr fullSync owns it — OPS-013). | Must |
| R-180 | **Every unmatched want is a Missing entry queued in LazyLibrarian, BOTH formats (owner ruling R3 + "both formats always").** A shelf want not in the library mints a `book_requests` row and pushes to LL via the confined write client: GB-volume → `addBook` → `queueBook` (MANDATORY — addBook alone lands `Skipped`) → `searchBook`, for ebook AND audiobook. Usenet rips immediately; MAM stays behind the PLAN-039 governor (untouched); LL/GB calls are politely PACED with mandatory retry/backoff. A format that never lands stays a per-format Missing entry. | Must |
| R-181 | **Coverage % (owner ruling R4).** The tab shows "we have N% of your shelf" — the fraction of the synced want shelf already in the library (a match, or a request landed). Comics count for coverage but never route to LazyLibrarian. | Must |
| R-182 | **Manual re-search on Missing (owner ruling R4 / the *arr idiom).** A Missing request offers a non-destructive "Search again" that fires a real LL `searchBook` and is AUDITED (`request_book_search`). | Must |
| R-183 | **Comics are parked out of the LazyLibrarian route (owner note 2026-07-13).** A shelf item classified as a comic / graphic novel (Google Books category, or a matched library comic) is NOT blind-fired into LazyLibrarian (Kapowarr's domain); it stays a Missing request with `unroutable_reason='comic'` and is surfaced honestly. Classification unreliable-from-RSS-alone is acceptable — absent a GB match the item defaults to the LL route with the caveat noted. Seeds the saga's book⇄audiobook pairing / comics-routing phase. | Should |
| R-184 | **The tab ships Admin-only (owner ruling — link-first-then-review).** A new `integrations` Section Permission (`disabled` no-row default ⇒ Admin-only; the `ytdlsub`/`books` precedent) gates the Integrations tab; the owner opens it per role after the screenshot review. The tab + every `integrations.*` procedure are server-authoritative. | Must |

### Comic acquisition — Kapowarr routing (PLAN-046 — ADR-056)

| ID   | Requirement                                                                                                                                                                                                                                        | Priority |
| ---- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| R-185 | **Comics route to Kapowarr, not LazyLibrarian (owner ruling 2026-07-14 — "route comics to Kapowarr… rest comic acquisition on top of books").** A comic-classified shelf want (R-183) resolves to a ComicVine volume via Kapowarr's own search and is added MONITORED (the *arr "Wanted") to Kapowarr, whose per-volume state reconciles into a third per-format status `book_requests.comic_status`. Kapowarr acquires from its OWN GetComics DDL sources — NEVER MAM/qBittorrent/Prowlarr/the PLAN-039 governor. Absent a ComicVine key / no match ⇒ the comic stays PARKED (honest degradation), never a fabricated add. | Must |
| R-186 | **The Kapowarr acquisition write surface is import-confined to `packages/domain`** (`@hnet/kapowarr/write` — add volume / set monitored / trigger `auto_search`), enforced by the extended arr-write-import-guard, exactly like `@hnet/arr/write` / `@hnet/lazylibrarian/write`. The confined write co-locates with the single-writer `book_requests` mutations; the sync-driven route/reconcile is unaudited (the synced-content exemption), the USER force-search is audited (`request_book_search`). | Must |
| R-187 | **One force-search endpoint for the whole book wall (feeds PLAN-045's Library "Force Search" button).** `integrations.search` DISPATCHES by format: a comic fires Kapowarr's `auto_search` task; a book/audiobook fires LL's `searchBook` — both gated by `integrations`, ownership re-checked, and audited identically. The comic leg is fully stub-backed + unit/e2e-covered (route → reconcile → force-search; absent-Kapowarr degradation; no-ComicVine-match parking). | Must |

### Integrations hub + Goodreads sub-section + composed Library-Wanted (PLAN-045 — ADR-057 / DESIGN-029)

| ID | Requirement | Priority |
| --- | --- | --- |
| R-188 | **ALL synced shelves acquire (owner ruling 2026-07-14 — A1 OVERRULED).** The Goodreads sync covers all four shelves (`to-read · currently-reading · read · did-not-finish` — `GOODREADS_SHELVES`, migration 0047 default + backfill); EVERY shelf's unmet items mint requests and push LL wants BOTH formats / route comics via Kapowarr — the same no-gate Missing flow, pacing and backoff paths reused verbatim. An ABSENT custom shelf (Goodreads 404s a never-created `did-not-finish`) reads as an EMPTY shelf, never an integration error (A3); a built-in shelf failure stays the error it is and never tombstones. | Must |
| R-189 | **Integrations is a HUB; providers are SUB-SECTIONS (owner spec — the "common theme" ruling).** `/integrations` renders provider cards (the Trash-Overview idiom) that PUSH into `/integrations/goodreads` (D-19); the sub-section is `?tab=` navigation (the Metrics/Trash precedent): a STATS page (the fixed link card, the WANT-SHELF headline coverage — Q-02 — with the per-shelf breakdown and request-phase summary tiles) and an ITEMS page — a REAL library wall (`MediaPoster` grid, cover-proxy art where matched, designed fallback tiles, the shared filter/sort chrome, corner-puck state badges — ADR-015). The v0.49.0 flat Requests & Missing wall folds into the sub-section. | Must |
| R-190 | **Shelf chips = the Helpdesk ticket state-chip semantics (owner spec).** The items wall filters by shelf with multi-select union chips + a SUPERSET "All", counts on every chip, repeated `?shelf=` params via replace, canonical-default-writes-no-param, the `shelf=none` empty sentinel, populated-value-gated chips (an absent shelf grows no chip). | Must |
| R-191 | **Library-Wanted (books) with full TV/Movies parity (owner rulings Q-01/Q-03).** The Books/Audiobooks/Comics walls surface household Wanted tiles COMPOSED from `book_requests` (`books.wanted` — books-section-gated; `books_items` stays a pure mirror, ADR-046): a clearly-badged Wanted strip + a registry-declared `?wanted=1` state filter (ADR-051 row), each tile badged by source shelf + requesters, deep-linking to its request context in the sub-section, and carrying the FORCE-SEARCH button (the dispatching `integrations.search`, PLAN-015-style reserved-slot feedback; affordances owner-scoped server-side). | Must |

### Platform & non-functional

| ID   | Requirement                                                                                                                                                                                                                                        | Priority |
| ---- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| R-60 | Responsive in browsers on phones, tablets, and PCs (viewport-fit layout + phone-width breakpoints; Playwright resize matrix proves it).                                                                                                            | Must     |
| R-61 | Light/dark theme via demo-console's CSS-token system (`data-theme` on `<html>`), with localStorage persistence and `prefers-color-scheme` seeding; no raw hex outside `tokens.css`.                                                                | Must     |
| R-62 | Postgres 16 (CNPG in-cluster); Drizzle ORM; migrations run as init container. Real Postgres in tests (embedded binary — no Docker locally).                                                                                                        | Must     |
| R-63 | Deployed from `ghcr.io/thaynes43/haynesnetwork` via haynes-ops Flux (bjw-s app-template, namespace `frontend`); secrets via External Secrets + 1Password.                                                                                          | Must     |
| R-64 | Staged rollout: internal `haynesnetwork.haynesops.com` first; claim `haynesnetwork.com` + `www` (traefik-external + Cloudflare tunnel) after e2e passes against staging.                                                                           | Must     |
| R-65 | PR flow with required CI checks (lint-and-typecheck, test, build) after the bootstrap GATE A cutover; conventional commits + release-please; squash merges.                                                                                        | Must     |
| R-66 | UI testing with Playwright, including the login flow (stub OIDC in CI) and per-role dashboard visibility.                                                                                                                                          | Must     |
| R-73 | Published images are cryptographically signed (keyless cosign via GitHub Actions OIDC → Fulcio/Rekor) in the release run and verified in-run; the cluster admits **only** signed `haynesnetwork` images once enforcement is on (ADR-020, OPS-006). | Must     |

## User stories

| ID    | Story                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Acceptance criteria |
| ----- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------- |
| US-01 | As a user, I sign in with my Plex account (via Authentik) and land on my dashboard.                                                                                                                                                                                                                                                                                                                                                                               | AC-01, AC-02        |
| US-02 | As the owner, my first login makes me Admin without any manual DB step.                                                                                                                                                                                                                                                                                                                                                                                           | AC-03               |
| US-03 | As a Member, I see Seerr and the Plex tiles by default and can open them.                                                                                                                                                                                                                                                                                                                                                                                         | AC-04               |
| US-04 | As an Admin, I give a user Immich and it appears on their dashboard without their re-login. _(Amended by ADR-012: done by assigning a **role** whose app set includes Immich — or adding Immich to their current role — not a per-user grant.)_                                                                                                                                                                                                                   | AC-05               |
| US-05 | As an Admin, I create a **`Family` role** (Phase 3: family designation + family libraries) and assign a user; they can now add HNet Home Videos on any Plex server. _(Amended by ADR-012: "family" is an admin-created role, not a tag/flag; library gating lands in Phase 3, C-09.)_                                                                                                                                                                             | AC-06               |
| US-06 | As a Member, a movie won't play; I find it in the app, hit Fix, pick "won't play / corrupt", and the *arr blocklists that release and searches for another.                                                                                                                                                                                                                                                                                                       | AC-07, AC-08        |
| US-07 | As an Admin, Radarr's DB is lost; I diff the ledger against the fresh instance and re-add everything monitored with correct profiles.                                                                                                                                                                                                                                                                                                                             | AC-09               |
| US-08 | As a user on my phone, the dashboard and fix flow are fully usable without horizontal scrolling.                                                                                                                                                                                                                                                                                                                                                                  | AC-10               |
| US-09 | As a household admin, last week's disk sweep removed 200 movies (and thousands sit unmonitored with nothing on disk); I open Ledger → Movies, filter to `removed` / `unmonitored + no file` (by vote tier), select a batch, **Add & search in Radarr**, and also **Export** the list as a backup.                                                                                                                                                                 | AC-11, AC-12, AC-13 |
| US-10 | As a household admin, I open **Trash → Movies**, see what Maintainerr will delete and when + how much space it frees, **Save** a title I want to keep (it can never be watched off), **Expedite** a batch I'm done with, and give a trusted user a role that can Save but not Expedite. Anything watched on any of my three Plex servers recently is already protected.                                                                                           | AC-14, AC-15, AC-16 |
| US-11 | As a household admin, I create a **batch** from what Maintainerr proposes, scroll a poster wall on my phone tapping X→lock to rescue keepers, **green-light** it (a "Leaving Soon" collection appears on Plex for a few weeks), and let my users save what they still want; when the window closes the rest is deleted one item at a time — anything watched, requested, or freshly saved is spared, and I get a record of what went and how much space it freed. | AC-17, AC-18, AC-19 |

## Acceptance criteria

| ID    | Criterion                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| AC-01 | Unauthenticated visit → single "Sign in" → Authentik → back, session established (7-day cookie). No password form exists.                                                                                                                                                                                                                                                                                                                                                                                      |
| AC-02 | First login creates a Member row; profile shows displayName/email from OIDC claims.                                                                                                                                                                                                                                                                                                                                                                                                                            |
| AC-03 | Login with an email on `BOOTSTRAP_ADMIN_EMAILS` yields role Admin and a `user_role_transitions` audit row with system initiator; repeat logins are no-ops.                                                                                                                                                                                                                                                                                                                                                     |
| AC-04 | A fresh Member's dashboard shows exactly the default-visible catalog entries; every dashboard href is a valid http(s) URL. _(Amended by ADR-013: relaxed from `https://*.haynesnetwork.com/*` — arbitrary normalized http(s) URLs are allowed.)_                                                                                                                                                                                                                                                               |
| AC-05 | Admin assigns a role that includes the app → user's next dashboard query (or live refresh) includes the tile; changing to a role without it removes it; role assignment is audit-logged (`user_role_transitions`). _(Amended by ADR-012: role assignment, not a per-user grant/revoke.)_                                                                                                                                                                                                                       |
| AC-06 | ~~Tag application yields the union of tag + direct permissions; removing the tag removes only tag-derived permissions.~~ **Superseded by ADR-012:** a user's effective apps are exactly their role's app set (no union); editing a role's apps changes every member's effective set immediately.                                                                                                                                                                                                               |
| AC-07 | Fix on an item with grab history calls the *arr's history-failed endpoint (blocklist) then a search command; the fix record stores reason + actions + *arr responses.                                                                                                                                                                                                                                                                                                                                          |
| AC-08 | Fix without grab history deletes the file(s) and triggers search; outcome recorded.                                                                                                                                                                                                                                                                                                                                                                                                                            |
| AC-09 | Restore preview lists exactly the ledger items absent from the target *arr; execution re-adds them monitored with stored quality profile, root folder, and tags; report shows successes/failures.                                                                                                                                                                                                                                                                                                              |
| AC-10 | Playwright resize matrix passes at 375×667, 390×844, 412×915, 768×1024, 820×1180, 1280×800, 1920×1080, 2560×1440: no page-level scrollbars, no off-screen controls, panes scroll internally.                                                                                                                                                                                                                                                                                                                   |
| AC-11 | A bulk Add-&-search over N filtered Ledger items adds exactly those absent from the live *arr (monitored, stored profile/root/tags), sets monitored on those present-but-unmonitored, skips those already present + monitored, triggers a search per acted item, and reports per-item added/monitored/skipped/failed. A searched run over 1000 items is refused before any *arr write.                                                                                                                         |
| AC-12 | Ledger export produces a deterministic JSONL file listing exactly the filtered set with the external ids needed to re-import into the target *arr.                                                                                                                                                                                                                                                                                                                                                             |
| AC-13 | A role set to **Disabled** for Ledger never sees the nav entry or route; **Read-Only** sees browse + export but no Add-&-search control, and the mutation is server-refused (FORBIDDEN) even if called directly. _(Amended by ADR-032 (2026-07-07): the "nav entry" is now the role-gated **user-menu** item — the top row is the universal Home · Library · Trash · Bulletin rail — and the Ledger's **no-row default is Disabled**, so out of the box only admins see it; a role row opts members back in.)_ |
| AC-14 | Trash → Movies/TV lists exactly Maintainerr's pending items for that kind, each with its scheduled-delete date + space freed, plus a total-space figure; an item known to the ledger shows its poster/title/facets, an unknown one still lists with Maintainerr's fields.                                                                                                                                                                                                                                      |
| AC-15 | Save adds the item to Maintainerr's exclusion list and records a `trash_excluded` event (idempotent); Expedite is refused with a safety error when the Maintainerr audit is not SAFE, and a recently-watched/requested item is auto-protected rather than deleted; music is never a Trash target.                                                                                                                                                                                                              |
| AC-16 | Trash section **Disabled** ⇒ no nav/route (redirect); **Read-Only** browses but each write action is server-refused unless its per-action grant is present (FORBIDDEN even if called directly); `edit_rules` additionally requires section **Edit**.                                                                                                                                                                                                                                                           |
| AC-17 | Creating a batch snapshots the current pending set for one kind into `admin_review` (or, with the skip-gate on, straight to `leaving_soon` with `gate_skipped=true` + a system-attributed audit); a second open batch for the same kind is refused (`TRASH_BATCH_ALREADY_OPEN`); `draft`/`admin_review` can never reach `deleted` without passing through `leaving_soon`.                                                                                                                                      |
| AC-18 | Green-light moves `admin_review→leaving_soon`, sets `expires_at = now + window_days`, and creates the Plex-visible "Leaving Soon" collection; a save flips the item `pending→saved` (Maintainerr exclusion + a `trash_batch_saves` row + removal from the collection); a member with `save_leaving_soon` can save during the window but not manage the batch, and vice-versa for `manage_batches`.                                                                                                             |
| AC-19 | The expiry sweep deletes only cold, positively-evaluated survivors one item at a time (recently-watched/requested/`dnd`/unresolved/live-excluded/saved items are kept and land `skipped`/`saved`); an unsafe Maintainerr audit refuses the whole sweep; each deletion writes a `trash_expedited` event + freezes the size/resolution/rating snapshot same-tx; the batch closes `deleted` with per-item counts.                                                                                                 |

## Phasing

| Phase | Scope                                                                                                                       |
| ----- | --------------------------------------------------------------------------------------------------------------------------- |
| 1     | R-01..R-15, R-20..R-22, R-60..R-66 — auth, dashboard, admin permissions, roles (tags→roles per ADR-012), deploy to staging. |
| 2     | R-40..R-52 — *arr ledger, fix with reasons, Seerr attribution, failsafe restore.                                            |
| 3     | R-25..R-28 — Plex library self-service + family libraries.                                                                  |
| Later | R-30 Authentik permission sync; Maintainerr integration; root-domain cutover per R-64 gates on Phase 1 e2e.                 |

## Open questions

| ID   | Question                                                                                    | Resolution                                                                                                                                                                                                                                                                                                                                                                                                        |
| ---- | ------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Q-01 | Which email does Authentik emit for the owner (manofoz@ vs t.haynes43@)?                    | Mitigated: allowlist contains both; confirm on first real login.                                                                                                                                                                                                                                                                                                                                                  |
| Q-02 | Seerr cutover timing (owner migrating in parallel) — when does the tile URL flip?           | Owner action; catalog edit when ready.                                                                                                                                                                                                                                                                                                                                                                            |
| Q-03 | Do plexops/k8plex share library names with legacy Plex (affects per-server allowed-set UX)? | **Resolved by ADR-017 / DESIGN-007**: names differ (HAYNESOPS mirrors Movies/TV as `HOps Movies`/`HOps TV Shows` vs `HNet *`); library identity is `(server, section_key)`, never name — the `plex_libraries` UNIQUE(`server_id`,`section_key`) models the mirror explicitly (verified live 2026-07-06).                                                                                                          |
| Q-04 | Maintainerr deployment timing (for deletion attribution enrichment).                        | **Resolved by ADR-023 / DESIGN-010 (PLAN-006):** Maintainerr is deployed (3.17.0, `media` ns); the **Trash** section integrates it as the deletion system of record (read-through pending + a confined write surface: Save/Expedite/Rules), gated by Section Permission (VIEW) + Trash Action Grants (writes), behind a preflight safety audit. Backend vertical shipped 2026-07-06; the Trash UX is a follow-up. |
| Q-05 | Per-user fix rate limits — fixed default or admin-configurable?                             | Default constant in Phase 2; revisit (R-47).                                                                                                                                                                                                                                                                                                                                                                      |
| Q-06 | **ytdl-sub durable posters (R-124):** where do the replacement poster files live, and where should the durable home be — a git repo (which one), a PVC on gasha01, or a Kometa-style overlay re-applied after each ytdl-sub write? And should the app serve the durable poster directly, or just read Plex after the store re-applies it? | **Open — owner, morning (PLAN-022).** Resilient display (proxy + fallback tile) ships now; the durable **store** is a Phase-2 follow-up once the file location is known. ADR-038 C-09 / DESIGN-017 Q-01. |
